# 工具使用记录（Kun 运行时备忘）

> 用途：记录本环境可用工具与实战踩坑，防止跨会话/长时间开发遗忘。与《开发进度记录.md》配合使用。
> 更新原则：发现新坑立即补记。

## 一、可用工具清单（按用途分类）

### 文件与代码
| 工具 | 用途 | 关键注意事项 |
|---|---|---|
| `read` | 读文件（分页，next_offset 续读） | 大文件 read 可能显示异常（如缩进显示丢失），以 `python -c "ast.parse(...)"` / `py_compile` 为准 |
| `write` | 创建/整体覆盖文件 | **大文件（>8KB）多次出现内容截断或缩进损坏**（report.py/test_report/auth.py 均踩过）；写后必须验证（wc -l / ast.parse）；损坏时用 bash heredoc（quoted 'PYEOF'）或 edit 修复 |
| `edit` | 精准替换 | 有 read-before-edit 守卫：**先 read 该文件再 edit**；oldText 必须与磁盘逐字符一致；多个片段须互不重叠且唯一 |
| `ls` / `glob` | 列目录/找文件 | |
| `grep` | 内容搜索 | **path 必须是目录**（传文件会报 ENOTDIR）；大目录会命中 dist 等垃圾，加 glob 过滤 |
| `repo_map` | 代码地图（符号/依赖/热度排序） | 改代码前先用它定位 |
| `lsp` | TS/Python 符号跳转/诊断 | 需要对应 language server 已装 |

### Shell 与进程
| 工具 | 用途 | 关键注意事项 |
|---|---|---|
| `bash` | 执行命令（同步或 background） | **必须传 command 参数**（空参数报 "command is required"）；**相同命令连续 3 次触发 repeat-loop guard 直接拒绝**，失败后先换个方式再试；Windows Git-bash 环境，路径用 /g/phpstudy_pro/进销存 风格 |
| `background_shell` | 管理后台会话（list/read/poll/write/stop） | 长任务用 bash background=true，轮询结果后再交付；后台 uvicorn 用完要 taskkill（netstat -ano | grep 端口） |

### 项目开发专用
| 工具 | 用途 | 关键注意事项 |
|---|---|---|
| `todo_write` / `todo_list` | 阶段进度跟踪 | 每阶段开始/结束更新 |
| `verify_changes` | 变更验收 | 本项目实际用：pytest + 前端 build + curl 冒烟 |
| `git_inspect` | 只读 git 操作 | 提交用 bash git |

### 交互与其他
`request_user_input`/`user_input`（结构化提问，一次问完）、`delegate_task`（子代理，大任务并行）、`design_component`（UI 原型）、`web_fetch`/`browser_use`（网络）、`mcp_*`（外部能力）、`memory_create/update/delete`（长期记忆，需用户批准）、`get_goal/update_goal`（目标）、`send_im_attachment`（发文件）、`office_inspect/office_edit/office_preview`（Office 文档）、`echo`（回显）。

## 二、本项目实战踩坑（重要）

1. **pytest 输出乱码**：Windows 下必须 `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m pytest -q`，否则中文变乱码无法读。
2. **测试库与其他测试共享**：全局计数断言一律用增量（before/after）或按本测试创建的商品/仓库/用户过滤（test_report/test_admin 已按此规范）。
3. **.env 路径反斜杠**：dotenv 会把 `\p` `\E` 等解析成转义（\x1b/\x08）→ Windows 路径必须写**正斜杠**（G:/phpstudy_pro/...）。
4. **mysqldump**：路径在 .env `BACKUP_MYSQLDUMP=G:/phpstudy_pro/Extensions/MySQL5.7.26/bin/mysqldump.exe`；输出用 stdout 重定向到文件（capture_output 会丢数据）；密码走 MYSQL_PWD 环境变量。
5. **MySQL 表结构变更四层同步**：init.sql（建库脚本）+ 本地库 ALTER/CREATE（幂等 INSERT IGNORE）+ models 映射 + 测试。改表后先同步本地库再跑测试。
6. **数量 Decimal 尾零**：Numeric(12,3) 返回 '30.000'，报表/货架图输出统一走 `_fmt_qty`（quantize 0.001 去尾零）。
7. **FastAPI 路由顺序**：静态路径（/products/export）必须在动态（/{id}）之前注册；static_router 先行。
8. **权限点**：init.sql sys_permission id 固定（1-24，24=dept:manage 单位管理）；新增权限点需同步 INSERT + 超管角色映射（SELECT 1,id FROM sys_permission 全量）。
9. **git 提交**：先 `git add -A` 检查 status（注意 .env 被 .gitignore 忽略，勿强加）；提交信息用中文总结交付+验证；**用户/并行会话可能同时改文件**（曾出现 Home.tsx 被删、AppLayout 重构），提交前 git status 确认，避免覆盖他人工作。
10. **前后端开发端口**：后端 HTTPS 8443（自签名），desktop dev 5173，mobile dev 5174；生产 nginx：/ → desktop、/m/ → mobile（vite base=/m/ + router basename=/m/）。
11. **跨端入口**：otherEndUrl 开发环境同主机换端口（兼容局域网 IP），生产直达登录页（/login、/m/login）；Landing 优先级=用户选择(localStorage wlt_entry) > 已登录 > 设备检测；用户选择优先可防手机点电脑版入口被弹回循环。
12. **write 大文件保命**：写完立刻 `python -m py_compile` + 看关键函数数；损坏用 bash heredoc（quoted delimiter）重写，别用 write 重试。

13. **批量替换脚本两大坑**：① re.sub 回调误用 `m.group(1)`（带引号整体）会拼出三段式垃圾，必须用 `m.group(0)[1:-1]` 处理字符串字面量；② 匹配 `const [x, setX] = useState` 时不能只匹配到 `useState` 就截断——`useState<T>([])` 泛型会被拆行导致 TS1003；替换前先小样本实验（python -c），替换后 tsc 全量验证。
