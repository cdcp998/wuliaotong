# 会话交接 —— UI 对齐 OP 设计（2026-08-23）

> 目标：把「导航管理/侧边栏/顶部栏/统计面板/地图工作台」按 OpenPencil 设计（OP）对齐，全站页面满宽平铺，并记录使用文档。
> 本文件供新会话直接复制全文继续任务。

## 一、环境与入口

- 工作目录：`G:\wuliaotong_dev`（前端 `frontend/`，后端 `backend/`）
- 设计基准：`AI开发文档/OpenPencil设计稿/物料通UI全界面设计.op`（62 页，可用 OpenPencil MCP 打开，当前 MCP 服务已载入该文档）；文字版规格 `AI开发文档/UI设计交付文档.md`
- 实机：桌面 dev `https://localhost:5174`（HMR 生效）、手机 5175、后端 `https://localhost:8443`，均在运行
- 登录：`admin / admin123`
- 视觉代理：hohai，baseURL `https://api.hohai.eu.org/v1`，Key 在 `C:\Users\CDCP\.dsh\.credentials.yaml`（HOHAI_API_KEY），模型 `deepseek-v4-flash-vision-exp`（会返回 reasoning_content，脚本已做回退）
- 本机无视觉能力；COI 适配器 kimi/hermes 未安装（spawn ENOENT），codex 可用

## 二、已完成（本轮全部修改，typecheck ✓ / wlt-desktop build ✓）

1. **AppLayout.tsx（侧栏+顶栏）**
   - 侧栏：232px 白底 `#FFFFFF` + 右描边 `#E4EAF6`；保留 Logo / 「搜索导航…」搜索框 / 「全部展开·全部收缩」按钮；Menu `flex:1 + overflowY:auto` 可滚动
   - 顶栏：60px 白底 + 下描边；折叠按钮 34×34 白底描边圆角；面包屑「物料通 / 当前页」13px；全局搜索框（描边 #CBD6EC）；铃铛白底描边按钮；用户 chip：`#F6F8FE` 浅底胶囊 + 26px 圆形头像（`#EAEFFF` 底 `#3B5BDB` 字）+ 姓名/角色
   - 宽屏动态菜单加载后自动全展开（可手动收缩）；≤992 只展开当前分组
2. **MapView.tsx**：新增 `onMapReady`（回传 Leaflet map 实例）；移除内置 ZoomControl 与右下 SourceInfoBadge
3. **MapWorkbench.tsx**：重写为设计页 46「地图全工作区」——无页头/无内边距/无左侧面板；左上图源 pill（Dropdown 切换底图）；右上玻璃工具栏（位置/测距/图层/刷新/标记，激活品牌蓝）；图层下拉（线缆/故障/设备/标记 行开关）；测距浮层卡；左下故障导航 pill+图例 pill；右下自定义缩放(+/-)+指北回正
4. **Dashboard.tsx**：按设计页 13 重做——副标题去掉问候语；页头 3 动作按钮白底+描边 #CBD6EC+品牌图标（新建采购入库/领用审计/新建盘点）+刷新；4 统计卡（数值 22/700 彩色在上、标签 12.5 灰在下：今日入库蓝/今日出库青/库存预警红/待审计橙，点击直达）；趋势卡图例胶囊（#EAEFFF/#E0F2FE）+双柱（入库 #5B7FFF/出库 #7CC4E8，柱 24px 圆角 6）；待办清单卡（标题+总数胶囊 #EFF3FC、行 #F6F8FE 圆角+彩色圆点+数量）；底部 5 张快捷入口卡（图标 34px #F6F8FE 底）
5. **Menus.tsx**：设计页 52 两栏——左菜单树卡+右「侧边栏预览」250px；去掉第三栏提示卡；副标题更新；树行胶囊改为 999 圆角品牌色（路径/权限 #EAEFFF、公开 #E8F9EF、已隐藏 #FDEBEC）；行内 ＋✎ 品牌蓝；弹窗 440px
6. **global.css**：`body{margin:0}`（修复 16px 页面溢出）；`.ant-layout-sider-children` 弹性列（修复侧栏无法滚动）；侧栏分组标题 11px #8A93A8 / 菜单项 12.5px / 选中 #EAEFFF+#3B5BDB；`.wlt-menu-tree` 树行浅底圆角；`.wlt-page` flex 列 + flex:1（地图全工作区高度铺满）
7. **满宽平铺**：14 个页面移除 `maxWidth:1480/1560, margin:"0 auto"`：Dashboard/Menus/Users/Roles/StockQuery/CableList/CableFaults/DeviceList/DeviceTasks/Knowledge/KnowledgeWrite/MapCache/TaskBoard/TaskList
8. **工具脚本**（已可用，保留）：
   - `scripts/verify-ui.cdp.mjs`：Chrome headless+CDP 登录 admin → 4 页截图（/dashboard /system/menus /cable/map /system/users）
   - `scripts/prep-compare.ps1`：图片压成 1200px JPEG
   - `scripts/vision-compare.mjs`：视觉模型比对 OP 基准图 vs 实机截图（输出 `AI开发文档/design-ref/compare_*.md`）
   - `scripts/probe-map.mjs`：诊断页面溢出/容器高度
9. **文档**：新建 `dsh-usage/OP设计使用文档.md`（设计源/Token 机制/各界面对齐内容/核验结果/待对齐项/实施建议）

## 三、验证现状

- 已截图（最新一轮）：`AI开发文档/screenshots/desktop_1440_{dashboard,system_menus,cable_map,system_users}.png`
- 视觉比对已跑：`AI开发文档/design-ref/compare_dashboard.md`、`compare_menus.md`（结论齐全）；map/users 只拿到 reasoning（`compare_map.md`、`compare_users.md` 部分）
- **注意**：body margin 0 与侧栏滚动修复是在上一轮截图之后落的，最新截图尚未再做视觉比对

## 四、遗留（新会话建议顺序）

1. Menus.tsx 剩余小项：删除页头「刷新」按钮、删除预览卡「管理员/当前账号」Tag（vision 报告指出，均 5 分钟量）
2. Dashboard 快捷入口若想在宽屏严格一行 5 张：把 `.wlt-grid` 换成 `display:flex;gap:12` + 每卡 `flex:1 1 150px`（当前 auto-fit 220 可能换行）
3. 重新跑 `node scripts/verify-ui.cdp.mjs` + `node scripts/vision-compare.mjs`（四页全量）确认最终效果
4. 向 `AI开发文档/开发进度记录.md` 顶部追加本轮记录（格式仿已有条目：需求/交付/验证/说明）
5. 删除临时文件 `backend/scripts/_tmp_dump_llm.py`
6. git 提交（typecheck+build 已绿）：`style(web): 侧栏/顶栏/统计面板/地图工作台/导航管理按OP设计对齐+全站满宽平铺`

## 五、关键命令

```bash
cd G:\wuliaotong_dev\frontend
npm run typecheck
npm run build -w wlt-desktop
node ../scripts/verify-ui.cdp.mjs        # 截图（需 5174 在跑）
powershell -File ../scripts/prep-compare.ps1   # 压图（可选）
node ../scripts/vision-compare.mjs       # 视觉比对
```

## 六、踩坑备忘

- read_image 无视觉 → 用 `scripts/vision-compare.mjs` 走 hohai 代理的 vision-exp 模型；该模型常把结论写在 reasoning_content，脚本已 `content || reasoning_content` 回退
- OpenPencil MCP：`set_active_page(index)` 0 基；`export_frames(outputDir)` 导出当前页顶层 frame PNG；`read_nodes(pageId/nodeIds,depth)` 读节点结构
- antd Sider 内容包在 `.ant-layout-sider-children` 里，flex 滚动必须作用在该层
- 页面级 `body{margin:0}` 没写之前，整页有 16px 溢出（截图 1424 宽、右侧滚动条）
