# 物料通管理系统 —— 数据库表结构设计

> 数据库：MySQL 8.0+，InnoDB，utf8mb4
> 依据：《需求大纲.md》（2026-03-31 已确认版）

## 0. 设计约定

- 命名：`模块前缀_表名`，主键 `id BIGINT AUTO_INCREMENT`，所有表含 `created_at`、`updated_at DATETIME`
- 金额 `DECIMAL(12,2)`，数量 `DECIMAL(12,3)`（兼容多单位换算小数）
- 单据编号规则：`{前缀}{yyyyMMdd}{4位流水}`，前缀：RK-采购入库 / LL-领用 / DB-调拨 / PD-盘点 / QT-其他出入库 / QCK-期初，规则存 `sys_config`
- **库存一致性铁律**：任何库存变动必须走「单据 → 确认 → 同一事务内写 `stk_stock_log` 流水 + 更新 `stk_stock` 实时库存」，禁止直接改库存表
- 逻辑删除：单据类用状态字段（作废/取消），基础资料用 `status` 停用，不做物理删除

## 1. 表清单（共 39 张）

| 分组 | 表 |
|---|---|
| 用户权限 | sys_user, sys_role, sys_permission, sys_role_permission, sys_session, **sys_register_apply(注册审核)** |
| 系统 | sys_config, sys_operation_log, sys_notification, sys_backup_log, sys_file, sys_storage |
| 基础资料 | base_supplier, base_category, base_product, base_unit, base_product_unit, base_warehouse, base_shelf, base_location, **base_department(组织单位), base_department_shelf(单位-货架)** |
| 库存核心 | stk_stock, stk_stock_log, stk_opening, stk_opening_item |
| 采购 | pch_purchase_in, pch_purchase_in_item |
| 领用 | out_requisition, out_requisition_item |
| 其他出入库 | stk_other_io, stk_other_io_item |
| 调拨/盘点 | stk_transfer, stk_transfer_item, stk_check, stk_check_item |
| OCR/大模型 | ocr_record, ai_suggestion |

---

## 2. 表结构

### 2.1 用户与权限

**sys_user 用户**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| username | VARCHAR(50) UNIQUE | 登录名 |
| password_hash | VARCHAR(255) | bcrypt 加密 |
| real_name | VARCHAR(50) | 姓名 |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(100) | 邮箱（找回密码用） |
| role_id | BIGINT | → sys_role.id |
| status | TINYINT | 1 启用 / 0 停用 |
| last_login_at | DATETIME | |

**sys_role 角色**：id, code UNIQUE, name, description, is_builtin(内置角色不可删), department_id(所属单位 → base_department.id，控制可见货架), created_at

**sys_register_apply 注册申请**（审核注册模式）：id, username, password_hash, real_name, phone, email, status(0 待审核/1 通过/2 拒绝), handled_by, handled_at, created_at

**sys_permission 权限点**：id, parent_id(菜单父子), name, code, type(1 菜单 / 2 按钮), sort

**sys_role_permission 角色权限**：id, role_id, permission_id — UK(role_id, permission_id)

**sys_session 会话表**（Session 认证，多进程共享、支持踢人下线）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| session_id | VARCHAR(64) UNIQUE | 随机 token |
| user_id | BIGINT | → sys_user.id |
| ip, user_agent | VARCHAR | 登录信息 |
| expire_at | DATETIME | 过期时间（滑动续期） |

### 2.2 系统

**sys_config 系统配置**：id, config_key UNIQUE, config_value TEXT, remark
> 存放：公司信息、单据编号规则、大模型 API Key/BaseURL（豆包/DeepSeek）、OCR 引擎选择与参数（rapidocr/paddle）、预警参数、**注册模式 auth.register_mode（open/closed/review）、找回方式 auth.forgot_method（email/phone/both）、管理员电话 site.contact_phone、SMTP smtp.host/port/user/password/from** 等

**sys_operation_log 操作日志**：id, user_id, username, module, action, method, url, params TEXT(JSON), ip, user_agent, duration_ms, created_at
> 索引：idx(user_id, created_at), idx(module, created_at)

**sys_notification 站内通知**：id, user_id, title, content, biz_type(预警/待办/审批), is_read, created_at

**sys_backup_log 备份记录**：id, file_path, file_size, backup_type(自动/手动), status, created_at

**sys_file 文件/照片表**（所有照片统一入口）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| biz_type | VARCHAR(30) | 归属：purchase_bill 送货单 / purchase_item 入库库位 / requisition_item 出库商品 / product 商品图 / other |
| biz_id | BIGINT | 对应单据或明细 id |
| storage_id | BIGINT | 存储位置 → sys_storage.id（多存储地址） |
| original_name | VARCHAR(255) | 原文件名 |
| file_path | VARCHAR(255) | 相对存储根目录的文件路径 |
| file_size | BIGINT | 字节 |
| md5 | CHAR(32) | 去重 |
| uploader_id | BIGINT | 上传人 |
| created_at | DATETIME | |

> 索引：idx(biz_type, biz_id), idx(storage_id)。照片**永久保存不清理**。

**sys_storage 存储位置表**（多存储地址，后台管理）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| name | VARCHAR(50) | 名称（如 本地磁盘D盘） |
| type | VARCHAR(20) | local 本地目录（当前仅本地；网络盘/NAS 后期扩展） |
| path | VARCHAR(500) | 存储路径：绝对路径或相对 backend/ 的目录（如 data/files） |
| policy | VARCHAR(10) | 选择策略：fill 最空闲（默认）/ round 轮询 / manual 手动指定 |
| is_default | TINYINT | 1 默认（manual 策略未指定时使用） |
| status | TINYINT | 1 启用 / 0 停用（停用不再被选择） |
| remark | VARCHAR(255) | |
| created_at / updated_at | DATETIME | |

### 2.3 基础资料

**base_supplier 供应商**：id, code, name, contact, phone, address, remark, status, created_at

**base_category 商品分类（多级）**：id, parent_id(0 为顶级), name, path(如 /1/5/), sort

**base_product 商品**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| code | VARCHAR(50) UNIQUE | 商品编码（**纯数字**，留空自动生成=最大数字+1） |
| material_code | VARCHAR(50) | **物料编码（公司系统编码，可空，空则提示管理员补录）** |
| barcode | VARCHAR(50) | 条码（可空，扫码用） |
| sku | VARCHAR(50) | SKU |
| name | VARCHAR(100) | 名称 |
| category_id | BIGINT | → base_category.id |
| spec | VARCHAR(100) | 规格/型号 |
| unit_id | BIGINT | 基本单位 → base_unit.id |
| purchase_price | DECIMAL(12,2) | 进价 |
| min_stock / max_stock | DECIMAL(12,3) | 库存预警上下限 |
| image_file_id | BIGINT | 商品图片 → sys_file.id |
| status | TINYINT | 1 启用 / 0 停用 |

> 索引：idx(barcode), idx(name), idx(category_id)

**base_unit 计量单位**：id, name(件/箱/包), remark

**base_product_unit 商品多单位换算**：id, product_id, unit_id, rate(相对基本单位倍数), is_default — UK(product_id, unit_id)

**base_warehouse 仓库**：id, code, name, address, manager_id, remark, status

**base_shelf 货架**：id, warehouse_id, code(如 J01), name, remark — idx(warehouse_id)

**base_department 组织单位（部门）**：id, code UNIQUE, name, remark, status
**base_department_shelf 单位-货架关联**：id, department_id, shelf_id — UK(department_id, shelf_id)
> 角色所属单位下的用户（非超管/管理者）仅能查看/管理本单位关联的货架（2D 货架图与库位选择同源过滤）

**base_location 库位（仓库-货架-层 三级）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| warehouse_id | BIGINT | 仓库 |
| shelf_id | BIGINT | 货架 |
| layer_no | INT | 层号 |
| code | VARCHAR(50) UNIQUE | 库位编码，如 CK01-J01-03 |
| remark | VARCHAR(255) | |

### 2.4 库存核心

**stk_stock 实时库存**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| product_id | BIGINT | 商品 |
| warehouse_id | BIGINT | 仓库 |
| location_id | BIGINT | 库位 |
| qty | DECIMAL(12,3) | 当前数量 |
| cost_price | DECIMAL(12,2) | 移动加权平均成本 |

> **UK(product_id, warehouse_id, location_id)**；idx(warehouse_id, location_id)。百万件是**总件数**（数量级），本表按 SKU×库位记录，行数远小于此，查询无压力。

**stk_stock_log 库存流水**（一切库存变动的唯一事实来源）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| product_id | BIGINT | 商品 |
| warehouse_id / location_id | BIGINT | 仓库/库位 |
| change_type | VARCHAR(20) | 采购入库/领用出库/报废/报损/盘盈/盘亏/调拨入/调拨出/期初/其他入/其他出 |
| bill_type / bill_no | VARCHAR | 来源单据（如 pch_purchase_in / RK202604010001） |
| bill_item_id | BIGINT | 来源单据明细 id |
| before_qty / change_qty / after_qty | DECIMAL(12,3) | 变动前/变动量/变动后 |
| cost_price | DECIMAL(12,2) | 本次成本单价 |
| photo_file_id | BIGINT | 拍照留底 → sys_file.id（可空） |
| operator_id | BIGINT | 操作人 |
| remark | VARCHAR(255) | |

> 索引：idx(product_id, created_at), idx(bill_no), idx(created_at)。**报表全部从本表聚合**，可随时据此重建 stk_stock 对账。

**stk_opening 期初库存单**：id, bill_no UNIQUE, warehouse_id, status(0 草稿 / 1 已过账), remark, creator_id, created_at
**stk_opening_item 期初明细**：id, bill_id, product_id, location_id, qty, cost_price
> 人工录入 + Excel 导入；过账时生成期初流水并写入 stk_stock

### 2.5 采购

**pch_purchase_in 采购入库单**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| bill_no | VARCHAR(30) UNIQUE | 单号 RK... |
| supplier_id | BIGINT | 供应商（可空） |
| warehouse_id | BIGINT | 入库仓库 |
| total_qty / total_amount | DECIMAL | 汇总 |
| status | TINYINT | 1 已入库 / 0 草稿 / -1 作废 |
| bill_date | DATETIME | 单据日期 |
| operator_id | BIGINT | 操作人 |
| remark | VARCHAR(255) | |

**pch_purchase_in_item 采购入库明细**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| bill_id | BIGINT | → 单头 |
| product_id | BIGINT | 商品 |
| qty | DECIMAL(12,3) | 数量 |
| unit_name | VARCHAR(20) | 本次单位（快照） |
| price / amount | DECIMAL(12,2) | 进价/金额 |
| location_id | BIGINT | 存储位置（必选库位） |
| photo_file_id | BIGINT | 库位拍照留底（不强制） |

> 入库确认 = 事务：写流水 + 更新 stk_stock（含移动加权成本）+ 更新单据状态。送货单照片挂 sys_file(biz_type=purchase_bill)。

### 2.6 领用出库（使用者手机端）

**out_requisition 领用申请单**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| bill_no | VARCHAR(30) UNIQUE | 单号 LL... |
| applicant_id | BIGINT | 申请人（使用者） |
| use_location | VARCHAR(100) | **使用地点（必填）** |
| use_reason | VARCHAR(255) | **因何使用（必填）** |
| location_photo_file_id | BIGINT | 使用地点照片（不强制） |
| warehouse_id | BIGINT | 出库仓库 |
| total_qty | DECIMAL(12,3) | 汇总 |
| status | TINYINT | 1 待审计 / 2 已通过 / 3 已驳回 / 4 已取消 |
| audit_by / audit_time / audit_remark | | 仓管员审计信息 |
| remark | VARCHAR(255) | |

**out_requisition_item 领用明细**：id, requisition_id, product_id, qty, location_id(出库库位), photo_file_id(出库商品照片，不强制), sort

> 状态机：使用者手机申请（附出库商品照片）→ 仓管员审计 → 通过=事务扣库存记流水；驳回=可修改后重新提交。驳回/取消不产生库存变动。

### 2.7 其他出入库（报废/报损/赠品）

**stk_other_io**：id, bill_no UNIQUE, warehouse_id, io_type(报废/报损/赠品入/赠品出/其他入/其他出), status(1 已过账 / 0 草稿 / -1 作废), operator_id, remark, created_at
**stk_other_io_item**：id, bill_id, product_id, qty, location_id, photo_file_id, sort

### 2.8 调拨

**stk_transfer**：id, bill_no UNIQUE, from_warehouse_id, to_warehouse_id, status(1 已审核 / 0 草稿 / -1 作废), audit_by, remark, created_at
**stk_transfer_item**：id, transfer_id, product_id, qty, from_location_id, to_location_id
> 审核通过 = 同一事务：调出仓扣库存 + 调入仓加库存 + 两条流水。

### 2.9 盘点

**stk_check**：id, bill_no UNIQUE, warehouse_id, status(0 草稿 / 1 盘点中 / 2 已审核), checker_id, check_date, remark
**stk_check_item**：id, check_id, product_id, location_id, book_qty(账面), real_qty(实盘), diff_qty(盘盈+ / 盘亏-), photo_file_id(盘点拍照记录，可选 → sys_file.id)
> 审核后按 diff_qty 生成盘盈/盘亏流水（走 stk_other_io 类型记录），支持复盘（重新盘点）。

### 2.10 OCR 与大模型

**ocr_record OCR 识别记录**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| file_id | BIGINT | → sys_file.id |
| ocr_type | TINYINT | 1 送货单 / 2 商品外包装 / 3 标签型号 |
| engine | VARCHAR(20) | rapidocr / paddle / doubao / deepseek |
| raw_result | JSON | 引擎原始输出 |
| structured | JSON | 结构化结果（商品名/数量/单价/金额等） |
| matched_product_id | BIGINT | 匹配到的商品（可空） |
| match_status | TINYINT | 1 已匹配 / 2 未匹配 / 3 人工修正 |
| duration_ms | INT | 耗时 |
| user_id | BIGINT | 操作人 |
| created_at | DATETIME | |

> 索引：idx(created_at), idx(match_status)

**ai_suggestion 大模型商品建议**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| ocr_record_id | BIGINT | 来源识别记录 |
| product_name | VARCHAR(100) | 大模型给出的商品名 |
| model | VARCHAR(20) | doubao / deepseek |
| suggestion | JSON | 建议的商品资料（名称/规格/分类等） |
| status | TINYINT | 1 待处理 / 2 已新增商品 / 3 已忽略 |
| new_product_id | BIGINT | 确认新增后的商品 id |
| handled_by / handled_at | | 处理人/时间 |

---

## 3. 关键设计决策

1. **库存一致性**：所有单据确认在**同一数据库事务**内写流水 + 更新实时库存；`stk_stock` 可由 `stk_stock_log` 全量重建，用于对账。
2. **成本核算**：采购入库按进价更新该商品移动加权平均成本（存 `stk_stock.cost_price`），领用出库按当时成本结转；报表据此计算库存金额。不做财务模块。
3. **条形码 ≠ OCR**：RapidOCR 只识别文字。条形码用 **zxing-cpp**（服务端解码，Python 封装）或手机端摄像头扫码组件直接解码后查 `base_product.barcode`。商品外包装名字/标签型号文字走 RapidOCR + 大模型。
4. **大模型分工**：**豆包视觉模型**负责"看图识别商品"（图像输入）；**DeepSeek 不支持图像输入**，只做 OCR 文本结构化/纠错/商品名归一化。API Key 与 BaseURL 存 `sys_config`，支持外网或内网 API。
5. **照片**：上传后服务端压缩（WebP/JPEG q80，长边≤1600px），原图与压缩图均由 `sys_file` 管理；永久保存不清理。磁盘预估：约 200~500KB/张，百万件级业务建议 1TB+ 磁盘或独立存储盘。
6. **Session 认证**：FastAPI 服务端 Session 存 `sys_session` 表（多进程共享、可踢人下线、可查在线）；手机外网访问**需要**，部署启用 HTTPS（正式证书，Session Cookie Secure）。
7. **预警**：定时任务（每分钟）扫描 `stk_stock.qty` 与商品上下限 → 生成 `sys_notification` 站内通知（已确认仅站内通知）。
8. **性能**：单据保存 100ms 目标只含事务写入；OCR/大模型识别异步处理（前端先返回"识别中"，完成后站内通知）。百万件总件数对表行数无压力，核心查询均走索引。
9. **多端**：手机端（Antd Mobile）与电脑端（Antd）两套页面共用同一套 API，Session Cookie 跨端通用。
10. **OCR 引擎抽象**：统一 `OCRClient` 接口（recognize(图片) → [{text, box, score}]），两个实现：`RapidOCREngine`（Windows，复用后端 `app/services/ocr/rapidocr_api.py` 子进程常驻）与 `PaddleOCREngine`（Debian/Linux，paddleocr CPU 推理）；引擎选择存 sys_config（ocr.engine），切换只影响识别层，结构化/匹配/大模型链路不变。
11. **多存储地址**：上传文件按 `sys_storage` 策略落盘——fill 选已用空间最小（默认）、round 轮询、manual 由请求指定 storage_id；`sys_file.storage_id` 记录落盘位置；停用/删除保护（有文件的存储禁止删除）；相对路径基于 backend/ 解析，支持网络盘符。

## 4. 已确认（2026-03-31）

1. 项目名称：**物料通管理系统**（物料管理：入库、出库、维修使用）
2. 手机外网访问：**需要**（部署启用 HTTPS）
3. 库存预警通知：**仅站内通知**
4. OCR 引擎：**可切换**——Windows 用 RapidOCR-json，后期 Debian 部署用 PaddleOCR（统一 OCRClient 接口）
