# 部署前置检查清单（P10 上线用）

> 依据《开发进度记录.md》各阶段「遗留」项收敛整理。上线前逐项核对，全部确认后再执行部署，
> 避免「上线即事故」。核对人/日期填写于右侧。

## A. 数据库（MySQL 8.0+）

| # | 检查项 | 依据/说明 | 核对 |
|---|---|---|---|
| A1 | 目标库已存在且已导入 `sql/init.sql`（建表+种子） | 安装页要求目标库已存在（README 部署流程 `mysql < init.sql`） | ☐ |
| A2 | 已部署库执行权限名变更（角色管理页显示） | 遗留：init.sql 的权限名变更仅影响角色管理页显示 | ☐ |
| A3 | 已部署库执行 `ALTER` 语句（init.sql 已含） | 遗留：已部署库需执行 ALTER | ☐ |
| A4 | 已部署库执行 `base_product_supplier` 建表 | `CREATE TABLE IF NOT EXISTS`（init.sql 已含） | ☐ |
| A5 | 已部署库执行 `ALTER TABLE pch_purchase_in ADD COLUMN ocr_record_id ...` | 语句见 init.sql | ☐ |
| A6 | 存量 `sys_config.ocr.engine` 值已按需 UPDATE（init.sql 不覆盖已有配置） | 遗留：改默认需手动 UPDATE 或 `.env OCR_ENGINE` | ☐ |
| A7 | 确认最低 MySQL 版本为 8.0（5.7 仅兼容性保留） | 评审：明确最低支持 8.0，简化兼容逻辑 | ☐ |
| A8 | 备份：`BACKUP_MYSQLDUMP` 指向真实 mysqldump 路径；备份目录可写 | 遗留：部署时确认 mysqldump 路径与备份目录 | ☐ |

## B. 缓存（Redis）

| # | 检查项 | 依据/说明 | 核对 |
|---|---|---|---|
| B1 | Redis 服务已启动 | 遗留：本机 Redis 服务未启动导致缓存层长期降级 | ☐ |
| B2 | Redis 服务开机自启（Windows 服务 `RedisWLT` 启动类型=自动） | 已注册服务，需确认启动类型为自动 | ☐ |
| B3 | `REDIS_URL` 已在部署环境配置正确（不可用时自动降级直查库，属预期） | config.py 默认 `redis://127.0.0.1:6379/0` | ☐ |

## C. 大模型 / OCR

| # | 检查项 | 依据/说明 | 核对 |
|---|---|---|---|
| C1 | OCR 引擎已安装（Paddle：`pip install -r` 会装 CPU 版 + 首次自动下载模型；或 rapidocr） | 遗留：全新部署约 200MB+ 依赖 | ☐ |
| C2 | 三模型槽位 API Key / Base URL / 模型名经系统设置填写并保存 | 未配置时降级人工录入，属预期但需确认 | ☐ |
| C3 | 默认模型仍在线可用（`GET /health` 的 `llm_availability` 状态非 down/degraded） | 评审 P1-5：防止默认模型下线静默失效 | ☐ |
| C4 | SMTP 已配置并实测发信（找回密码/邮件通知） | 遗留：需配置 SMTP 后实测发信 | ☐ |
| C5 | 逆地理：内网环境确认 OpenStreetMap 不通时提示手动地址；或已替换国内服务 | 遗留：内网不通时提示手动填写地址 | ☐ |

## D. Web / 反向代理 / 安全

| # | 检查项 | 依据/说明 | 核对 |
|---|---|---|---|
| D1 | 生产 Nginx/uvicorn 配置已落地（反向代理、静态资源、HTTPS 证书） | 遗留：生产 Nginx/uvicorn 配置需运维侧落地 | ☐ |
| D2 | Nginx/uvicorn 限流参数按 README 配置（`RATE_LIMIT_*` 与代理层配合） | 评审：限流配置依赖运维，需自动检查并警告 | ☐ |
| D3 | 生产使用正式 HTTPS 证书（dev 自签仅开发用） | dev 证书不入库，部署时用正式证书 | ☐ |
| D4 | 安全头（CSP/HSTS/X-Frame-Options）已评估 | 遗留：列入长期优化；上线前建议至少开启 HSTS | ☐ |
| D5 | `COOKIE_SECURE=true`、`DEBUG=false`、`INIT_ALLOW_PUBLIC=false` | 生产必须关闭调试与公网初始化 | ☐ |
| D6 | 前端构建产物（desktop/mobile dist）已用 `npm ci && npm run build` 重新生成 | 遗留：生产 dist 需重新构建（源码已含全部改动） | ☐ |

## E. 进程 / 运维

| # | 检查项 | 依据/说明 | 核对 |
|---|---|---|---|
| E1 | 后端以受管进程启动（部署脚本或服务），升级时统一重启，避免「旧进程未重启」 | 评审 P1：提供 start_all.bat / stop_all.bat（Windows dev）或 systemd（Linux） | ☐ |
| E2 | 每日备份（scheduler 02:00）与预警任务已按 ENV 区分是否启用 | 评审：调度任务未分环境，dev 也运行 | ☐ |
| E3 | 上传/备份目录（`backend/data`）有足够磁盘空间并纳入备份 | 默认本地存储 | ☐ |
| E4 | 日志目录可写且按天轮转 | app/logs，`LOG_LEVEL` 按需 | ☐ |

## F. 数据 / 权限 / 账号

| # | 检查项 | 依据/说明 | 核对 |
|---|---|---|---|
| F1 | 管理员密码已通过安装向导（初始化页）设置，且确认无默认口令逻辑残留 | 遗留：init.sql 仅插不可登录占位 `!`；老安装 bug 已修复 | ☐ |
| F2 | 部署库无测试残留数据（可运行 `tests/_data_cleanup.py` 归因清理 + 零残留校验） | 评审 P0/P2-7 | ☐ |
| F3 | 库存/报表关键表与对账口径一致（stk_stock_log 可对账） | 遗留：报表全部基于 stk_stock_log 聚合 | ☐ |

## G. 发布 / 开源合规

| # | 检查项 | 依据/说明 | 核对 |
|---|---|---|---|
| G1 | git 历史已清理真实凭据（`cdcp520` 等）并经 filter-repo 重写 + force-push 验证 | 评审 P0-2（用户已确认当前不处理）；清理方案要点见《开发进度记录.md》评审整改条目「二次评审」段 | ☐ |
| G2 | `.gitignore` 已含 `开源发布/`、`.env*`、证书、数据目录 | 已就绪（.gitignore 已含） | ☐ |
| G3 | `开源发布/` 副本经脱敏流程重新生成（不打包内部文档/AI开发文档） | 遗留：源文档含真实凭据/内网 IP，勿打包 | ☐ |
| G4 | LICENSE 已选定（MPL-2.0） | 遗留：许可证已补充（ci.yml 相关提交含 LICENSE MPL-2.0） | ☐ |

---
*核对完成后，在《开发进度记录.md》P10 阶段记录逐项结果与部署验证输出。*
