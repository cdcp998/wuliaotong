# 评审整改二次评审（P0+P1+P2 落地核验）

> 依据《开发进度记录评审报告》P0/P1/P2 建议逐条落地后的复核。全部改动经本地实跑验证：
> 后端全量 `pytest`（TEST_DB_URL 独立库）180/180、前端 `typecheck` + 双端 `vite build` 通过。

## 一、P0 项核验（原「立即」优先级）

| # | 原建议 | 落地结果 | 验证 | 状态 |
|---|---|---|---|---|
| P0-1 | CI 强制 `TEST_DB_URL`，测试不得直连 dev/prod | `.github/workflows/ci.yml` 两 job 增建 `wuliaotong_test` 独立库并导入 init.sql，测试步骤注入 `TEST_DB_URL`；`backend/conftest.py` 在 CI 未设置 `TEST_DB_URL` 时**直接 raise**（不再是警告） | 本地 TEST_DB_URL 实跑 180/180；静态核对 ci.yml | ✅ 已闭环 |
| P0-2 | git 历史清理 + `.gitignore` 补 `开源发布/` | 实证扫描确认：真实口令 `cdcp520` 仍存在于 git 历史（README/联调脚本）；**当前已跟踪文件零残留**（`git grep cdcp520 HEAD` 无匹配）；`.gitignore` 已含 `开源发布/`、`.env*`、证书/数据目录；输出《git 历史清理方案.md》（filter-repo 命令+验证+force-push 风险） | `git log -p` 全历史扫描 | 🟡 方案已交付；**执行（filter-repo+force-push）待用户确认** |
| P0-3 | 部署前置检查清单 | 新增《部署前置检查清单.md》：A 数据库(8) / B 缓存(3) / C 大模型与OCR(5) / D Web与安全(6) / E 进程运维(4) / F 数据账号(3) / G 发布合规(4)，逐项含依据与核对勾选 | 依据全部出自开发进度记录「遗留」项 | ✅ 已闭环 |

## 二、P1 项核验（原「短期」优先级）

| # | 原建议 | 落地结果 | 验证 | 状态 |
|---|---|---|---|---|
| P1-4 | `stop_all.bat` / `start_all.bat` 统一进程管理 | 新增两脚本：start_all 三窗口启动，stop_all 按端口 8443/5174/5175 定位并 taskkill 监听进程 | 静态核对 + 与既有启动脚本配套 | ✅ 已闭环 |
| P1-5 | `GET /health` 增加各供应商模型可用性缓存探测（仅探测不消耗 token），默认模型下线自动回退 | `llm.py` 新增 `probe_llm_availability`（GET `{base_url}/models` 免费接口，不消耗 token），Redis+进程内存双缓存（TTL 300s）；`/health` 返回 `llm_availability`；`PUT /settings` 保存 llm.* 时失效缓存；`chat_*_with_fallback` 探测判 down 时**自动跳过回退备用模型**（避免 60s 超时卡顿）；新增 `tests/test_llm_probe.py` 4 例 | test_llm_probe 4/4 + test_auth 7/7；全量 180/180 | ✅ 已闭环 |

## 三、P2 项核验（原「中期」优先级）

| # | 原建议 | 落地结果 | 验证 | 状态 |
|---|---|---|---|---|
| P2-6 | `ProductPicker`/`BarcodeScanner`/`PhotoUpload` 移入 shared 包由两端引用 | 连同 `CameraAlbum`/`icons`/`useBackToClose`/`zxing_reader.wasm` 全部迁入 `packages/shared/src/components/mobile|hooks|assets`；shared 显式依赖 `antd-mobile`/`zxing-wasm`；移动端 8 页改从 `@wlt/shared` 导入，旧文件删除 | typecheck 零错误；desktop+mobile 构建成功（wasm 正常产出） | ✅ 已闭环 |
| P2-7 | 测试后数据库行数对比 fixture（CI 强制零残留） | `tests/conftest.py` 会话级 `_zero_residue_guard`：快照 31 张业务表行数 → 测试后兜底清理 → 净增即失败（仅 TEST_DB_URL 隔离时启用，CI 已强制）；`_data_cleanup.py` 隔离库分支全量清除无法归因的 OCR 记录/建议/文件 | **首轮即抓出 ocr_record+11/ai_suggestion+1 真实残留**，修复后 180/180 零残留通过 | ✅ 已闭环（并有真实发现） |
| P2-8 | `apiBase()` 统一 + REST 重复逻辑收进共享 service 层 | 实证：apps 内已**零**裸 `apiBase()`/`/api/v1`/axios（REST 全部在 `@wlt/shared`）；新增 `fileApi.url()` 统一文件预览 URL；desktop StockQuery 改走既有 `exportReportUrl()` | grep 无残留；typecheck+build 通过 | ✅ 已闭环（原已基本完成，仅剩 2 处 URL 收敛） |

## 四、文档治理核验（评审「文档质量」建议）

- **4 条「未归档」条目**：逐一核对 main 分支真实提交哈希后并入「已提交」区——
  - 接口限流/Proactor → `361927b`（原文档写的 4ec369a 为并行 worktree 哈希，已注明）
  - 移动端宽屏 CSS → `f64c6d3`
  - 移动端记住登录 + 找回密码排版 → `e68be0d`
  - 系统设置页重构（邮件并入基础设置） → `ded6aef`（HEAD 已含合并结构）
- **总览表**：新增「归档」列；P8/P9「待提交」修正为「已提交（并入后续提交）」；P10 保持未归档。
- **遗留**：`kun/worktree-0862a6` 并行分支与 main 哈希不一致（并发会话产物，已提示勿误用）。

## 五、剩余风险（二次评审结论）

| 风险 | 级别 | 处置 |
|---|---|---|
| **git 历史仍含 `cdcp520`**（开源前必须处理） | 🔴 | 方案已就绪，执行 filter-repo + force-push 属破坏性操作，**待用户确认**后按《git 历史清理方案.md》执行；若仓库未公开也可选择发布前重建仓库 |
| desktop 主 chunk ~996KB 体积告警 | 🟡 | 非本次引入；后续可再拆 lazy chunk / manualChunks |
| 本地无 TEST_DB_URL 时跑测试仍会落开发库 | 🟡 | 本地开发建议在 `backend/.env` 或 shell 设置 `TEST_DB_URL`；README 已说明 |
| `_data_cleanup` 隔离分支全量删 OCR/文件 | 🟡 | 仅当 `TEST_DB_URL` 设置时启用；测试库内无真实数据，安全 |
| scheduler 每日备份/预警未按 ENV 区分 | 🟡 | 原报告 P1 未含此条（属新增观察）；建议下一轮处理 |

## 六、结论

- **P0/P1/P2 全部落地**，其中 P0-2 的破坏性执行项（历史重写）以「方案 + 待确认」交付，属审慎处理。
- **零残留守卫实战抓出真实残留**是本轮最有价值的验证：证明评审 P2-7 建议的「行数对比断言」确实能拦截既有测试清理盲区。
- 二次评审基准：**后端 180/180 + 前端双端构建通过**，达到可提交状态。

---
*本二次评审基于实际代码改动与本地实跑验证（非纯静态分析），较首轮评审可信度更高。*
