
### 评审整改 P0+P1+P2（2026-08-22，评审报告落地）
- **交付**：按《开发进度记录评审报告》P0/P1/P2 全项落地——
  - **P0-1 CI 强制测试库隔离**：`.github/workflows/ci.yml` 双 job 增建 `wuliaotong_test` 独立库并导入 init.sql，测试步骤注入 `TEST_DB_URL`；`backend/conftest.py` 在 CI 环境未设置 `TEST_DB_URL` 时直接报错（禁止直连开发库）。
  - **P0-2 git 历史凭据**：实证扫描确认真实开发库口令 `cdcp520` 存在于 git 历史（README/联调脚本），当前已跟踪文件零残留、`.gitignore` 已含 `开源发布/`；filter-repo 清理方案要点已压缩回写至本条目「二次评审」段（原《git 历史清理方案.md》已删）。**用户已确认：仓库已公开发布，不执行历史重写（接受现状）**。
  - **P0-3 部署前置检查清单**：新增《部署前置检查清单.md》（A 数据库 / B 缓存 / C 大模型与 OCR / D Web 与安全 / E 进程运维 / F 数据账号 / G 发布合规，7 组 20+ 项）。
  - **P1-4 进程管理脚本**：新增 `start_all.bat` / `stop_all.bat`（按端口 8443/5174/5175 定位并结束监听进程），与既有启动脚本配套。
  - **P1-5 LLM 模型健康探测**：`app/services/llm.py` 新增 `probe_llm_availability`/`_probe_provider`（GET {base_url}/models 免费探测，不消耗 token），Redis+进程内存双缓存（TTL 300s），`llm_availability_status` 汇总；`GET /health` 新增 `llm_availability` 字段；`PUT /settings` 保存 llm.* 配置时 `invalidate_probe_cache()` 失效；`chat_*_with_fallback` 在探测判定 down 时自动跳过该模型回退备用。新增 `tests/test_llm_probe.py` 4 例。
  - **P2-6 移动端共享组件收敛**：`ProductPicker`/`BarcodeScanner`/`PhotoUpload`/`CameraAlbum`/`icons`/`useBackToClose` + `zxing_reader.wasm` 从 `apps/mobile` 迁入 `packages/shared/src/components/mobile|hooks|assets`；shared 显式依赖 `antd-mobile`/`zxing-wasm`；移动端 8 个页面改从 `@wlt/shared` 导入，删除旧文件。
  - **P2-7 测试零残留断言**：`backend/tests/conftest.py` 新增会话级 `_zero_residue_guard`（仅 TEST_DB_URL 隔离时启用，业务表行数前后对比，净增即失败）；`_data_cleanup.py` 隔离库分支全量清除无法归因的 OCR 记录/AI 建议/测试文件——**首轮运行即抓到 ocr_record+11/ai_suggestion+1 真实残留并修复**。
  - **P2-8 API 网关化**：apps 内零裸 `apiBase()`/`/api/v1`/axios（REST 全部集中在 shared）；新增 `fileApi.url()`、desktop StockQuery 改走既有 `exportReportUrl()`。
  - **文档治理**：4 条「未归档」明细核对 main 分支真实哈希后全部并入「已提交」（限流→`361927b`、widescreen→`f64c6d3`、remember→`e68be0d`、设置页→`ded6aef`）；总览表新增「归档」列并修正 P8/P9 状态；顶部追加提交说明。
- **验证**：后端 `compileall` 零错误 ✅；`pytest tests` 全量 **184/184 通过**（含零残留守卫与 test_llm_probe，TEST_DB_URL 独立库实跑）✅；前端 `npm run typecheck`（desktop+mobile）零错误 ✅；`vite build`（desktop+mobile）成功 ✅。
- **问题**：① P0-2 的 git 历史重写 + force-push 未执行（用户确认仓库已发布、不重写，接受现状，方案保留备用）；② `kun/worktree-0862a6` 并行分支与 main 哈希不一致（并发会话产物，勿误用）；③ 前端 desktop 主 chunk ~996KB 仍有体积告警（非本次引入）；④ widescreen/remember 等 4 条原「未归档」明细中的「vite build 未运行」等历史备注保留原样（当时环境限制，后续已多次构建通过）。
- **提交**：`cabb7c5`（CI 测试库隔离 + 零残留断言）、`ec86d45`（LLM 可用性探测）、`9c3b9e9`（移动端共享组件收敛）、`7df24ac`（API URL 统一收拢）、`69113c3`（文档治理 + 二次评审回写），未推送
- **二次评审（压缩回写，原《评审整改二次评审.md》已删）**：
  - **逐项闭环**：P0-1 CI 测试库隔离 ✅ · P0-3 部署前置检查清单 ✅ · P1-4 进程管理脚本 ✅ · P1-5 LLM 可用性探测 ✅ · P2-6 共享组件收敛 ✅ · P2-7 零残留守卫 ✅ · P2-8 API 网关化 ✅；仅 P0-2「历史重写」一项未执行（用户确认仓库已发布、不处理）。
  - **文档治理**：4 条未归档核对 main 分支真实哈希后并入「已提交」（`361927b`/`f64c6d3`/`e68be0d`/`ded6aef`）；总览表加「归档」列并修正 P8/P9；`kun/worktree-0862a6` 并行分支与 main 哈希不一致（并发会话产物，勿误用）。
  - **剩余风险**：① git 历史仍含真实口令 `cdcp520`（开源前如需处理：`git filter-repo --replace-text` 替换为占位 + 全历史验证零残留 + force-push，或发布前重建仓库；用户已确认当前不处理）；② desktop 主 chunk ~996KB 体积告警（非本次引入）；③ 本地未设 `TEST_DB_URL` 时测试仍会落开发库（本地建议同样设置）；④ scheduler 每日备份/预警未按 ENV 区分（建议下一轮）。
  - **结论**：P0/P1/P2 全部落地；**零残留守卫首轮即抓出 ocr_record+11 / ai_suggestion+1 真实残留并修复**，为本轮最有价值验证（证明「行数对比断言」能拦截既有测试清理盲区）；二次评审基准 **184/184 + 双端 typecheck/build 通过**，达可提交状态。

### 接口限流防刷屏 + Windows Proactor 异常消除（已提交）
- **交付**：① `app/core/ratelimit.py`——全局接口限流中间件（每 IP 滑动窗口，默认 300 次/60 秒；`RATE_LIMIT_ENABLED`/`RATE_LIMIT_REQUESTS`/`RATE_LIMIT_WINDOW_SECONDS` 环境变量可调）；超限返回 HTTP 429 + 统一响应体 `code=4008`「请求过于频繁，请稍后再试」+ `Retry-After` + `Connection: close`；`/api/v1/health` 豁免；执行顺序 CORS → 限流 → 审计（本版本 starlette 后注册者更靠外层，审计中间件由装饰器改为显式 `add_middleware(BaseHTTPMiddleware, dispatch=...)` 注册）——被限请求不落操作日志（防洪泛放大审计 DB 写）；同一 IP 限流告警 30 秒节流；单进程内存态（与验证码一致），pytest 每用例自动清空。② `app/core/loop_guard.py`——两层 Windows Proactor 防护（lifespan 安装、幂等）：a. 事件循环异常过滤器：Python 3.13 下客户端强制断开时 `_call_connection_lost()` 内 `sock.shutdown()` 未捕获 OSError（WinError 10054/10053/64 等）冒泡为未处理异常，过滤器仅静默该良性模式（winerror/errno ∈ {64,10053,10054,995,996,1236} 且回调含 `_call_connection_lost`），其余异常链到原处理器；b. Proactor accept 加固：实测发现客户端在 accept 完成前断开时 stdlib（3.13.8）`BaseProactorEventLoop._start_serving` 会**关闭监听 socket 导致服务停止接受新连接**（同时 accept_coro 产生「Task exception was never retrieved」），按本机 stdlib 源码逐字复制 + 单一改动打补丁：良性 accept 错误改 50ms 退避后重挂 accept（不关闭监听）、`IocpProactor.accept` 的 accept_coro 吞掉 OSError；补丁安装前校验源码标记（版本漂移自动跳过并告警）。③ 新错误码 4008 入《后端API设计.md》§11.8/§11.12；config/.env.example 新增 3 个限流配置项；《开发排期.md》风险表补「接口限流（反刷屏）」
- **验证**：L1 `python -m compileall` 零错误 ✅；L2 pytest 全量 173/173 通过（新增 test_ratelimit.py 8 例 + test_loop_guard.py 9 例，含「RST 暴力断连后监听器仍存活且零上报」集成用例）✅；L3 真实 uvicorn 冒烟：① 洪泛 30 连发——第 21 个起 429（body code=4008、`access-control-allow-origin` 正常、Retry-After=60、Connection: close），被限请求不再出现在审计日志，`app.ratelimit` 告警仅 1 条；② `/health` 超额度仍放行；③ 60 次「连接后立即 RST」暴力断连——服务端日志 0 条 asyncio 错误、0 条 ERROR，监听器存活（修复前同场景：WinError 64 导致监听 socket 被关闭、连接被拒）；前端零改动
- **问题**：① 全量测试曾 20 例失败——`uk_bill_no` 唯一键冲突，系历史测试运行遗留单据数据（LL/QT 单号残留），非本次改动；跑 `tests/_data_cleanup.py::cleanup_test_data()` 清理后全量通过（该脚本为上一提交遗留工具，保持未跟踪不提交）；② 限流按 IP 计数，多用户共享 NAT 出口共享额度（默认 300 次/分钟充裕）；③ 补丁依赖 asyncio 内部实现，已做源码标记校验 + 失败跳过告警兜底
- **提交**：main 上随 `361927b`（test(cleanup)，整树提交，本功能全部代码+测试+文档均在库内；并发 worktree 对应 4ec369a）

### 移动端电脑端兼容 CSS（widescreen，已提交）
- **交付**：新增 `frontend/apps/mobile/src/styles/widescreen.css`（`main.tsx` 引入），≥768px 宽屏（电脑浏览器打开移动端）时「应用窗口化」适配，一套 CSS 覆盖全部页面，≤767px 手机端零影响：① body 浅灰底（#e9ebef，margin 0）；② 页面根容器限宽居中——所有页面根 div 均带 `height/min-height:100vh` 内联样式，用 `#root > div[style*="100vh"]` 属性选择器命中：Tab 页/二级页 720px，登录页根 div 加 `wlt-login` 类收窄为 480px 居中登录卡片，均带轻阴影；③ 弹层限宽居中——`.adm-popup-body` max-width 720px，bottom 弹层 `left:50% + margin-left:-360px` 平移居中（antd-mobile bottom body 为 `width:100% + left:0`，fixed 定位下 auto margin 不生效；不动 transform 避免破坏滑入动画），覆盖「修改密码」全屏弹层、库位/供应商选择、扫码弹层等全部 Popup/ActionSheet；④ 覆盖策略与桌面端一致：仅依赖稳定类名/结构特征，不逐页改组件
- **验证**：playwright 实机实测（1280×800 桌面视口 + 375×844 手机视口对比）——①登录页 480px 居中（x=393/w=480，body 灰底 #e9ebef，margin 0）；②登录后首页 TabLayout 根容器 720px 精确居中（x=280/w=720/vw=1280）、TabBar 720px 贴底；③「修改密码」全屏弹层 720px 居中（x=280，left:50%+margin-left:-360px 生效）；④二级页 /requisitions/list 720px 居中（x=273=扣除滚动条后的精确居中）、NavBar 与根容器对齐、列表正常；⑤手机 375px 视口下媒体查询完全不生效（max-width none、body 背景透明、原始布局）✅；`tsc --noEmit`（mobile/desktop）零错误 ✅（本轮仅新增 CSS+main.tsx import+Login 类名，类型层无风险）；`vite build` 未运行（本会话无 shell 环境）
- **问题**：无（①CSS 属性选择器依赖「根 div 带 100vh 内联样式」这一既有结构约定，若未来页面根容器改版需同步；②body 默认 8px margin 在手机端保留原样、仅宽屏清除；③宽屏下登录/页面为固定 480/720px 窗口化而非流式重排，属有意设计——保持手机端布局一致，避免逐页双布局维护成本）
- **提交**：`f64c6d3`（feat(mobile): 移动端电脑端兼容样式——宽屏窗口化限宽居中 + 弹层限宽）；`9c5af7c`（全局 UI 打磨）亦含相关调整

### 移动端登录页：记住登录状态复选框 + 找回密码排版修复（已提交）
- **交付**：①手机端登录页「记住登录状态」由纯文本改为 antd-mobile `Checkbox`（默认勾选，与电脑端「记住我」一致）——勾选→登录带 `remember: true`；②后端 `LoginReq` 新增 `remember: bool = False`，`config.py` 新增 `SESSION_REMEMBER_HOURS`（默认 720 小时=30 天），登录时按 remember 选择会话时长（DB `sys_session.expire_at` / Redis TTL / Cookie max-age 三处一致）：勾选 30 天、不勾 8 小时（`SESSION_EXPIRE_HOURS`）；③shared `authApi.login` / `useAuthStore.login` 透传 remember；④找回密码/重置/注册页排版修复——根因：`返回登录`按钮是 Form 直接子元素，被 antd-mobile Form 渲染进 `.adm-list-body-inner`（无列表项 12px 左内边距，横向错位 x=32 vs 输入框 x=48、贴列表底边、与输入框仅 12px 间距），改为统一移出 Form、放在表单下方居中文本链接（`padding: 12px 24px`，触屏目标 ≥44px，符合前端规范 §5.6）
- **验证**：L1 后端 `compileall` ✅ + 前端 mobile/desktop `tsc --noEmit` 零错误 ✅；L2 pytest `tests/test_auth.py` 4/4 通过（原有用例；另新增 2 个 remember 用例——Cookie max-age 与 DB expire_at 断言，因本会话无 shell 环境未执行，等价的 Cookie 行为已由下述 L3 实机验证覆盖）；L3 用新代码实例（8444）联调：`POST /auth/login` remember=true → Set-Cookie `Max-Age=2592000`、remember=false/省略 → `Max-Age=28800`，均 200 正常 ✅；L4 双端 `vite build` 未运行（无 shell 环境）；浏览器 UI 实测未完成（mobile dev server 5175 已停止且无法重启）——修复前已量化确认「返回登录」按钮错位（x=32 vs 输入框 x=48、贴列表底边），修复后待有 dev server 环境时截图核对
- **问题**：无（桌面端「记住我」仍是纯展示未接后端 remember，本轮按需求仅改手机端，留待后续如需统一再接线）
- **提交**：`e68be0d`（feat(auth): 移动端记住登录状态复选框 + 会话时长按 remember 区分；fix(mobile): 找回密码/重置/注册页返回按钮排版）

### 系统设置页重构：邮件服务与账号与安全并入基础设置 + 分区美化（已提交）
- **交付**：desktop 系统设置页去掉独立「邮件服务」「账号与安全」Tab，SMTP 配置（smtp.host/port/user/password/from）与注册/找回配置（auth.register_mode/auth.forgot_method/site.contact_phone）全部合并进「基础设置」Tab 分区（导航保持「系统设置」入口，页面内邮件服务、注册模式、忘记密码找回均作为基础设置子分区呈现）；两个 Tab 统一浅色圆角分区卡片布局（图标+标题+描述+右侧状态 Tag，字段统一样式）：**基础设置**（站点信息 / 邮件服务（SMTP，带「已配置/未配置」状态 Tag）/ 完成工作照片水印 / 运行日志 / 注册模式 / 忘记密码找回）、**OCR 与大模型**（本地 OCR 引擎（引擎单选+模型版本+安装状态）/ 视觉模型 / 文本模型 / 豆包兜底（各带「已启用/未启用」Tag+模型列表获取）/ 配额与预警（带「预警已开启/关闭」Tag）/ 模型与工作任务）；配额预警提示文案指向「基础设置 → 邮件服务」；保存/脱敏/模型列表/配额/水印预览/安装轮询等交互逻辑不变（表单字段名与后端 PUT /settings 契约完全一致）
- **验证**：L1 desktop `tsc --noEmit` 零错误 ✅；L4 `vite build` 成功 ✅；后端 tests/test_settings.py 5/5 通过（接口契约未变）✅；浏览器实测因环境无浏览器面板未做，界面结构静态核对通过
- **问题**：无（纯前端布局重构，无后端/数据库变更；账号与安全「忘记密码找回方式保存后仍提示未保存修改」问题已另行修复）
- **提交**：并入 `ded6aef`（feat: 手机端功能中心/通知联动/采购计划全链路与全局体验收尾——HEAD 已含「邮件服务并入基础设置分区并美化设置页」结构）

### P0 基础框架（2026-08-06）
- **交付**：backend/sql/init.sql（35 张表 + 种子数据：4 角色/23 权限点/admin 账号）；FastAPI 骨架（config/db/core/models/schemas/api）；Session 认证（login/logout/me，存 sys_session 滑动续期）；审计中间件；OCRClient 抽象 + RapidOCREngine；HTTPS dev（8443，自签名证书）
- **验证**：L1 compileall ✅ → L2 pytest 4/4 ✅ → L3 curl：health/login(23权限点)/me/logout/401 ✅
- **问题**：Python 3.11 安装失效→改 3.13；phpstudy MySQL 默认 MyISAM→建表显式 InnoDB；35 表缺 created_at/updated_at 已补齐
- **提交**：docs 基线、refactor(ocr)、feat(backend)

### P1 基础资料（2026-08-06）
- **交付**：分类树/单位/供应商(Excel导入)/商品(多单位换算、导入导出模板)/仓库/货架/库位(自动编码) 全部 CRUD + 权限校验
- **验证**：pytest 11/11 ✅；L3 冒烟（建单位/商品/搜索）✅
- **问题**：测试非幂等→唯一后缀；路由注册顺序（/products/export 被 /{id} 抢占）→ static_router；读接口无登录校验→router 级统一；商品列表默认过滤停用
- **提交**：feat(base)

### P2 库存核心（2026-08-06）
- **交付**：`post_stock_change()`（行锁+移动加权成本+库存不足4001）；采购入库(单号重试)/作废冲销(仅当日)；期初(草稿/修改/过账/Excel导入)；库存查询/流水
- **验证**：pytest 16/16 ✅；L3：入库 RK202608060026→库存100→流水0→+100→100 ✅
- **问题**：_opening_out 误用 Req 模型；草稿期无库存行；4001 改事务层单测覆盖
- **提交**：feat(stock)

### 多存储地址（2026-08-06）
- **交付**：sys_storage 表(35表)；fill/round/manual 三策略；上传压缩(WebP q80 长边1600)；GET /files/{id}；/storages CRUD + health 检测
- **验证**：pytest 19/19 ✅；L3：3000x2000 JPEG 94KB→WebP 3.1KB ✅
- **问题**：StorageReq 缺 status 字段(静默忽略→停用失效)；manual 混入自动轮询→排除
- **提交**：feat(storage)

### P3 领用流程 + 前端骨架（2026-08-06）
- **交付**：领用申请(地点/原因必填)/我的申请/审计(approve 行锁扣库存+通知/reject)/驳回重提/取消；通知接口(列表/未读/已读)；参数校验统一 4006；前端 monorepo(npm workspaces: shared+desktop+mobile)、Zustand auth store、路由守卫、登录页、HTTPS(5173/5174)；**手机端领用申请页+我的申请列表+路由（2026-08-06 补）**
- **验证**：pytest 24/24 ✅（后补至 39/39）；L3：申请 LL202608060018→审计→库存50→40→通知 ✅；前端两端 build ✅
- **问题**：手机端 /requisitions 路由缺失导致入口报错（2026-08-06 用户反馈后补齐）；FastAPI 0.141 新路由内部类型(诊断踩坑)；tester_user 依赖 conftest 幂等创建
- **提交**：feat(requisition)、feat(frontend)、fix(requisition)

### P4 库存进阶（2026-08-06）
- **交付（后端）**：调拨(草稿/审核双流水/作废冲销)；盘点(自动带账面/录实盘/盘盈盘亏流水/照片可选 photo_file_id)；其他出入库(六类型直接过账/作废)；APScheduler 库存预警(1分钟/幂等)；手机端：入库/出库/盘点列表/盘点执行页
- **验证**：pytest 38/38 ✅；L3 冒烟 ✅；**电脑端页面补齐**：调拨/盘点/其他出入库管理页 + 工作台入口卡片（2026-08-06 第二轮），两端 build ✅
- **遗留**：无（P4 全部完成）
- **问题**：TransferReq 漏 import(FastAPI 降级 query)；stk_check_item 补 photo_file_id 四层同步；CheckItemOut 漏字段
- **提交**：feat(stock) P4、feat(mobile)

### P5 OCR/大模型（2026-08-06）
- **交付（后端+前端）**：异步识别任务(/ocr/recognize→task 轮询)；拍照快查(/ocr/quick 同步+商品匹配)；DeepSeek 送货单结构化(未配置降级)；豆包视觉 AI 建议(未匹配→建议→人工确认新增/忽略)；LLMClient 抽象；后台配置 GET/PUT /settings(密钥脱敏、掩码不修改)、get_ocr_engine 读库实时切换、desktop 系统设置页；**前端**：手机端拍照快查页(带入出入库)、电脑端送货单 OCR 录入页(图片预览+结构化表格+AI 建议处理)、/ocr/records、/ai-suggestions 页
- **验证**：pytest 39/39 ✅；L3：真实 RapidOCR 识别中文图片"轴承6204数量10" ✅；引擎切换 health 实时反映 ✅；前端两端 build ✅
- **遗留**：无（P5 全部完成；真实豆包/DeepSeek Key 配置后需实链验证）
- **问题**：ocr.py 首次写入缩进损坏→重写；ai_suggestion_accept 参数注入错误；后台线程会话安全
- **提交**：feat(ocr)、feat(settings)

### 兼容公司导入（2026-08-06）
- **交付**：商品导入自动识别**公司 13 列表头**（序号/材料用途/材料大类/材料分类/物料编码/材料名称/型号规格/单位/数量/用途/用量(仅导入使用)/使用单位/备注）——物料编码→code、材料名称→name、型号规格→spec、单位→unit(自动创建)、材料大类+材料分类→两级分类(自动创建)、备注→remark，其余列忽略；两种模板并存自动识别；**2026-08-06 确认：物料编码=公司系统唯一编码（code）；公司模板无条码列，条码留空不自动生成，响应 notice 提示管理员从公司系统抄写补录**
- **验证**：pytest 49/49（公司表头导入测试：字段映射/两级分类/失败行/条码留空+notice）✅
- **问题**：历史同名分类干扰→测试用唯一名；分类树断言需查 children
- **提交**：feat(import)

### P6 报表看板 + 2D 货架图（2026-08-06）
- **交付（后端）**：/stock/summary 看板（SKU/总件数/今日出入库/预警/待审计/7日趋势）；/reports/dashboard 经营看板（今日/本周/本月、预警、待办：领用待审计+调拨待审核+盘点进行中）；/reports/inventory-summary 进销存汇总（期初+入库-出库=结存，按库位成本加权算金额，支持 warehouse/product/日期过滤）；/reports/stock 库存报表（qty/amount/turnover 排序，30天出库+呆滞天数）；/reports/export Excel 导出（inventory-summary/stock/flow 三类型，openpyxl）；/stock/location-summary 2D 货架图数据源（库位商品+预警绿/红/黄）；报表权限 report:view / report:export（管理者+超管）；数量输出统一去尾零（30.000→30）
- **交付（前端）**：desktop 经营看板页(/dashboard：统计卡片+7日双柱趋势图+待办跳转)、报表中心(/reports：进销存汇总/库存报表/流水导出三个 Tab+仓库日期筛选+Excel 导出)、仓库货架图(/warehouses 列表 + /warehouses/:id/map 2D 分层货架图：按层展示库位格、颜色标识预警、点击格子看商品明细)、工作台入口卡片；shared 新增 report.ts（报表接口+导出 URL）、baseApi.shelves
- **验证**：pytest 49/49 ✅（新增 test_report.py 10 用例：看板/汇总/期初口径/库存排序/导出内容/货架图/权限）；两端 build ✅
- **遗留**：无（报表全部基于 stk_stock_log 聚合，可对账）
- **问题**：数量 Decimal 尾零（'30.000'）→ _fmt_qty 统一去尾零；coalesce 兜底日期被驱动返回 str → 类型兼容解析；导出行数断言需 product_id 过滤（测试库共享）
- **提交**：feat(report)

### P7 系统管理（2026-08-06）
- **交付（后端）**：用户 CRUD(/users 列表/新建/修改含重置密码/停用启用，保护：admin 不可停用、不可改自己角色状态、重名拦截)；角色管理(/roles CRUD + /permissions + /roles/{id}/permissions 分配，保护：super_admin 角色权限不可改、内置角色不可删、有启用用户引用不可删)；操作日志查询(/logs 按用户/模块/方法/日期过滤)；备份(/backups 手动 mysqldump→gzip、列表、删除、下载；MYSQL_PWD 环境变量传密码不进命令行；.env 配置 BACKUP_MYSQLDUMP 路径)；scheduler 每日 02:00 自动备份(保留最近 14 份自动清理，失败记日志不中断)
- **交付（前端）**：desktop 用户管理页(/system/users 新建/编辑/重置密码/启用停用)、角色与权限页(/system/roles 角色 CRUD+权限勾选分配)、操作日志页(/system/logs 多条件查询)、备份管理页(/system/backups 立即备份/下载/删除)；工作台入口卡片；shared 新增 admin.ts
- **验证**：pytest 56/56 ✅（新增 test_admin.py 7 用例：用户 CRUD+保护规则、角色权限生效、日志查询、真实 mysqldump 备份/下载/删除、权限 4005）；两端 build ✅
- **遗留**：无（部署时确认 mysqldump 路径与备份目录）
- **问题**：.env 反斜杠路径被 dotenv 转义(\p→ESC)→改用正斜杠；mysqldump 输出被 capture_output 丢弃→stdout 直写文件；测试路径漏 /api/v1 前缀
- **提交**：feat(admin)

### P8 盘点重做 + 宫格自定义 + 入库改名（2026-08-06）
- **需求（用户确认）**：盘点改为物品级别（管理员/仓管员发起 → 选仓库 → 逐物品录实盘）；导出 Excel 参考《库存金额收发存（2026.06）》模板；手机主页宫格可编辑；采购管理→入库管理、采购入库→入库
- **交付（后端）**：create_check 按商品聚合账面（跨库位汇总，location_id=0 物品级）；audit_check 差异按库位分摊（盘亏从库存多库位依次扣、盘盈记入库存最多库位且按当前成本入账不摊薄移动加权）；GET /checks/{id}/export——21 列（模板 16 列月度收发存 + 账面/实盘/盘盈盘亏数量金额/备注），宋体+边框+合并标题，月度收发存从流水按商品聚合（金额=变动量×流水成本）；CheckItemOut 加 material_code/spec/unit_name/category_name；init.sql 权限点 6 名称改「入库」
- **交付（前端）**：desktop CheckDetail 导出 Excel 按钮 + 编码(物料码优先)/规格/单位列；mobile Checks 发起盘点（选仓库→创建→进入执行）+ CheckRun 物品级展示；mobile Home 宫格编辑（快捷操作「编辑」→ 点选隐藏/恢复 → localStorage 按用户保存）；AppLayout 菜单「采购管理」→「入库管理」、「采购入库」→「入库」；系统设置页改为 Tabs 分类（基础设置/OCR 与大模型/账号与安全/邮件服务）且系统管理菜单「系统设置」移至末位
- **验证**：pytest 65/65 ✅（盘点测试改为两库位聚合 1 条明细、盘盈 2 分摊至库存多库位、成本不被摊薄、导出 21 列结构与字段断言）；两端 build ✅
- **遗留**：导出文件需在已部署库手动执行 init.sql 的权限名变更（仅影响角色管理页显示）
- **提交**：待提交

### 领用私用隐藏触发（2026-08-06，已完成）
- **需求（用户确认）**：「因何使用」字段连续点击 15 次（间隔 ≤1.5s）→ 确认后锁定为「私用」不可编辑；私用状态仅管理员可见；非管理员看到的「因何使用/使用地点」为固定掩护值（取自最近 30 天内未盘点领用单，管理员可编辑）
- **交付（后端）**：out_requisition 加 is_private/display_reason/display_location 三列（init.sql + 本地库 ALTER）；创建/重提时 is_private=1 → use_reason 强制「私用」+ 随机取最近 30 天非私用领用单的 reason/location 作掩护值（无则默认「日常工作使用/公司办公区」）；_req_out 按查看者角色脱敏（管理员=超管或有 req:audit 权限：见真实+掩护值；非管理员：恒 is_private=0 + 只看掩护值）；新增 PUT /requisitions/{id}/display（req:audit 编辑掩护值）；my/list/detail 全链路脱敏
- **交付（前端）**：mobile 申请页「因何使用」连续点击 15 次触发 Dialog 确认→锁定灰显「私用」（Tag 标注+提示）；mobile 详情页管理员见红色「⚠ 私用申请」标注+掩护值；desktop 领用审计列表「因何使用」列红色「私用」Tag、详情 Drawer 红色特殊标注框+「编辑对外显示信息」Modal（两个输入框保存）
- **验证**：pytest 66/66 ✅（新增私用用例：管理员见真实/非管理员见掩护值、非管理员改掩护值 4005、管理员编辑后固定生效、列表脱敏）；两端 build ✅
- **遗留**：已部署库需执行 ALTER（init.sql 已含）；掩护值来源「未过盘点」按创建 30 天内口径实现

### GPS 坐标查询地址 + 领用单 GPS/地点编辑 + 查询页重做（2026-08-06，已完成）
- **需求（用户确认）**：①实现 GPS 坐标查地址（适用于无原始 {location} 记录场景，可调第三方逆地理编码）；②提供界面/组件：手动输入或外部传入坐标、查询展示地址、地址字号可调、整体背景透明；③领用申请单查询支持编辑 GPS 坐标与地点；④电脑端领用界面重做
- **交付（后端）**：新增 app/api/geo.py——GET /api/v1/geo/reverse（OpenStreetMap Nominatim 免费逆地理编码，中文优先，8s 超时；无外网返回可读业务错误；坐标范围 ±90/±180 校验，非法→4006）；main.py 注册 geo 路由；新增 PUT /api/v1/requisitions/{id}/work-location（req:audit 权限，编辑 use_location/work_lat/work_lng，保存后水印照片按新值生成）
- **交付（前端）**：新增 shared geoApi.reverse；desktop 新增 GeoAddressPanel 组件（透明背景容器、纬度/经度输入或外部 props 传入、GPS 查询地址、地址可编辑、字号 A+/A− 调节 12~28px、保存/取消回调）；发布交互原型（.kun-design/component-prototypes/gps-8cf24f6731）；RequisitionQuery 详情 Drawer 重做：使用地点行加「编辑 GPS 与地点」按钮 + GPS 坐标显示 + 弹窗内嵌 GeoAddressPanel（初始值取单据坐标/地点，保存调 updateWorkLocation 并刷新详情）；页面层级/信息分组重排
- **验证**：pytest 71/71 ✅（新增：管理员编辑 GPS/地点生效、非管理员 403、坐标非法 4006）；两端 build ✅
- **遗留**：逆地理编码依赖外网访问 OpenStreetMap（内网不通时提示手动填写地址）；如需国内服务可在 geo.py 替换为高德/百度（需配置 key）

### 端口调整：电脑端 5174 / 手机端 5175（2026-08-06，已完成）
- **需求（用户确认）**：电脑端访问手机端时应使用 5175，电脑端使用 5174；先关闭所有后台进程再编辑避免端口错乱
- **交付**：清理占用 5173/5174/5175 的残留 dev server 进程（taskkill）；desktop vite.config port 5173→5174（注释「电脑端」）、mobile vite.config port 5174→5175（注释「手机端」）；实际拉起两端 dev server 验证端口监听后关闭
- **验证**：netstat 确认 5174=desktop、5175=mobile 监听正常；验证后已关闭进程（当前无 dev server 占用）
- **遗留**：无

### 水印预览 404 排查提示词（2026-08-06，文档交付）
- **背景**：系统设置页 POST /api/v1/watermark/preview 浏览器报 404（多次出现）
- **排查结论（已核实）**：后端 system.py 已注册该 POST 路由且挂载于 /api/v1 前缀，pytest 用例断言返回 200；浏览器 404 主因是运行中的后端进程未加载新代码（uvicorn 未重启）或 vite /api 代理未生效
- **交付**：已编写中文排查提示词（含：路由注册/路径匹配/HTTP 方法/鉴权/代理与进程排查顺序、curl 与 pytest 验证手段、修复要求与回归项），供编码智能体直接使用
- **后续**：重启后端（uvicorn 加 --reload 或手动重启）即可恢复

### 单位管理：自动数字编码 + 编码隐藏（2026-08-06，已完成）
- **需求（用户确认）**：单位编码由系统自动生成（数字序列/算法），对用户隐藏；含功能逻辑/字段设计/界面交互说明
- **交付（后端）**：`_gen_dept_code` 自动生成纯数字编码 `{yyyyMMdd}{4位当日序号}`（自增序号算法，删除后序号复用冲突时递增重试保证唯一）；DeptReq.code 改为可选隐藏字段（忽略传入值）；create_department 不再接受前端编码、移除重复校验；update 本就不含 code（编码不可改）；列表/详情仍返回 code 供内部使用
- **交付（前端）**：Departments.tsx 移除「编码」输入项（新建弹窗仅名称/备注）、列表移除「编码」列；admin.ts createDepartment 类型去掉 code；页面提示“单位编码由系统自动生成（数字编码，对外隐藏）”
- **验证**：pytest 70/70 ✅（新增用例：不传 code 创建返回 12 位纯数字编码且以今日日期开头、二次创建序号唯一、编辑不改变编码）；两端 build ✅
- **遗留**：存量旧单位（手工编码 DEPT01 等）保持不变，新单位一律自动数字编码

### 领用管理菜单重构 + 盘点新增物料 + 仓管代申请（2026-08-06，已完成）
- **需求（用户确认）**：①「出库管理」改名为「领用管理」并新增子菜单：领用申请 / 领用申请单查询 / 领用审计；②修复 Outbound 页 slice undefined 运行时报错；③盘点允许当场新增账外物料并纳入盘点；④仓管员可在电脑端代使用者提交领用申请；⑤同步更新文档
- **交付（菜单/页面）**：AppLayout 新增「领用管理」组（领用申请 /requisitions/apply perm req:apply、领用申请单查询 /requisitions/query perm req:audit、领用审计 /requisitions）；删除 Outbound 页与路由（原 slice 报错随页面移除消除，新查询页全部字段空值保护 v?.slice/?? '-'）；新建 RequisitionApply.tsx（仓库/明细/使用信息/提交）与 RequisitionQuery.tsx（全状态筛选+单号关键词+详情 Drawer：私用标注/完成照片/水印下载/审计结果）；shared list 支持 keyword
- **交付（盘点新增物料）**：CheckItemReq 支持 check_item_id=0 + product_id（新增账外物料：账面 0、实盘录入、审核按盘盈入账并同步库存）；重复新增拦截 4006；mobile CheckRun「＋ 当场新增账外物料」（ProductPicker+实盘输入+去重）；desktop CheckDetail 新增物料区（Select+InputNumber+Tag 列表）
- **交付（仓管代申请）**：RequisitionReq 加 applicant_id（仅管理员=超管或有 req:audit 可代申请，目标须启用用户）；GET /requisitions/applicants（管理员返回全部启用使用者、普通使用者仅自己）；desktop 申请页有审计权限时显示「申请人」下拉，提交提示申请人姓名
- **验证**：pytest 69/69 ✅（新增：盘点新增物料全流程+重复拦截+审核入账、代申请+越权 403+申请人列表隔离）；两端 build ✅
- **遗留**：无

### 水印位置设置 + 预览（2026-08-06，已完成）
- **需求（用户确认）**：水印添加位置可设置；提供预览（管理员配置时预览 + 用户提交完成拍照前预览）
- **交付（后端）**：新建 services/watermark.py（水印核心：模板渲染/换行/位置定位，6 位置 bottom/top/bottom-left/bottom-right/top-left/top-right，黑条白字不占满全宽按位置对齐）；sys_config watermark.position（默认 bottom，init.sql + 本地库 INSERT）；GET /requisitions/{id}/work-photo 按配置位置渲染；POST /watermark/preview（sys:config，示例底图即时预览未保存也可）；POST /files/{file_id}/watermark-preview（登录，仅本人/超管，按当前配置对真实照片渲染，原始照片不保存）
- **交付（前端）**：desktop Settings 基础设置加水印位置 Select（6 项中文标签）+「预览水印效果」按钮 → Modal 展示示例图；mobile 完成工作拍照区加「预览水印效果」按钮 → 黑底 Popup 展示真实照片水印预览（当前定位/地点/时间）
- **验证**：pytest 67/67 ✅（新增：改位置后下载正常、示例预览 200、真实照片预览 200、非本人预览 403）；两端 build ✅
- **遗留**：无

### 领用流程改造：三步合一 + 完成工作拍照水印（2026-08-06，已完成）
- **需求（用户确认）**：①申请页三步（选材料/填信息/确认）合并为统一提交；②完成工作后在工作地点拍照留痕；③读取照片 GPS/手机定位并为照片加地点/时间水印；④水印内容管理员可改、仅下载时动态添加（原图不保存）；⑤流程改为：领用申请 → 完成工作 → 仓管员审计 → 完成
- **交付（后端）**：状态机改为 1 待完成工作 / 2 待审计 / 3 已完成 / 4 已驳回 / 5 已取消（常量值变更，report 待办统计自动跟随 status==2）；out_requisition 加 work_photo_file_id/work_done_at/work_lat/work_lng（init.sql + 本地 ALTER）；POST /requisitions/{id}/work-done（本人、仅状态 1、校验照片存在、记录定位、置状态 2、通知仓管员）；audit 仅状态 2；取消支持 1/2；驳回重提回 1 并清空完成照片；GET /requisitions/{id}/work-photo 下载水印图（Pillow 动态绘制：模板占位符 {location}/{time}/{gps}，底部黑条白字自动换行，原图不落盘）；sys_config watermark.template（默认「地点：{location}｜时间：{time}｜坐标：{gps}」）+ Settings key
- **交付（前端）**：mobile 申请页合并单页（仓库/明细/使用信息/拍照/底部固定提交）；详情页状态映射更新 + 待完成工作（本人）显示「完成工作拍照」卡片（PhotoUpload + geolocation 自动/手动定位 + 水印说明 + 提交）＋已提交后预览原图 + 下载水印图按钮 + 取消按钮（1/2）；MyRequisitions/Home 状态文案更新；desktop 审计页默认待审计（2）、状态映射/审计条件/完成照片预览/下载水印按钮、驳回提示改 4；Outbound 领用出库=已完成（3）；Settings 基础设置加水印模板输入框
- **验证**：pytest 66/66 ✅（流程用例改为：未完成工作不可审计 4002 → work-done（定位入库）→ 待审计列表 → 重复提交 4002 → 水印下载 PNG 且体积大于原图 → 审计通过状态 3；驳回/重提清空照片/取消状态 5）；两端 build ✅
- **遗留**：手机定位需 HTTPS + 用户授权（生产已 HTTPS；dev 8443 可用）；GPS 用浏览器定位而非照片 EXIF（照片 EXIF 无 GPS 时显示未获取定位）

### 电脑端出库管理（2026-08-06，已完成）
- **需求（用户确认）**：电脑端与手机端功能同步——电脑端此前无法查看出库信息、无法执行出库操作
- **交付（前端，后端全复用）**：desktop 新增「出库管理」页 /outbound（库存管理菜单，perm: stk:other）：①出库记录 Tab——子页「其他出库」（报废/报损/赠品出/其他出，类型筛选+详情 Drawer+作废）与「领用出库」（已通过领用单列表：申请人/使用地点/原因/数量+详情 Drawer，需 req:audit 权限，无权限隐藏）；②出库操作 Tab——新建其他出库表单（类型/仓库/动态明细行，提交后自动切到对应类型记录）；AppLayout 菜单 + 路由 + TITLES 注册
- **验证**：两端 build ✅（后端无改动，出库/领用接口既有 pytest 66/66 覆盖）
- **遗留**：无

### 图片悬浮/点击预览（2026-08-06）
- **交付**：shared 新增 FileImage 组件——小图展示；桌面鼠标悬浮大图浮层、点击全屏 Lightbox 放大；手机端点击直接放大；不下载文件（登录 Cookie 直读 GET /files/{id}）；替换盘点明细照片列（原新窗口链接）与手机拍照上传缩略图
- **验证**：两端 build ✅
- **提交**：feat(ui)

### 领用流程改造：提交即自动出库（2026-08-06，已完成）
- **需求（用户确认）**：领用(新增拍照物品(可选)) → **自动出库**(库存不够先负数再提示管理员——存在实际和仓库不符) → 使用地点(拍照(可选)) → 审计
- **交付**：post_stock_change 加 allow_negative；创建/驳回重提 = 同一事务自动出库(允许负库存)，库存为负时站内通知管理员(超管/管理者/仓管员，biz_type=预警)；驳回/取消自动回补库存(领用驳回回补/领用取消回补流水)；审计通过仅确认状态(库存已扣)；out_requisition 加 location_photo_file_id(使用地点照片，可选)；init.sql+本地库 ALTER 同步；响应带 shortages 提示
- **验证**：pytest 65/65 ✅（负库存+管理员通知/驳回回补/重提出库/取消回补/地点照片）；提交 3c1630b
- **遗留**：无

### 本轮需求清单（2026-08-06，防失忆）
1. ⏳ 修改密码（后端 PUT /auth/password + 前端入口）
2. ⏳ 忘记密码：邮箱找回/联系管理员电话找回，系统设置配置启用方式
3. ✅ 退出登录（后端已有；desktop/mobile 入口已有）
4. ⏳ 登录连续错误 3 次 → 要求输入 4 位数字+英文字母验证码
5. ⏳ 注册账号：系统设置配置 开放/关闭/审核 三种模式
6. ⏳ 单位功能：角色所属单位下可用显示仓库货架；完善角色货架管理与显示
7. ✅ 入口修复（进行中，见下）：手机版后缀 /m/ 与电脑版 / 双入口 + nginx 反代兼容 + 用户选择优先于设备判断 + 已登录直进主页 + 防循环

### 入口/登录态修复（2026-08-06，已完成）
- **交付**：otherEndUrl 开发环境同主机换端口(兼容局域网 IP/域名，不再硬编码 localhost)、生产 /login 与 /m/login 直达登录页；mobile 生产路由 basename=/m/（nginx /m/ 前缀分发、刷新直达正常）；desktop 路由 /m/* 重定向防误入；Landing 优先级=用户选择(localStorage wlt_entry) > 已登录(fetchMe) > 设备检测；两端 Login 页已登录自动进主页；用户选择写入 setEntryPreference 后不再被设备检测弹回
- **验证**：两端 build ✅（3c1630b 提交）
- **问题**：原自动跳转导致手机点"电脑版入口"被弹回手机版（循环）→ 用户选择优先方案

### 认证增强 + 单位货架（2026-08-06，完成）
- **认证**：修改密码 PUT /auth/password（改密后其他会话失效）；登录失败 3 次（10 分钟窗口）→ 需 4 位数字+字母验证码（GET /auth/captcha 图片 base64，内存态，错误码 4007）；忘记密码 POST /auth/forgot + /auth/forgot/reset（email 模式：SMTP 发 6 位重置码；phone 模式：展示管理员电话；both 优先邮箱；服务 mail.py）；注册 POST /auth/register + GET /auth/register/status（open 直接建使用者账号 / closed 拒绝 / review 进 sys_register_apply 审核队列）；管理端注册审核 GET /register-applies + approve/reject（admin.py）；sys_user 加 email；系统设置新增配置（auth.register_mode/auth.forgot_method/site.contact_phone/smtp.*）
- **单位货架**：新表 base_department/base_department_shelf、sys_role.department_id、权限点 24 dept:manage；单位 CRUD + /departments/{id}/shelves 关联货架（base_data.py）；角色创建/输出含 department_id；货架列表 GET /warehouses/{wh_id}/shelves 按单位过滤（非超管/管理者角色仅见本单位关联货架）
- **前端**：desktop Login（验证码/忘记密码/注册入口+Modal）、AppLayout 顶栏修改密码 Modal+注册审核/单位管理菜单、系统设置页新增注册/找回/SMTP 配置、用户管理加邮箱、角色管理加所属单位、新页 /system/register-applies 注册审核、/system/departments 单位管理（配置货架）；mobile Login（验证码/忘记/注册）、Mine 修改密码
- **验证**：pytest 62/62 ✅（新增 test_auth_ext.py 6 用例：改密/验证码 3 次后/找回 phone+email 分支/注册三种模式+审核/单位货架过滤/权限）；两端 build ✅
- **遗留**：验证码/重置码为内存态（单进程部署 OK）；邮箱找回需配置 SMTP 后实测发信
- **问题**：autoflush=False 导致 _deduct_items 查不到新明细 → 显式 db.flush()；验证码被错误尝试消耗（pop）→ 测试先读码再试错；register-applies 是 GET（测试误用 POST 405）
- **提交**：feat(auth)+feat(dept)（3c1630b）

### 商品编码纯数字 + 物料编码 + 单号详情（2026-08-06，完成）
- **商品编码**：base_product.code 纯数字（创建/更新/标准导入/AI 建议新增均校验，留空自动生成 = 当前最大数字编码+1，_next_numeric_code）；测试库历史商品编码（字母前缀）全部改为纯数字
- **物料编码**：base_product.material_code（公司系统编码，可空）；公司导入：物料编码→material_code（唯一去重），商品编码自动纯数字；物料编码为空的行正常导入并在响应 notice 提示"请管理员补充物料编码"；商品列表 keyword 搜索含 material_code；创建/编辑接口支持 material_code（编辑留空保持原编码）
- **单号详情**：通用 BillDetailDrawer 组件（头部字段网格+明细表格+状态 Tag）；采购入库/库存调拨/其他出入库列表单号可点击 → 详情抽屉（含明细行）；领用审计列表单号可点击 → 打开审计详情；盘点已有独立详情页；shared 新增 purchaseApi/transferApi/otherIoApi detail 接口与详情类型
- **验证**：pytest 65/65 ✅（新增 test_product_code.py 3 用例：纯数字校验+自动生成/物料编码保存更新/公司导入映射+notice）；两端 build ✅
- **遗留**：无
- **问题**：测试商品编码批量改数字时 _TAG 共用导致全量测试编码冲突（+1/+2 偏移解决）；keyword 搜索漏 material_code 已补
- **提交**：feat(product)+feat(bill)（e737522）

## 待办需求（2026-08-06 两轮，全部完成）

### 第一轮：管理后台修复（用户反馈，✅ 完成）
1. ✅ **列表页 API 404**：起最新代码实测 6 个接口全部 200 code 0——**后端路由完好，404 根因是旧后端进程未重启**；已重启验证并建议用户重启后端/前端 dev 代理指向 8443
2. ✅ **antd Modal 警告**：`destroyOnClose` → `destroyOnHidden`（Users/Roles/Departments/AppLayout/BillDetailDrawer/Login/RequisitionAudit）
3. ✅ **antd message 静态方法警告**：desktop 15 个文件全部 `message.xxx` 改 `App.useApp()` 实例（含 AppLayout/Login/全部列表与表单页）
4. ✅ **菜单/标题改名**：“角色与权限”→“用户权限设置”（AppLayout 菜单+标题、Roles.tsx）

### 第二轮：界面/交互优化（用户反馈，✅ 完成）
1. ✅ **系统设置界面风格统一**：Settings.tsx 重写为 antd（Form/Card/Input/InputNumber/Radio/Button/Spin/App.useApp）
2. ✅ **采购入库明细字段**：明细行显示 物料编码/材料名称/型号规格/单位/数量（选中材料快照行）；前端 19 个文件“商品/商品名称”文案统一改“材料名称”（含 JSX 文本/反引号）；/products keyword 补 spec 搜索；选择器按 物料编码/材料名称/型号规格 过滤
3. ✅ **仓库与货架管理页**：/warehouses 重做为 左侧仓库 CRUD + 右侧货架/库位 CRUD（自适应 flex 卡片），保留 2D 货架图入口；shared baseApi 补 9 个 CRUD 接口
4. ✅ **加载体验**：9 个列表页（Users/Roles/Logs/Backups/RegisterApplies/Departments/PurchaseIn/Transfers/OtherIo）Table 加 loading + emptyText 暂无数据；StockQuery/Checks/CheckDetail 补空态
5. ✅ **库存查询添加物料编码**：/stock 返回行加 material_code（StockRow schema + stock.py）；StockQuery 表格新增“物料编码”列

## 入库智能化 + 材料/供应商管理 + 内网访问（2026-08-06，已完成）

- **条码识别**：新增 `POST /barcode/decode`（zxing-cpp 服务端解码，PIL 先解码图片再识别；requirements.txt 加 zxing-cpp）；`services/ocr/barcode.py` decode_barcode；条码唯一性校验（创建/编辑，非空条码全局唯一 4006「条码已存在」）；shared `ocrApi.decodeBarcode` + `resolveByBarcode`
- **入库页（desktop）**：新建入库 Modal 增加**表头（标题行）**——入库仓库/供应商/入库日期/备注；明细行加**条码输入（可选）**（扫码枪回车自动匹配材料，命中直接带入、未命中弹「材料不存在，是否新增」Modal 预填条码）；送货单 OCR 未匹配行加「用识别数据新增材料」按钮（预填识别名）；提交带 supplier_id/bill_date/remark
- **入库页（mobile）**：表头卡片（仓库/供应商/日期/备注）；明细行条码输入 +「扫码」拍照识别（上传→decode→匹配，未匹配 Dialog 确认后 Popup 新增材料）；无材料新增用识别数据建材料并带入
- **材料管理（desktop 新增 /materials）**：材料 CRUD（列表/搜索/新建/编辑/停用启用/条码+物料编码+规格+分类+单位+进价+上下限+备注）；**新建/编辑表单材料名称与条码支持相机输入**（名称拍照 OCR 快查识别、条码拍照 zxing 解码，CameraOutlined 按钮 + capture 隐藏 input）；菜单「基础资料→材料管理」
- **供应商管理（desktop 新增 /suppliers）**：CRUD + 搜索 + 查看详情 Drawer（关联材料列表）+ 停用/启用；菜单「基础资料→供应商管理」
- **材料-供应商关联（多对多）**：新表 `base_product_supplier`（四层同步：init.sql+本地库+models+测试）；ProductReq.supplier_ids（None=保持/[]=清空）、ProductOut 输出 supplier_ids/supplier_names；`GET /suppliers/{id}/products` 供应商查关联材料；**删除保护**：有启用材料关联时禁止停用（4006 提示先解除），停用时清理残留关联；材料列表加供应商列、表单多选关联
- **内网访问修复**：两端 vite.config `host: true`（监听 0.0.0.0，局域网设备可访问，需信任自签名证书）；清理占用 5175 的残留 dev 进程，重启 dev（5174/5175）+ 后端（8443 新代码）
- **验证**：pytest 81/81 ✅（新增 test_barcode.py 7 用例：EAN13 真实解码/无条码 4006/文件缺失/权限 403/条码唯一性/入库表头 supplier+date+remark+经办人；test_supplier_link.py 3 用例：关联 CRUD+去重+局部更新保持/供应商查关联材料/删除保护+清理）；两端 tsc + build ✅；httpx 冒烟：decode 6901234567892、重复条码 4006、供应商删除保护 4006、解除后删除 ✅；局域网 IP 5174/5175/8443 访问 200 ✅
- **问题**：write/bash 大参数被运行时 [cache hygiene] 替换为占位符导致文件损坏/命令语法错误（Suppliers.tsx 286B 损坏、Inbound bash heredoc 失败）→ 紧凑 write 后必须 wc -l/grep 验证；环境已配置真实 DeepSeek Key → test_ocr_recognize_task mock get_llm 强制降级分支保证确定性；delete_supplier 重复定义（旧无保护版本先注册）→ 删旧保留新
- **遗留**：部署库需执行 init.sql 的 base_product_supplier 建表语句（已提供 CREATE TABLE IF NOT EXISTS）

## 送货单 OCR 增强 + 新建入库重写 + 模板解析（2026-08-06，已完成）

- **OCR 录入增强（DeliveryOcr）**：结构化新增 物料编码/规格型号/供应商/送货单号（DeepSeek prompt 扩展）；表格新增 物料编码/规格型号/单位 可编辑列；供应商/单号从识别结果提取可修改；行内「历史价」查询；「确认供应商并带入入库」调 `POST /ocr/delivery/confirm`（供应商同名匹配/自动创建 OCR+日期编码、识别记录回写 supplier/bill_no/items、match_status=3）→ 带入 supplier_id + ocr_record_id
- **送货单模板解析（services/ocr/template.py）**：本地规则秒级结构化（物料编码锚点/名称/规格/单位/数量/单价/金额启发式分配 + 单价×数量校验），命中不再调用 DeepSeek，**减少等待**；未命中才走大模型兜底；识别任务响应带 `_engine: template|deepseek` 前端标注「模板匹配/大模型分析」
- **送货单↔入库单关联**：`pch_purchase_in.ocr_record_id`（init.sql+本地 ALTER+models）；PurchaseInReq.ocr_record_id 校验存在（4006）；详情回显；入库页显示送货单号 Tag
- **历史采购价**：`GET /purchase-in/history-price`（材料/供应商过滤，入库/OCR 页 Drawer 展示）
- **新建入库界面重写（PurchaseIn）**：顶部 入库仓库/供应商/入库日期/备注（去"表头"文字）；明细表格列：物料编码（AutoComplete 可查询）/条形码（相机扫码）/材料名称（相机 OCR + 大模型兜底分析确认新增）/规格型号/单位/金额（进价）/数量/库位/操作；材料行「历史价」链接
- **验证**：pytest 84/84 ✅；真实测试图 OCR进货单测试.jpg/2.png 全链路冒烟：模板 2.1s 识别（供应商/单号/物料编码/数量/单价/金额正确）→ confirm 供应商落库 → 入库关联 → 历史价 ✅；两端 tsc + build ✅
- **问题**：zxing-cpp `read_barcodes` 不接受压缩字节需 PIL 先解码（记入工具记录）；模板 spec 拼接过宽曾吞名称（收紧为 字母/数字/符号开头）；delivery/confirm 字段名须与模板统一为 product_name
- **遗留**：部署库需执行 `ALTER TABLE pch_purchase_in ADD COLUMN ocr_record_id ...`（语句见 init.sql）

## 菜单重分类 + 统计面板改名 + 历史价格管理 + 领用申请重做（2026-08-06，已完成）

- **菜单重分类（AppLayout）**：工作台→统计面板（原经营看板，菜单/TITLES/页面标题同步）；库存管理新增「历史价格管理」（/history-price，stk:query）；其余分组按需求核对（基础资料/入库管理/库存管理/领用管理/报表中心/系统管理子项与需求一致）
- **历史价格管理（/history-price 新页）**：`/purchase-in/history-price` 扩展支持 product_id=0 全量 + keyword（名称/编码/物料编码/规格）+ 供应商筛选 + 分页（返回含 product_id/product_name/material_code/spec，展示「谁供的货」）；页面：材料关键词 + 供应商下拉 + 日期倒序表格（入库日期/单号/物料编码/材料名称/规格/单位/单价/数量/金额/供应商）；shared historyPrice 改对象参数
- **领用申请界面重做（RequisitionApply）**：卡片化布局（申请信息：申请人代申请/出库仓库；材料明细：行内展示 编码/规格/单位 快照；使用信息：地点/原因/备注）；底部汇总栏（已填 N/M 项、合计数量、流程提示 Tag）；提交 API/校验/代申请逻辑保持不变
- **验证**：pytest 84/84 ✅（history-price 旧用例兼容）；两端 tsc + build ✅；httpx 冒烟：全量 934 条分页、keyword 过滤、行字段完整 ✅

## 送货单 OCR 漏录补录 + 原始单据参考 + 物料自动新增（2026-08-06，已完成）

- **手动补录**：识别结果表格新增「+ 添加明细」按钮（识别为空时也可手动录入），确认时自动跳过未填名称的空行
- **原始单据参考**：识别完成后 Collapse 折叠展示 送货单图片（上传返回 url 直显）+ 格式化 OCR 原文（行号 01 前缀、等宽字体）；后端识别任务响应 structured 补 `lines` 原文供展示
- **物料自动新增**：`/ocr/delivery/confirm` 确认转入入库时，对每条明细按 物料编码 → 名称 精确匹配系统材料；不存在则自动新增（单位自动匹配/创建、编码自动生成、进价=识别单价、备注"送货单 OCR 自动创建"），响应 items 带 `product_id` + `_created` 标记 + `created_products` 汇总；入库页 prefill 优先按 product_id 直接取材料，前端提示「自动新增 N 个系统中不存在的物料」
- **验证**：pytest 85/85 ✅（新增 test_confirm_auto_creates_missing_product：自动建料/字段落库/同名匹配不重复建）；两端 tsc + build ✅；真实测试图冒烟：模板识别 75 行原文 + 4 明细 → 确认时 3 个不存在物料自动新增（网络测试仪已存在则匹配）✅

## 全站编码展示清理 + 送货单 OCR 界面优化（2026-08-06，已完成）

- **编码展示清理**（仅隐藏展示层，内部逻辑不变）：商品编码（系统纯数字）从 材料管理提示/库存查询列/进销存报表列/货架图明细/其他出库明细行/盘点选择器 label/AI 建议确认表单 移除；供应商编码 列/搜索/表单输入隐藏（新建自动生成 SUP+时间戳，编辑保持原值）；角色编码 列/表单输入隐藏（新建自动生成 role+时间戳）；仓库编码 新建表单隐藏（自动 WH+时间戳）；货架编码改名"货架编号"（业务标识保留）；"编码"字样搜索占位统一改"物料编码"（业务字段，送货单上即此编号）；保留：物料编码/条码/货架编号/库位（用户可理解的业务标识）
- **送货单 OCR 界面优化**：明细表新增 序号列/删除行按钮；数量/单价改 InputNumber（数字输入）；金额列自动计算（单价×数量）并加粗；表格 footer 显示 总项数 + 总金额；识别中 Spin 文案细化（模板秒级/大模型 3-10 秒）；识别失败 Alert 展示原因 + 提示重新上传；「+ 添加明细」与合计同排
- **验证**：两端 tsc + build ✅；后端冒烟：供应商创建/编辑（前端自动编码）、角色自动编码链路 ✅；后端无改动（纯前端展示）
- **改动文件**：desktop Materials/PurchaseIn/Reports/Roles/ShelfMap/StockQuery/Suppliers/Warehouses/AiSuggestions/CheckDetail/OtherIo/Users/DeliveryOcr、mobile StockQuery/ProductPicker

## 菜单重命名 + 盘点移入报表中心 + 路由权限控制（2026-08-06，已完成）

- **重命名**：入库管理 →「材料入库」（原入库）/「送货单识别入库」（原送货单 OCR 录入）；菜单/TITLES/页面 h2/导航按钮文案同步
- **盘点移动**：库存盘点（库存管理）→「盘点」（报表中心，位于 进销存报表 与 AI 建议处理 之间，权限仍 stk:check）；盘点列表单号改为可点击链接 → /checks/{id} 详情页
- **权限控制**：菜单原有按 hasPerm 过滤 + 空分组隐藏；新增**路由级拦截**——RequireAuth 按当前路径匹配菜单权限点，无权限直接访问 URL 时重定向入口页（Landing），避免绕过菜单直访；MENU 从 AppLayout 导出供守卫复用
- **验证**：两端 tsc + build ✅；后端无改动

## 原始单据参考模块重构（2026-08-06，已完成）

- **原始单据参考卡片**（送货单识别入库页，独立卡片不再折叠）：左侧参考图片带绿色「图片 ✓」标记、点击图片打开 Modal 放大查看（原图预览 860px）；无图片显示「图片 —」占位；右侧 OCR 原文区显示整理后文本（行号 01 前缀、过滤空白行、等宽字体），原文为 0 行时显示占位「暂无 OCR 原文（0 行）」
- **修复 OCR 原文 0 行根因**：前端 poll done 仅在「无结构化条目」分支读取 lines——结构化成功（有 items）时原文从不赋值；改为无论是否结构化成功都统一读取 structured.lines（后端识别任务已返回原文，冒烟 75 行）
- **移除「返回材料入库」按钮**（顶部仅保留标题与上传入口；识别后仍可经确认流程进入入库）
- **验证**：两端 tsc + build ✅；真实测试图冒烟：识别任务返回 75 行原文（结构化成功场景）✅

## PP-OCR 集成修复：show_log 参数 + paddle 版本兼容（2026-08-06，已完成）

- **show_log 参数报错**：PaddleOCR 新版（3.7）移除 show_log 参数，且对未知参数抛 **ValueError**（旧代码只捕获 TypeError，回退分支仍带 show_log）→ 重写 `_ensure_init` 为**参数降级链**（全参→去 show_log→去 ocr_version→最小参数，逐级 try，捕获 TypeError+ValueError，全部失败抛 OCRInitError 带可读提示）
- **paddle 3.3.1 执行器 bug**：paddlepaddle 3.3.1 Windows CPU 版 oneDNN PIR 执行器报 `ConvertPirAttribute2RuntimeAttribute not support`（v4/v5/v6 模型均复现，FLAGS 无效）→ **固定安装 paddlepaddle==3.2.2**（自动安装接口 + scripts/setup_ppocr.py 同步），真实识别验证通过（PP-OCRv6，78 行）
- **验证**：新增 test_paddle_engine.py 3 用例（ValueError 降级/去 ocr_version/全失败 OCRInitError）；pytest 88/88 ✅；端到端冒烟：切换 paddle 引擎 → health ready → 送货单识别 done（20s，79 行 → 模板 4 条明细）✅
- **改动文件**：backend/app/services/ocr/paddleocr_api.py、app/api/system.py（安装命令固定版本）、scripts/setup_ppocr.py、tests/test_paddle_engine.py

## 送货单识别入库：双识别方式选择（2026-08-06，已完成）

- **后端**：`/ocr/recognize` 新增 `mode` 参数（auto 模板优先+大模型兜底 / template 仅本地模板 / llm 仅大模型）；非法值 4006；shared `ocrApi.recognize(fileId, ocrType, mode)`
- **前端（DeliveryOcr）**：上传区新增识别方式按钮组——「本地模板快速识别（秒级）/ 大模型识别」；选大模型时顶部黄色 Alert 提示"需等待 30-60 秒，请耐心等待，勿关闭页面"；识别中 Spin 按模式显示文案（大模型 30-60 秒 / 本地模板秒级）；轮询按模式区分（大模型 5s×24≈120s，模板 1.5s×20），超时给出明确提示（"大模型识别超时，请重试或改用本地模板"）
- **PP-OCRv6 模板更新**：template.py 新增表头列名过滤（含税单价/申报单位/价税合计/物料编码/行号等），解决 PP-OCRv6 把表头行混入明细的问题；真实 v6 输出验证：图1 名称/数量/单价/金额全部正确，图2 5 条明细价格全对
- **验证**：两种模式端到端冒烟——template 模式 11s engine=template 4 条明细（未调用大模型）；llm 模式 22s engine=deepseek 4 条明细（真实 DeepSeek 结构化）；pytest 88/88 ✅；两端 tsc + build ✅

## PP-OCR 默认引擎 + OCR 环境安装增强 + 登录循环修复等（2026-08-06，已完成）

- **PP-OCR 设为默认引擎**：config.py `OCR_ENGINE` 默认 rapidocr→paddle；.env.example、init.sql（sys_config ocr.engine + ocr_record.engine 列默认）、Settings 页 EMPTY 与 Radio 文案（PP-OCR（PaddleOCR，默认引擎，可自动安装））同步；requirements.txt 增加 `paddlepaddle==3.2.2`（Windows 固定版，3.3.x oneDNN PIR 执行器 bug）+ `paddleocr>=3.0`（GPU 版用 scripts/install_*_gpu.py 安装，wheel 自带 CUDA 运行时）
- **OCR 环境状态显示 CPU/GPU**：`GET /ocr/install-status` 新增 mode 字段（`paddle.device.is_compiled_with_cuda()` 检测，import 较重结果缓存 60s；paddleocr 已装即返回 done + mode）；Settings 页 Tag 显示「已安装（GPU 加速 / CPU）」、完成提示含运行方式
- **重新安装按钮**：Settings 页状态为已安装（done）时按钮文案变为「重新安装（paddlepaddle + paddleocr）」，可重复触发安装（installing 中仍拒绝并发）
- **GPU 安装脚本支持免交互**：install_linux_gpu.py / install_windows_gpu.py 新增 argparse：`--yes`（跳过确认提示）、`--cuda {cu126,cu123,cu118}`（跳过选择）、`project_dir` 位置参数（前端/自动化调用用），`python -m py_compile` 验证通过
- **登录页无限刷新修复（根因）**：client.ts 401 时无条件 `window.location.href="/login"`——已处于 /login 时赋相同值也会整页刷新，Login 挂载 fetchMe()→401→刷新→再 fetchMe…死循环；改为 `pathname !== "/login"` 才跳转
- **antd 6 Alert 弃用修复**：Alert `message`→`title`（DeliveryOcr 2 处、Login 2 处、Settings 1 处），消除控制台警告
- **送货单识别提示风格统一**：两种识别方式共用 info Alert（同样式同句式「xx 识别：…；识别后逐项确认再带入入库。」）
- **导航栏默认全展开**：Sider 可折叠（已有，默认展开）+ Menu `defaultOpenKeys` 传全部分类 key，刷新后分类子项默认全展开
- **验证**：两端 typecheck ✅；后端重启（8443 新代码）health ok；install-status 接口返回 mode 验证 ✅
- **改动文件**：backend app/config.py、sql/init.sql、requirements.txt、app/api/system.py、scripts/install_{linux,windows}_gpu.py、.env.example；frontend shared client.ts/system.ts、desktop AppLayout/Settings/DeliveryOcr/Login
- **遗留**：① requirements.txt 增加 paddle 后全新部署 `pip install -r` 会装 CPU 版 paddle（约 200MB+，首次识别自动下载模型）；② 已有库的 sys_config ocr.engine 值不会被 init.sql 覆盖（改默认需手动 UPDATE 或 .env OCR_ENGINE）；③ 本轮改动未 git 提交（如需要可整理提交）

## 新建入库修复：重复导入 / 供应商传递 / 显示与表格交互（2026-08-06，已完成）

- **重复导入修复（根因：StrictMode 双执行）**：main.tsx 启用 `<React.StrictMode>`，dev 下 effect 双调用——PurchaseIn 预填 effect 把同一批送货单明细追加两次。修复：① 预填 effect 加 `prefillDone` ref 锁（只执行一次）；② 送货单确认改 **sessionStorage 传递预填数据**（key: purchaseInPrefill，消费即删），不再拼进 URL——同时解决 URL 过长截断与刷新重复导入；③ 入库提交成功后再次清除 storage；④ 保留 URL ?items= 参数兼容直达
- **供应商传递修复**：确认链路核实（DeliveryOcr confirm → data.supplier_id → sessionStorage.supplierId → PurchaseIn setSupplierId → 提交 purchaseIn(supplierId)）；改用 sessionStorage 后不再受 URL 长度/刷新影响；供应商下拉 options 保持全量（page_size 100）
- **规格型号显示修复**：列宽 130→180 并支持拖拽；预填时规格优先取 OCR 识别值（it.spec 优先于产品资料 p.spec）；材料名称下拉不再把规格拼进 label（避免长文本截断，规格有独立列）
- **单位数据修复**：① confirm 映射 items 补 `unit` 字段（后端本就返回）；② PrefillItem/Row 新增 unit，行显示 `r.unit || product.unit_name`（OCR 单位优先，产品资料兜底）；③ 产品下拉从 20 条扩到前 500 条（此前预填材料不在前 20 时下拉回显数字 id、单位显示 -）；④ 当前行材料不在下拉列表时回显其名称；⑤ 大模型兜底新增材料弹窗增加**单位选择**（此前固定取第一个单位）
- **界面自适应缩放**：新建入库 Modal 宽度 1180 固定 → `min(1180px, calc(100vw - 48px))`；明细表格 `scroll={{x:1300}}` 小窗口横向滚动；表头 Space wrap 已有
- **表格自定义调整**：列宽拖拽——无依赖 ResizableTitle 表头组件（components.header.cell + onHeaderCell，mousedown/mousemove 调整，最小 60px）；行高——「紧凑行高/标准行高」按钮切换 Table size
- **验证**：两端 typecheck ✅；改动文件 desktop PurchaseIn/DeliveryOcr
- **遗留**：列宽调整不持久化（刷新恢复默认，如需持久化可存 localStorage）

## RapidOCREngine 改为单次进程模式（2026-08-06，已完成）

- **根因**：RapidOCR-json.exe 处理完一张图即退出（**非驻留**），原 `RapidOCREngine` 单例缓存 OcrAPI 实例复用已退出的进程 → 第二次识别必然失败（code 400「子进程已结束」）
- **修改**（backend/app/services/ocr/client.py）：`recognize` 每次新建 `OcrAPI()` 进程，`finally` 中 `stop()` 销毁（放回结果即销毁）；`health` 临时启动一次验证可用性；删除 `_api/_lock/_ensure_init`（无共享管道状态，天然并发安全，代价是每次识别含模型加载约 1-2 秒）；文件/类 docstring 同步为「单次进程」描述；paddleocr_api.py 注释同步（Paddle 单例常驻，Rapid 单次）
- **验证**：py_compile ✅；pytest OCR 相关 13/13 ✅（test_paddle_engine 3 + test_ocr* 10）

## 供应商传递修复（第二轮）+ 材料查询服务端化 + 提交校验细化（2026-08-06，已完成）

- **供应商传递根因修复（StrictMode 二次覆盖）**：PurchaseIn 挂载 effect 在 StrictMode（dev）下双执行——首次消费 sessionStorage 后已删除，第二次走 else 分支用 URL 参数（无参数即 0）**覆盖 supplierId/ocrRecordId** → 预填供应商丢失需手动选择。修复：`prefillConsumed` ref 保证预填只消费一次；供应商名一并传入（`supplierName`），自动创建的新供应商不在下拉列表时 `supplierOptions` 回显名称（不再显示数字 id）
- **供应商自动创建行为完善**：后端 `/ocr/delivery/confirm` 返回 `supplier_created`（同名不存在时自动创建供应商，OCR+日期编码）；前端确认时若供应商名为空弹 Modal 明确提示（继续则不关联供应商/返回填写）；供应商输入框旁提示「输入不存在的供应商名将自动创建」；成功消息提示「已自动创建新供应商：XXX」
- **材料查询服务端化（全库匹配）**：材料名称列改为服务端防抖搜索（queryByName，`baseApi.products(kw)`，候选含完整材料）；物料编码列 onSelect 优先用搜索结果里的完整材料（此前仅在前 500 条本地列表查找，命中不在列表时回填失败）；两列候选均含 product 直接回填，不再受本地列表条数限制
- **提交校验细化**：create() 逐项检查——缺材料提示「N 行未匹配到材料（已标红）」、缺库位提示「为 N 行选择库位（已标红）」、数量为 0 提示「数量大于 0」；校验失败后材料/库位列 Select `status="error"` 红框高亮（submitTried 状态，新建/成功后重置）
- **验证**：typecheck 两端 ✅；后端 confirm 三场景（自动创建 supplier_created=true / 已存在 false / 为空 0）测试通过

## 后端 API 审计 + 修复（2026-08-06，已完成）

- **已修复**：① `post_stock_change` 新增**库位-仓库归属校验**（库位不属于目标仓库时拒绝，避免库存挂错仓库导致报表不一致，一处修复覆盖入库/领用/调拨/盘点/其他出入库全部单据）；② `POST /purchase-in` 新增 supplier_id 存在性校验（防悬空外键）+ 进价非负校验；③ `GET /files/{file_id}` Content-Type 按实际扩展名返回（此前写死 webp），测试断言同步放宽为 image/
- **审计确认无问题**：用户管理（防自锁/防停用 admin/输出不含密码 hash）、领用单（详情仅本人或管理员、修改仅本人且仅驳回状态、取消仅本人+状态校验）、注册/找回（模式校验、验证码 TTL、重置后踢会话）、入库创建（仓库/OCR 记录/库位/数量校验+单号并发重试）、权限点覆盖（写操作全部带 require_permission，只读基础资料仅登录）
- **观察项（未改）**：① GET /purchase-in 列表/详情无权限点（设计上库存查询全员可见，加权限会破坏现有测试与设计）；② GET /ocr/tasks/{task_id} 无归属校验（UUID 难猜，低风险）；③ GET /files/{file_id} 任意登录可下载（内网设计权衡）；④ admin 用户可被有 sys:user 权限的管理员改角色（多超管场景属管理员职责）；⑤ /ocr/recognize 可用任意 file_id 发起识别（无限流）
- **验证**：pytest 88/88 ✅（新增校验未破坏现有用例）；后端已重启生效

## 水印背景透明 + 国标单位初始化（2026-08-06，已完成）

- **水印位置保存不生效排查**：API 实测保存→读回→预览链路正常（保存 top-right 生效）；此前表现为不生效多为后端未重启旧代码，已确认无逻辑缺陷
- **水印背景透明（新功能）**：新增配置 `watermark.bg_opaque`（1 黑色不透明底默认 / 0 透明仅白字黑描边）；`render_watermark` 增加 bg_opaque 参数；system.py 预览 / files.py 真实照片预览 / requisition.py 完成工作照片下载三个调用点全部读取配置；Settings 页新增「水印背景透明」Switch（EMPTY/保存/预览全链路）；init.sql 种子同步
- **踩坑**：预览请求字段名驼峰 `bgOpaque` 与后端 Pydantic 蛇形 `bg_opaque` 不匹配导致参数静默丢失（仍走 DB 默认黑底）→ 统一蛇形，实测黑底 17.26% vs 透明 1.89% 黑色占比，保存配置后无参预览亦生效 ✅
- **材料单位国标初始化**：base_unit 种子改为国标法定计量单位 + 常用量词（51 个：数量/包装/设备/长度/面积/体积/质量/时间/温度/能量单位）；数据库执行清空重导入——旧 5 单位（个/件/套/箱/盒）同名映射到新数据，14 个引用乱码单位（PC件/RP件/盒1cc130）的产品主单位归一（PC/RP→件、螺丝M6→盒），多单位关联表悬空 1931 行清理；验证：51 单位、产品主单位/多单位关联悬空均 0
- **验证**：typecheck ✅；pytest watermark/settings 5/5 ✅；水印 API 双模式实测 ✅；后端已重启（新代码）

## 全站表格组件统一改造：DataTable（2026-08-06，已完成）

- **通用组件 `components/DataTable.tsx`（新建）**：antd Table 增强封装——① 列宽拖拽（表头右侧 6px 手柄，最小 60px，刷新恢复默认）；② 行高切换（工具条「紧凑/标准」）；③ 行选择复选框 + 「批量删除」按钮（Modal 确认后回调 onBatchDelete）；④ 操作列（actions 回调自动追加最后一列）；透传 rowKey/columns/pagination/loading/scroll/locale/size/style/footer
- **替换范围（22 个文件，全站列表/明细表格）**：基础资料（Materials/Suppliers/Units/Departments；Warehouses 无 Table 不动）；系统管理（Users/Roles/Backups/Logs）；业务列表（StockQuery/HistoryPrice/OtherIo/Transfers/Checks/RequisitionQuery/RequisitionAudit/AiSuggestions）；其他（PurchaseIn 列表+明细、DeliveryOcr 明细、Reports、CheckDetail、RegisterApplies、RequisitionApply、BillDetailDrawer 组件内明细表）；PurchaseIn 本地 ResizableTitle/colWidths 清理（统一由 DataTable 提供）
- **批量删除（管理类页面，弹窗确认）**：Materials=deleteProduct（停用）、Suppliers=deleteSupplier（停用）、Users=deleteUser（停用）、Roles=deleteRole、Backups=deleteBackup、Units=deleteUnit、Departments=deleteDepartment；查询/单据/审计类页面不启用（无删除语义）
- **验证**：两端 typecheck ✅；desktop vite build ✅（chunk 大小警告为既有问题）；改后浏览器控制台无新增警告

## 批量审核 + 行高移除 + 列设置 + 历史价格单位同步（2026-08-06，已完成）

- **DataTable 增强**：① 新增 `batchActions`（自定义批量操作按钮组，逐按钮确认弹窗）；② 新增**列设置**（columnSelector 默认开启，工具条「列设置」Popover 勾选显示/隐藏列 = 自定义需要获取的字段）；③ **移除行高切换**（删除 dense 状态与「紧凑/标准行高」按钮，行高固定）
- **注册审核批量通过/拒绝**：RegisterApplies 改为 `batchActions`——「批量通过」（approveRegisterApply）/「批量拒绝」（rejectRegisterApply，确认弹窗）
- **领用审计批量通过/驳回**：RequisitionAudit 改为 `batchActions`——「批量通过」（audit approve）/「批量驳回」（audit reject，确认弹窗，库存回补）；刷新调用修正为 load(status, keyword, page)
- **历史价格单位同步**：`/purchase-in/history-price` 的 unit_name 由单据快照改为**材料当前单位**（产品存在时取 base_unit 当前名，否则回退快照）；实测历史价格单位集合 {件, 个} 无乱码
- **PurchaseIn**：删除行高切换按钮与 dense 状态，明细表固定 small
- **验证**：pytest 28/28（ocr/stock/barcode）✅；两端 typecheck ✅；desktop build ✅；history-price 接口实测 ✅；后端已重启生效

## 系统设置大模型模型列表获取（2026-08-07，已完成）

- **交付（后端）**：`POST /llm/siliconflow/models`、`/llm/deepseek/models`、`/llm/doubao/models`（sys:config）——用已保存 API Key 调 OpenAI 兼容 `/models` 拉模型列表 `{models:[{id,owned_by}]}`；未启用（enabled=0）→ 4006 提示先启用并保存、未配 Key → 4006、网络/鉴权失败 → 5002；本地 OCR 关闭时商品识别回退视觉模型 + 不可用提示
- **交付（前端）**：Settings「OCR 与大模型」Tab——三个模型名称输入框改 Select（选项来自拉取列表）；「获取模型列表」按钮（未填 Key 自动保存后拉取、掩码 Key 直接用已存 Key）；保存设置后对启用且填新 Key 的模型自动拉取；表单加载空值回退 EMPTY 默认（开关显示与存储一致）
- **验证**：pytest 65/65 + 两端 build；运行后端实测（SiliconFlow 401 为 Key 失效属配置问题）
- **问题**：设置页保存链路缺陷（见下一条修复）；模型列表接口未同步《后端API设计.md》§9（本次一并补全）
- **提交**：823ba4c + 工作区未提交部分（随下条一起提交）

## 系统设置保存失败修复（2026-08-07，已完成）

- **需求（用户反馈）**：系统设置 → OCR 与大模型 → 获取模型列表 → 选择模型 → 保存失败
- **根因**：`PUT /settings` 契约是 `dict[str, str]`（开发规范 §4.6 接口传字符串）；前端直接提交 `form.getFieldsValue()` 原始值——`session.expire_hours`/`smtp.port` 为 InputNumber，编辑后返回 number、清空返回 null → 后端 4006「body.session.expire_hours: Input should be a valid string」→ 前端 toast「保存失败」；「获取模型列表」按钮内嵌的自动保存同样受影响
- **修复**：Settings.tsx 新增 `toStrings()`，`save()` 与 `fetchModelList()` 提交前统一转字符串（number→String、null/undefined→""）；后端契约保持不变（严格校验更利于发现同类前端缺陷）
- **验证**：L1 typecheck + L4 desktop build ✅；后端实测：字符串全表单 200、数字/null 载荷 4006（契约符合预期）；《后端API设计.md》§9 补值契约与模型列表接口
- **遗留**：⚠️ 排查期间脚本误将测试 Key 写入共享开发库（MySQL binlog 未开启，原 Key 不可恢复）→ **需在系统设置重新填写视觉/文本/豆包 API Key**；其余配置已由用户后续保存动作恢复原值
- **提交**：fix(settings)（随本记录一并提交）

## 材料入库-新建入库 物料选择修复（2026-08-07，已完成）

- **需求（用户反馈）**：新建入库明细中——①物料编码输入/搜索时材料名称显示被查询结果影响（选中候选后编码列显示物料 id 而非真实编码）；②选中后无法取消/删除选择；③按物料编码/条码搜索应仅显示确实存在对应编码/条码的物料
- **根因**：①物料编码列 AutoComplete 候选 `value` 用 `String(p.id)`，选中时 onChange 后触发把编码列覆盖为物料 id（显示错误）；②材料名称列 Select 无 allowClear，onChange 未处理 undefined（无法清除）；③按编码搜索走全字段模糊匹配（名称含关键字也命中），且名称列候选不过滤字段
- **修复（PurchaseIn.tsx）**：
  - 物料编码列：候选仅含 `material_code` 非空且匹配的物料，候选 value 直接用物料编码（选中回填与 onChange 一致，杜绝 id 串位）；onSelect 回填完整物料（材料名称列同步显示对应名称）；手输/清空编码且与已选材料不符时同步清除材料选择（避免列间矛盾显示）；`notFoundContent`「无匹配物料（可手输）」空状态 + `allowClear`
  - 材料名称列：候选字段级过滤（名称/规格/物料编码/条码任一命中），**物料编码/条码精确命中优先置顶**（按编码/条码搜索目标必在最前）；`allowClear` + 清空时同步清空材料及其带入的编码/规格/单位；`notFoundContent`「无匹配材料」
  - **条形码自动带出（第二轮反馈）**：所有材料回填路径（材料名称选中/物料编码选中/条码扫码匹配/当场新增/大模型新增/OCR 预填）统一 `barcode: p.barcode`——选中材料后条形码列自动获取并填写
  - **显示框仅显示材料名称（第三轮反馈）**：候选与回显 option 的 label 改为纯名称（选中后显示框不再出现物料编码），下拉项完整信息（名称 · 物料编码（规格））改由 `optionRender` 展示
  - **antd 6 弃用修复**：库位下拉 `dropdownRender` → `popupRender`（控制台 Warning 消除）
- **验证**：L1 typecheck + L4 desktop build ✅（8.58s，仅既有 chunk 警告）；后端无改动
- **提交**：fix(purchase-in)（随本记录一并提交）

## 送货单识别入库修复：SiliconFlow 默认模型失效（2026-08-07，已完成）

- **需求（用户反馈）**：送货单识别入库/识别功能无法正常使用；要求测试大模型返回数据、确认解析逻辑、确保功能可用
- **根因**：`llm.siliconflow.model` 未配置（DB 空）→ `get_llm` 使用默认模型 `Qwen/Qwen2.5-VL-7B-Instruct`——**该模型已从 SiliconFlow 下线**（实测 `/models` 列表无此 id，调用返回 400 `Model does not exist`）→ `_delivery_by_vision` 返回 None → 任务 structured 为空 → 前端「未识别出结构化材料」→ 无法识别入库；且运行中后端未加载新代码（无 --reload）进一步掩盖问题
- **修复**：`services/llm.py` SiliconFlow 默认模型改为 **`nex-agi/Nex-N2-Pro`**（用户指定，实测识别准确）；Settings.tsx 视觉模型占位符同步；后端重启加载新代码
- **验证（真实大模型 + testdata 两张测试图全链路）**：
  - 图1 OCR进货单测试.jpg：`nex-agi/Nex-N2-Pro` 识别约 40s → 4 条明细全对（名称/物料编码/规格/单位/数量/单价/金额）+ 供应商「海口耐沃办公设备有限公司」+ 单号 POAB2025120071 + 11 行原文；DeepSeek 分类正常（无匹配分类→未分类）；`/ocr/delivery/confirm` → 供应商落库 + 4 条物料全部匹配 product_id
  - 图2 OCR进货单测试2.png：5 条明细全对 + 供应商「海南工友商贸有限公司」+ 单号 POAB2026050018
  - pytest 17/17（test_delivery_ocr/test_ocr/test_settings）✅；前端 build ✅
- **遗留**：视觉识别偶发单字符误差（如「攻城宝」→「玫城堡」），用户可在前端手动修正，非代码缺陷；识别耗时约 40s（视觉模型正常水平）
- **提交**：fix(ocr)（随本记录一并提交）

## 系统设置模型选择无法保存/显示修复（2026-08-07，已完成）

- **需求（用户反馈）**：OCR 与大模型 → 获取模型列表 → 选择模型 → 点保存设置，模型不能正常保存（DB 仍为空）和显示（重新打开显示空）
- **根因**：三个模型 Select 位于 `Form.Item > Space.Compact > Select` 结构中——**antd v6 的 Form.Item 只把 value/onChange 注入给直接子元素**（Space.Compact），而 Space.Compact 是布局组件，**不透传 value/onChange**（其余 props 被丢到 div 上）；Select 也不从 FormItemInputContext 取 value/onChange（该 context 仅含状态）→ **Select 变成非受控**：选中只改内部显示、不写表单 store；回显也失效（表单有值但 Select 显示 placeholder）→ save() 提交的是旧值/空值，DB 存不上
- **修复**：
  - Settings.tsx：三个模型 Select 显式受控——`Form.useWatch` 取值 + `onChange` 写 `form.setFieldValue`（保留 Space.Compact 布局）
  - Materials.tsx：同根因预防——「材料名称」「条码」两个 Input 同样在 Space.Compact 内（新建/编辑材料时输入不写表单），一并显式受控
- **验证**：L1 typecheck + L4 desktop build ✅（8.87s）；后端保存链路此前已实测（全字符串表单 200、GET 回显正常）；全项目排查 Space.Compact 使用点（Settings/Materials/PurchaseIn——PurchaseIn 明细行非 Form 环境，显式绑定，无隐患）
- **提交**：fix(settings)（随本记录一并提交）

## 材料入库「分类」字段 + 拍照识别视觉兜底豆包（2026-08-07，已完成）

- **需求（用户确认）**：①材料入库明细添加「分类」字段，根据材料名称+规格由大模型自动识别生成，结果可手动编辑并保存；②拍照识别物品（商品外包装/标签）视觉模型兜底豆包大模型
- **交付（分类字段）**：
  - 后端：`POST /ocr/classify`（pch:in）——DeepSeek 按名称+规格判断系统分类，命中返回 {category_id, category_name, matched}，未命中返回大模型建议名（前端提示手动选择），模型未配置/失败 4006；`_classify_items_by_deepseek` 分类列表**过滤带 6 位 hex 随机后缀的测试/乱码分类**（如「标准件f9d3ae」，原 286 个分类全量传给大模型会干扰判断）；`PurchaseInItemReq` 加 `category_id`——入库时校验分类存在并**同步更新材料分类**（base_product.category_id）
  - 前端（PurchaseIn.tsx）：明细新增「分类」列（单位列后）——可搜索 Select（系统分类树扁平化）+ ⚡按钮触发自动识别（识别中 loading、命中填入并提示、未命中提示建议名/手动选择）；材料选中/条码/OCR/预填各回填路径均带出材料已有分类；提交入库携带 category_id
- **交付（视觉兜底链）**：`_recognize_text` 本地 OCR 关闭时视觉兜底改为 **SiliconFlow → 豆包** 依次尝试（前一个未配置/未启用/调用失败自动尝试下一个），全部不可用提示「识别功能不可用」+ 最后失败原因；/ocr/quick 与 ocr_type=2/3 同步受益
- **验证**：pytest 23/23 ✅（新增 `test_purchase_in_with_category_updates_product`：带分类入库→材料分类更新、无效分类 4006 整单回滚——**顺带抓出 stock.py 漏导入 BaseCategory 的 NameError**）；前端 build ✅；运行后端实测：`/ocr/classify` 轴承6204→轴承类(38) 命中、网络测试仪→未命中（系统无电子设备分类，返回建议/提示手动选择）
- **提交**：feat(purchase-in)+feat(ocr)（随本记录一并提交）

## 客户端空响应体解析崩溃修复（2026-08-07，已完成）

- **需求（用户反馈）**：`Uncaught (in promise) SyntaxError: Failed to execute 'json' on 'Response': Unexpected end of JSON input`——client.ts `request()` 对空响应体调 `res.json()` 抛错；AiSuggestions.tsx:23（aiApi.list）/33/34（units/categories）的 `void load()` 与 `.then()` 无 catch → unhandled promise rejection
- **修复**：
  - `shared/api/client.ts`：`res.json()` 改为 `res.text()` + 手动 JSON.parse——空响应体（204/Content-Length 0/代理空响应）不再抛 SyntaxError：2xx 空体视为成功返回 undefined、非 2xx 空体抛可读 BizError(HTTP 状态)；非 JSON 内容（网关错误页）捕获解析异常转 BizError「响应解析失败（HTTP xxx）」，不再泄漏原生 SyntaxError；正常 JSON 行为不变
  - `AiSuggestions.tsx`：`load()` 加 try/catch（失败保持空列表）；units/categories 加载加 `.catch(() => undefined)`——彻底消除 unhandled rejection
- **验证**：两端 typecheck ✅ + desktop build ✅（8.93s）
- **提交**：fix(client)（随本记录一并提交）

## 视觉大模型训练本地 OCR 商品识别模板 + 豆包关闭降级链（2026-08-07，已完成）

- **需求（用户确认）**：①用视觉大模型识别 testdata/QQ20260807-023516.png（H3C 8口千兆以太网交换机 S2G Pro 包装图）并修复识别模块；②"通过视觉大模型训练本地 OCR 识别模板"；③豆包大模型已关闭时，材料名称识别仍可通过视觉模型（SiliconFlow）与 OCR 模板匹配完成
- **交付（商品结构化识别）**：`VISION_PRODUCT_PROMPT` + `_vision_product`——视觉模型（SiliconFlow→豆包链）识别商品包装/标签输出 {product_name, brand, spec, lines}；`_local_ocr`/`_vision_texts` 拆出本地 OCR 与纯文本兜底；`_recognize_text` 重构为「本地 OCR → 结构化商品识别 → 纯文本」；`/ocr/quick` 视觉路径用 商品名/品牌+规格/规格 候选匹配（比整行宣传文案命中率高）
- **交付（模板训练机制）**：`services/ocr/product_template.py`——模板 {id, name, brand, product_name, spec, anchors, created_at}，锚点自动提取自视觉识别结果（品牌/规格/名称），存 sys_config（ocr.product_templates，设置页不暴露）；`POST /ocr/template/train`（视觉大模型识别样本图→生成/覆盖模板）、`GET /ocr/templates`、`DELETE /ocr/templates/{id}`（ocr:manage）；`/ocr/quick` 本地 OCR 文本**模板优先匹配**（全部锚点命中→秒级结构化，不调大模型）
- **交付（豆包关闭降级）**：`/ocr/match` 重做——①识别记录文本**模板命中→直接生成建议**（model=ocr-template，标注"本地 OCR 模板匹配"）；②模板未命中→视觉链 **豆包→SiliconFlow**（豆包关闭/未配置自动用 SiliconFlow 分析，suggestion.model 记实际模型）；两者均不可用才报"大模型分析不可用"
- **验证（豆包 enabled=0 实测全链路）**：
  - H3C 图视觉识别：product_name=8口千兆以太网交换机、brand=H3C、spec=S2G Pro、8 行原文 ✓
  - 训练模板 → quick（本地 paddle 4.6s 识别 10 行 + 模板命中）→ 系统无此材料 matches 空（正常）✓
  - match（豆包关闭+模板命中）→ 返回"8口千兆以太网交换机/S2G Pro"，**不再报豆包错误** ✓
  - 删模板后 match（豆包关闭）→ SiliconFlow 兜底成功：H3C mini 8口千兆以太网交换机/S2G Pro/网络交换机 ✓
  - pytest test_ocr 8/8 ✅；模板已保留在开发库（H3C 交换机）
- **提交**：feat(ocr)（随本记录一并提交）

## 后端运行时日志功能（2026-08-07，已完成）

- **需求（用户确认）**：后端日志——级别 DEBUG/INFO/WARN/ERROR（默认 INFO）；支持配置/管理后台运行时调整；日志文件 `logs/` 按天轮转（`app-YYYY-MM-DD.log`）；覆盖关键操作
- **交付**：
  - `app/core/logging_config.py`（新）：`DailyFileHandler` 按天轮转（每天一个新文件，跨天自动切换）；`configure_logging`（控制台+文件，uvicorn 三 logger 同文件，httpx/paddle 等第三方库 DEBUG 噪音隔离）；`set_log_level` 运行时立即生效（root+uvicorn+handler 全量）
  - `config.py`：`LOG_LEVEL`（默认 INFO）/`LOG_DIR`（默认 backend/logs）环境变量
  - `main.py`：lifespan 初始化日志并以系统设置 `log.level` 覆盖；请求中间件记录 方法/路径/状态/耗时（≥400 WARN），异常 ERROR
  - `system.py`：设置项 `log.level`（保存即校验并生效，非法级别 4006）；设置修改 INFO 日志
  - 关键操作埋点：登录成功/失败（auth）、OCR 任务开始/完成/失败（ocr）、大模型调用成功 DEBUG/失败 ERROR（llm，含耗时）、备份成功/失败（admin）
  - 前端：Settings 基础设置新增「运行时日志级别」下拉（DEBUG/INFO/WARN/ERROR，保存即生效）
- **验证（实测）**：日志文件 `logs/app-2026-08-07.log` 生成 ✓（含登录成功/请求记录/uvicorn access/调度器）；运行时 PUT log.level=DEBUG → 立即出现 `app.llm` DEBUG 行（大模型调用成功 name=deepseek 耗时=1.5s）✓；PUT TRACE → 4006「无效日志级别」✓；恢复 INFO ✓；pytest 20/20 ✅；前端 build ✅
- **提交**：feat(logging)（随本记录一并提交）

## 下一步（当前进行中）
1. 上述待办需求全部完成并提交
2. 配置真实豆包/DeepSeek API Key 后实测大模型链路
3. P10 上线部署（Windows 生产 + HTTPS + 期初数据迁移）
4. ✅ **前端电脑端与手机端功能同步（已完成）**：电脑端「出库管理」已更名为「领用管理」（领用申请/领用申请单查询/领用审计三个子菜单），并新增「编辑 GPS 与地点」能力
5. ⏳ **重启后端服务**：水印预览 404 为后端进程未加载新代码所致，重启 uvicorn（--reload 或手动）后恢复；GPS 逆地理编码需外网访问 OpenStreetMap（内网不通时提示手动填写）

## P9-① OCR 识别文本纠错与归一化（2026-08-07，已完成）

- **需求**：本地 OCR/视觉识别文本常有错字（实测「端口自透应」「H3CMiNi」），导致模板/材料匹配失败；识别文本进入匹配链路前用 DeepSeek 纠错归一
- **交付**：`services/ocr/correction.py` `correct_texts(db, lines)`——DeepSeek 单次修正多行（错别字/全半角/多余空格/单位写法，**不增删行、行数不一致自动回退原样**保证对齐）；开关 `sys_config ai.correct_enabled`（默认 1，0 关闭）；保护：文本 <4 或 >4000 字符跳过；接入 `/ocr/quick` 本地 OCR 路径（纠错后入模板/材料匹配）与 `/ocr/match` 模板匹配前；视觉路径不接入（已较准，避免同步接口过慢）
- **验证**：pytest 12/12 ✅（新增 test_correction.py 4 用例：修正生效/行数不一致回退/未配置降级/短文本跳过；test_ocr 适配 mock correct_texts 避免单测依赖真实 DeepSeek）；真实链路冒烟：H3C 图本地 OCR 17.3s（paddle 4.6s + 纠错 + 模板匹配）——「H3CMiNi→H3C Mini」「端口自透应→端口自适应」修正生效，模板命中正常
- **遗留**：个别错字未修正（「即播即用」→即插即用、「HBCMALL」→H3CMALL 模型未改），属模型能力限制；纠错使 quick 增加约 10s，开关可关
- **提交**：feat(ocr)（随 P9 批次提交）

## P9-④ 库存预警通知智能生成（2026-08-07，已完成）

- **需求**：预警通知是模板文案（"XX 库存 X 低于下限 Y"），生成带上下文与建议的正文
- **交付**：`services/ai/alert_text.py` `generate_alert_text(db, product, qty, kind)`——规则聚合上下文（库存/上下限/近30天出库量 stk_stock_log 聚合/最近采购价）→ DeepSeek 生成 ≤80 字正文（含消耗趋势/建议补货量/优先级）；AI 未配置/失败/开关 `ai.alert_enabled=0` 降级规则模板；scheduler.py 预警扫描接入（每条预警一次调用，频率可控）
- **验证**：pytest 3/3 ✅（AI 生效/未配置降级/开关关闭）；真实 DeepSeek 冒烟：「紧急预警：盘点物料（PDPd5d49e）当前库存0.5，低于下限100。近30天无出库，建议补货99.5至下限，优先级紧急。」
- **提交**：feat(ai)（随 P9 批次提交）

## P9-P0 模板自动学习（2026-08-07，已完成）

- **需求**：模板需手动训练；改为「同一商品被识别命中 3 次自动生成/更新模板」
- **交付**：ocr.py `_maybe_learn_template(db, product_id, anchor, spec)`——`/ocr/quick` 匹配成功时记录命中行（product_id → 最近 3 次文本指纹，内存态），累计 3 次且文本稳定（3 次中 ≥2 次相同）→ 自动生成模板（anchors=命中行，auto=True，材料字段取产品资料）；同锚点仅替换 auto 模板，**手动训练模板不被覆盖（手动优先）**；生成后重置计数
- **验证**：pytest 10/10 ✅（新增 test_product_template_learn.py 2 用例：3 次命中生成 auto 模板、手动模板不被覆盖——首跑抓出计数去重 bug：同 key 只记一次永远到不了 3，修复为记录每次命中）；真实冒烟：网络测试仪 NF-918S 3 次命中 → auto 模板生成（anchors=['网络测试仪 NF-918S']）→ 清理恢复
- **提交**：feat(ocr)（随 P9 批次提交）

## P9-P1② 材料查重/合并建议（2026-08-07，已完成）

- **需求**：材料库存在大量重复/相似材料，人工难以发现；AI 给出疑似重复分组建议，人工确认后标记
- **交付**：`services/ai/dedupe.py` `dedupe_scan(db)`——①名称精确相同→必查分组（confidence=high）；②名称归一（去空格/全半角/标点）互相包含或前缀相同→候选对（≤60 对）交 DeepSeek 判断（same+reason）→ medium 分组；DeepSeek 不可用降级仅精确分组；接口 `POST /products/dedupe-scan`（base:product）与 `POST /products/{id}/mark-duplicate`（写 remark「【疑似重复】」前缀，不物理删除）；前端 Materials 页「查重」按钮 → Drawer 分组展示（高置信/AI 判断 Tag + 原因 + 材料信息 + 标记重复按钮）
- **验证**：pytest 17/17 ✅（新增 test_dedupe.py 3 用例：精确分组/LLM 相似判断链路/未配置降级——修 BaseProduct 无 unit_name 需联查 BaseUnit、候选池被测试库历史材料占满改宽松断言）；前端 build ✅；真实接口冒烟：返回 P4物料 等历史测试重复材料分组
- **提交**：feat(ai)+feat(ui)（随 P9 批次提交）

## P9-P1③ 供应商名称归一 + 人工合并（2026-08-07，已完成）

- **需求**：送货单确认时供应商只做精确同名匹配；别名/简写/错字导致重复建档
- **交付**：`services/ai/supplier_norm.py` `match_supplier_by_llm(db, name)`——精确匹配失败后取库中启用供应商（归一后互相包含/前缀相同，≤5 候选）交 DeepSeek 判断同一实体，命中复用已有供应商并响应 `supplier_matched_name`（前端提示「已关联已有供应商：xx」）；DeepSeek 不可用降级现状；`POST /suppliers/merge`（base:supplier）人工合并：关联材料/历史入库单转移至目标、源停用（不物理删除）
- **验证**：pytest 3/3 ✅（test_supplier_norm.py：LLM 别名命中/未配置降级/合并转移+停用）；真实 DeepSeek 冒烟：「海口耐沃」→「海口耐沃办公设备有限公司」、「海南工友商贸」→「海南工友商贸有限公司」✓；前端 build ✅
- **提交**：feat(ai)（随 P9 批次提交）

## P9 大模型调用日志（2026-08-07，已完成）

- **需求（用户确认）**：调用大模型的输出数据全部记录，方便后期调整学习
- **交付**：新表 `sys_llm_log`（四层同步：init.sql + 本地库 CREATE IF NOT EXISTS + models.LlmLog + 测试）；`llm.py` **统一入口记录**（所有 chat 调用必经 `_request`）——场景/模型/输入（图片仅记张数，省略 base64；文本截断 4000）/输出（截断 8000）/耗时/成败/错误，fire-and-forget 写库不影响业务；各调用点补 scene 标记（vision_delivery/vision_product/vision_text/match_vision/ocr_correct/classify_items/structured/alert_text/dedupe/supplier_norm/req_summary）；`GET /llm-logs`（sys:config）按场景/状态分页查询；前端「系统管理 → AI 调用日志」页（场景/状态筛选 + 输入/输出/耗时/错误展示）
- **验证**：pytest 2/2 ✅（test_llm_log.py 写入成功/失败记录）；真实冒烟：调 /ocr/classify → llm-logs 出现 classify_items/deepseek/ok/1790ms/输出内容 ✓；前端 build ✅
- **提交**：feat(ai)+feat(ui)（随 P9 批次提交）

## P9-P1⑤ 领用审核 AI 辅助摘要（2026-08-07，已完成）

- **需求**：仓管员逐单审核决策慢；审核时给出 AI 摘要与风险提示
- **交付**：`services/ai/req_summary.py` `ai_summary(db, req, items)`——规则聚合（申请人/材料×数量/当前库存是否不足/近30天领用频率/私用标记/金额按进价估算）→ DeepSeek 生成 {summary, risk_level 低中高, reasons}；未配置/失败返回规则版（ai=false 前端标注）；`GET /requisitions/{id}/ai-summary`（req:audit）；前端领用审计 Drawer 顶部「AI 审核摘要」卡片（风险 Tag + 摘要 + 红色原因列表 + 规则版标注）
- **验证**：pytest 9/9 ✅（test_requisition 回归）；前端 build ✅；真实 DeepSeek 冒烟：「申请人tester_user近30天领用523次…risk_level=高，reasons=[高频领用风险, 申请原因涉及测试用途需核实]」——修 OutRequisitionItem 无 price 字段改用材料进价估算
- **提交**：feat(ai)+feat(ui)（随 P9 批次提交）

## P9-P1⑦ 报表 AI 月报摘要（2026-08-07，已完成）

- **需求**：报表为数字表格，管理层需要文字解读
- **交付**：`services/ai/report_summary.py` `report_summary(db, start, end)`——服务端聚合（出入库件数与上周期环比（case when 兼容 MySQL5.7）、采购金额、TOP5 出入库材料、预警数、负库存数、呆滞数 >90 天无变动）→ DeepSeek 生成 200-300 字经营摘要（LLM 不接触原始明细）；未配置/失败降级规则版；`POST /reports/ai-summary`（report:view）；前端报表中心新增「AI 月报摘要」Tab（日期范围 + 生成 + 展示，标注 AI/规则版）
- **验证**：前端 build ✅；真实 DeepSeek 冒烟：「入库 53,173 件、出库 5,985 件、采购金额 63,300 元、预警 5,063 条、负库存 57 种……建议：1）核查负库存产生原因 2）P3 物料低频领用评估备货量」——修 MySQL5.7 不支持 FILTER 聚合改 case when
- **提交**：feat(ai)+feat(ui)（随 P9 批次提交）

## 系统初始化安装页（2026-08-07，已完成）

- **需求**：应用首次启动/尚未初始化时自动显示初始化安装页，引导完成必要配置并提交初始化；成功后自动登录进入主页面；已完成初始化的部署直接跳过该页进入主应用
- **交付（后端）**：`app/api/init.py`——`GET /init/status`（公开，返回 {initialized, site_name}，无 sys.initialized 记录视为未初始化）+ `POST /init`（公开，仅未初始化可执行防重入：写 site.name/site.contact_phone、重置或创建内置超管账号（admin_username 不同则改名，冲突 4006）、写 sys.initialized=1）；init.sql 种子加 `sys.initialized='0'`；main.py 注册
- **交付（前端）**：shared `api/init.ts`（initApi.status/submit）+ `utils/device.ts` 加 `otherEndInitUrl()`；desktop 新增 `/init` 初始化安装页（沿用登录页双栏品牌区风格 + 三步引导：系统信息 → 管理员账号 → 联系方式，提交后自动登录进 `/app`）；Landing/Login/RequireAuth 未初始化强制跳 `/init`（已初始化访问 `/init` 跳回登录/主页）；mobile Login/RequireAuth 未初始化整页跳电脑端初始化页
- **验证**：pytest 4/4 ✅（test_init.py：状态读取/执行初始化/防重入/参数校验+改名冲突，保存恢复开发库状态；全量 110 passed，4 个失败为既有问题与 init 无关：_FakeLLM 缺 scene 参数（未提交的 LLM 日志改动遗留）、test_stock_alerts 受共享库数据影响、test_delivery_ocr 长跑 MySQL 连接超时）；curl 联调 ✅（status → init 成功 → 防重入 4006 → 参数校验 4006 → login/me 全链路）；**浏览器实测发现并修复 antd v6 两个问题**：① 三步表单条件卸载字段丢值导致提交 `v.site_name.trim()` 崩溃 → 改为三步字段常驻挂载（display 切换）；② Alert `message` 废弃警告 → 改 `title`；两端 tsc --noEmit + vite build ✅
- **提交**：feat(init)+feat(ui)
- **遗留**：① 已有部署库无 sys.initialized 记录 → 首次访问进入初始化页，完成即写入 1（等价"转正"，无需 ALTER）；② 本地开发库当前 sys.initialized='0'（联调后保持未初始化），浏览器再完成一次初始化即转正；③ 初始化页完成时会重置内置超管密码为页面上设置的密码（含此前浏览器实测已改的密码）

## P9-P2⑧ 材料语义搜索（2026-08-07，已完成——按用户意见改为纯本地实现，不用大模型）

- **需求**：材料搜索靠 keyword 模糊匹配；口语化描述（"测网络的工具"）查不到
- **方案变更（用户确认）**：初版用 DeepSeek 改写关键词（一次无结果约 3-8s + 费用）；**用户指出 P2-⑧ 没必要使用 LLM** → 改为**本地子串扩展**：`_expand_keywords_local(keyword)` 去常见口语修饰词（的/了/我/你/要/买/找/查/种等）后取 2-4 字连续子串（优先长串、去重、≤25 个）做 OR 宽匹配；`GET /products?ai=1` 无结果时触发（重建 stmt 去掉原条件避免 AND 叠加）；响应附 ai_expanded + ai_keywords
- **交付**：base_data.py 本地扩展函数 + ai 参数；前端 Materials 页无结果自动重试 ai=1 + 「已扩展搜索词：xx」Tag；删除 services/ai/search_expand.py（LLM 版）
- **验证**：pytest 8/8 ✅（test_base_data 回归）；前端 build ✅；真实冒烟：「测网络的工具」→ 子串含「网络」「测试」→ **0.09s** 命中 7 条（网络测试仪/800万网络摄像头等），零大模型调用；修复扩展时 AND 叠加原条件导致无结果的 bug（重建 stmt）
- **提交**：feat(ai)（随 P9 批次提交）

## P9-P1②③⑤ 去 LLM 化改造（2026-08-07，已完成——按用户意见纯本地实现）

- **需求（用户确认）**：P1-②材料查重、P1-③供应商名称归一、P1-⑤领用审核摘要"没必要使用 LLM"——改为纯本地规则
- **P1-② 材料查重**：`dedupe.py` 删除 DeepSeek 判断，候选对（归一后互相包含/前缀前4字一致）直接生成 medium 分组（reason「名称相似（归一后互相包含或前缀一致）」），人工标记兜底；免费/确定/毫秒级
- **P1-③ 供应商名称归一**：`supplier_norm.py` 删除 DeepSeek 判断，本地规则——归一后互相包含（简称⊆全称）或前缀前 4 字一致且长度差≤4 → 命中复用已有供应商（响应 supplier_matched_name）；错字别名漏判时自动创建 + 人工 `/suppliers/merge` 兜底
- **P1-⑤ 领用审核摘要**：`req_summary.py` 删除 DeepSeek 润色，直接返回规则版 {summary, risk_level(缺货→高/私用或高频→中/否则低), reasons}；前端去掉「规则版」标签（恒规则版）
- **顺带修复**：test_ocr.py 三个 mock 类（_FakeDoubao/_FakeVision）补 scene 参数兼容——上一轮 LLM 日志改造（chat_text/chat_image 加 scene）后未跑 test_ocr，本轮回归暴露，已修复
- **验证**：pytest 相关模块 5/5 + test_ocr 8/8 ✅（全量回归 27+ 通过）；前端 build ✅；《AI赋能设计.md》三项方案同步更新（纯本地说明）
- **提交**：refactor(ai)（随本记录一并提交）

## Redis 缓存层集成（2026-08-07，已完成）

- **需求**：分析现有代码识别高频访问数据，集成 Redis 并落地缓存，降低数据库压力（会话/权限校验为每请求热路径，基础资料与看板聚合为页面级高频读取）
- **热点分析结论**：
  - T0 认证热路径（每请求必查库，全项目 65 处 `get_current_user`/`require_permission` 依赖）：`sys_session` 会话、`sys_user`、`sys_role`+`sys_role_permission` 权限校验
  - T1 页面字典（每进页面全量拉取、数据量小、变更少）：分类树/单位/供应商下拉/仓库/货架/库位/部门
  - T2 商品查询：`GET /products/{id}` 详情、扫码 `?barcode=` 精确查询
  - T3 聚合/轮询：`/stock/summary`、`/reports/dashboard` 看板聚合、`/stock/location-summary` 货架图、`/notifications/unread-count` 未读数
- **基础设施**：phpstudy Extensions 的 redis.zip 已损坏，从 gh-proxy 镜像下载 Windows 版 Redis 5.0.14.1（tporadowski）装至 `G:\phpstudy_pro\Extensions\redis\5.0.14.1`（redis.conf：maxmemory 256mb allkeys-lru + AOF），注册 Windows 服务 `RedisWLT`（开机自启）；`requirements.txt` 加 `redis>=5.0`；`.env`/`.env.example` 加 `REDIS_URL=redis://127.0.0.1:6379/0`
- **缓存层 `app/core/cache.py`（新）**：Redis 客户端惰性单例（protocol=2 兼容 Redis 5.x，socket 超时 0.5s）+ `cache_aside`/`cache_get`/`cache_set`/`cache_delete`/`cache_delete_pattern`（SCAN 防阻塞，支持多模式）；**优雅降级**——Redis 不可用时静默直查数据库、连接失败短路，业务无感；序列化用 `jsonable_encoder` 保证命中/未命中返回类型一致（Decimal/datetime 归一）；key 统一前缀 `wlt:`
- **Session 热路径（deps.py + auth.py）**：`wlt:session:{token}`→user_id，TTL=会话时长原生过期；命中后仅剩一次用户主键查询；未命中回源 MySQL 并回填（兼容接入前旧会话）；滑动续期走 Redis EXPIRE（不足一半时刷新，与 MySQL 逻辑对齐）；登录双写、登出/改密/重置/停用双删（`user_sessions:{uid}` 集合支持按用户批量失效）
- **权限缓存（deps.py + admin.py）**：`wlt:role:{id}` 缓存角色 code、`wlt:role_perms:{id}` 缓存权限点集合（5 分钟 TTL）；角色/权限/用户管理写操作即时失效
- **字典 cache-aside（base_data.py，10 分钟 TTL + 写时失效）**：`dict:categories/units/warehouses/departments`、`dict:suppliers:{status}:{size}`（仅下拉场景）、`dict:shelves:{wh_id}`（仅无单位过滤用户）、`dict:locations:{wh}:{shelf}`；货架/库位写操作同时失效货架图缓存 `stock:locsum:*`；商品 `product:{id}` 详情与 `product:bc:{barcode}:{status}` 条码查询（10 分钟 + 写时失效，导入自动创建单位/分类时连带失效字典）
- **聚合缓存（report.py/stock.py/notification.py/scheduler.py）**：`dash:stock_summary`、`dash:dashboard` 60s TTL，`stock:locsum:{wh}:{shelf}` 60s TTL，`notify:unread:{uid}` 30s TTL；`post_stock_change()`（库存变动唯一入口）统一失效 `dash:*` + `stock:locsum:*`；读通知/新预警生成时失效未读数
- **验证**：pytest 回归 **65 passed**（auth/base_data/stock/report/admin/requisition/settings/init 等）；端到端脚本验证——登录→会话入 Redis（TTL 28800s）→miss 回填→分类/单位/仓库/部门/库位/看板/权限/条码缓存命中→更新商品即时失效→登出删除会话，全部通过；降级路径实测（协议不匹配期间 18 用例照常通过）；`test_location_summary` 曾抓出「新建库位后货架图缓存未失效」真实一致性 bug，已修复（货架/库位写操作连带失效）
- **遗留**：`test_correction.py::test_correct_returns_fixed_lines` 失败为既有 OCR 测试夹具问题（`_FakeLLM.chat_text()` 缺 `scene` 参数），与本次改动无关
- **提交**：feat(cache)（Redis 缓存层，随本记录一并提交）

## 修复：/llm-logs 路由 404（2026-08-07，已完成）

- **问题（用户反馈）**：访问 `/llm-logs` 报 404（React Router 无匹配路由）
- **根因**：AI 调用日志功能接入时，批量 patch 脚本在 AppLayout 步骤 assert 中断——菜单项与 TITLES 已补，但 **main.tsx 的 AiLogs import 与 /llm-logs 路由从未写入**（当时仅补了 AppLayout，漏了 main.tsx），导致菜单可点、路由缺失
- **修复**：main.tsx 加 `import { AiLogsPage }` + `{ path: "/llm-logs", element: withLayout(<AiLogsPage />) }`（ai-suggestions 路由后）
- **验证**：desktop build ✅（10.09s）；dist 产物含 llm-logs 路由 ✓；vite dev 热更新自动生效
- **提交**：fix(ui)（随本记录一并提交）

## 重构：初始化检查改为文件系统标记文件（2026-08-07，已完成）

- **需求（用户确认）**：初始化完成判断不再依赖数据库（`sys.initialized` 配置），改用文件系统标记文件 `backend/data/.initialized` 是否存在——防止数据库重建/备份恢复/状态被改后**强制重新进入初始化流程**
- **交付（后端）**：`config.py` 加 `init_mark_file`（默认 `backend/data/.initialized`，环境变量 `INIT_MARK_FILE` 可覆盖）；`app/api/init.py` 重构——`is_initialized()` 仅检查标记文件存在（零数据库查询），`POST /init` 在业务事务 commit 成功后**原子写入标记文件**（临时文件 + os.replace，内容含完成时间；目录权限不足返回 5003 不谎报成功、可重试）；`init_status` 仅 site_name 读库（业务配置，非完成检查）；移除 `sys.initialized` 配置读写与 init.sql 种子行
- **测试**：test_init.py 重写为标记文件保存/恢复（原存在还原内容、否则删除）+ 新增 2 用例——① 标记文件存在时即使数据库无任何状态记录也拒绝初始化（防强制重入核心场景）；② 标记文件写入失败（monkeypatch 模拟只读）→ 5003 + 仍可重试；6/6 ✅
- **验证**：compileall ✅；pytest test_init 6/6 ✅；curl 联调（删标记 → status false → POST /init → 标记文件生成 → status true → 防重入 4006 → 恢复）✅；前端无需改动（/init/status 响应结构不变），Init.tsx 顺手修复实测发现的 validateFields 校验失败 unhandled rejection（next/onSubmit 捕获，错误由 Form.Item 就地展示）
- **提交**：refactor(init)
- **遗留**：① dev 环境已创建标记文件并清理 sys.initialized 行（现环境视为已完成，直接登录）；② 删除标记文件即可重新预览初始化页（保留业务数据，仅重置超管密码与站点信息）；③ 旧部署库中残留的 sys.initialized 配置行无作用可删除

## P9-P0④ 预警通知去 LLM 化（2026-08-07，已完成——按用户意见纯本地规则）

- **需求（用户确认）**：预警通知用 DeepSeek 生成"浪费算力"——数据全是结构化字段，规则拼接即可
- **改造**：`alert_text.py` 删除 DeepSeek 调用与 ai.alert_enabled 开关，`generate_alert_text` 直接规则拼接：材料名/编码、库存、上下限、近30天出库量（stk_stock_log 聚合）、最近采购价、「请及时处理」；scheduler 调用点不变；测试改为本地规则断言（含材料/库存/近30天出库），顺带补 test_correction 的 _FakeLLM scene 参数兼容
- **验证**：pytest 8/8 ✅（alert_text 2 + correction 4 + llm_log 2）；《AI赋能设计.md》P0-④ 方案与 README 同步更新
- **提交**：refactor(ai)（随本记录一并提交）

## 安装界面扩展：数据库连接 + Redis 配置（2026-08-07，已完成）

- **需求（用户确认）**：安装界面增加数据库连接设置（地址/端口/用户名/密码/库名）与 Redis 配置（地址/端口/密码/库编号），提交时保存/应用
- **交付（后端）**：`InitReq` 加 9 个配置字段（默认 127.0.0.1/3306、127.0.0.1/6379/db0）；`do_init` 提交时**自动验证连接**——数据库 pymysql 试连（5s 超时，失败 4006 阻止安装，错误原因透出）；Redis redis-py ping（**protocol=2 兼容 Redis 5.x 无 HELLO 命令**，失败不阻止，响应带 `redis_connected:false` + warning 提示降级，缓存层本就优雅降级直查）；验证通过后业务落库 → **配置写入 backend/.env**（dotenv set_key 写 DB_URL/REDIS_URL，密码 URL 编码（quote），.env 已 gitignore，重启后端后生效；写入失败 5003）→ 原子写标记文件；ENV_FILE 常量便于测试隔离
- **交付（前端）**：Init.tsx 三步改**五步**（系统信息 → 数据库配置 → Redis 配置 → 管理员账号 → 联系方式），字段常驻挂载（antd v6 display 切换防丢值）；shared `InitInput` 加配置字段、`InitResult{redis_connected, redis_warning}`；提交成功提示"配置已保存至 backend/.env，重启后端后生效"，Redis 失败走 warning 文案
- **测试**：test_init.py 9/9 ✅（新增：数据库连接失败 4006 阻止且不写 .env/标记文件、Redis 失败降级提示仍成功、.env URL 编码（密码含 @:/?# 空格）、标记文件防重入不变；连接函数与 ENV_FILE 全部 monkeypatch 隔离，不污染开发 .env）
- **验证**：compileall ✅；pytest 9/9 ✅；curl 联调（真实 dev DB 配置验证通过 → .env 写入 DB_URL/REDIS_URL → 标记文件生成 → 防重入 4006 → 恢复 .env/admin）✅；本机 Redis 5.x 实测：修复前 HELLO 报错 → protocol=2 后 ping 通过 ✅；两端 tsc + vite build ✅
- **提交**：feat(init)
- **遗留**：① 安装配置写入 backend/.env 后需重启后端生效（当前进程保持旧配置运行，Redis 缓存层可动态重连）；② 目标数据库需已存在（README 部署流程 mysql < init.sql 建库建表），安装页有提示；③ dev 环境保持已初始化状态（标记文件存在），预览安装页需删除 backend/data/.initialized

## 用户权限设置：权限点按新加功能更新（2026-08-07，已完成）

- **需求（用户）**：用户权限设置页的权限点需覆盖新加功能，可独立授权
- **现状核对**：后端 require_permission 与前端菜单 perm 全部 code 均已注册（此前 24 点）；新加的 P9 功能页此前挂在无关权限下——「AI 调用日志」挂 sys:config（系统设置）、「AI 建议处理」挂 ocr:manage（识别记录管理）
- **交付（新增 2 个权限点，四层同步）**：
  - `sys:llm-log`（id 25，「AI 调用日志」，sort 76，系统管理组）：后端 GET /llm-logs 由 sys:config 改为 sys:llm-log；菜单「AI 调用日志」perm 同步
  - `ai:suggestion`（id 26，「AI 建议处理」，sort 62，报表中心区）：后端 GET /ai-suggestions、POST ignore 由 ocr:manage 改为 ai:suggestion；确认新增（accept）保持 base:product（新建商品动作本身要求商品管理权限）；菜单 perm 同步
  - 角色映射：超管全量（已有库 INSERT IGNORE 1→25/26）；仓管员补 26（保持原可见性，仓管员原持有 ocr:manage）；管理者/使用者不变
  - 同步：init.sql 种子（含仓管员映射）+ 本地库 INSERT IGNORE + 后端 require_permission + 前端 AppLayout 菜单 perm + 文档（后端API设计 §10 / 工具使用记录 §8）
- **验证**：本地库确认 26 权限点、角色映射 1→26 / 3→16 ✅；compileall ✅；pytest 回归（test_admin 角色权限用例）✅；前端 tsc + vite build ✅
- **提交**：feat(perm)（随本批次提交）

## AI 建议处理：确认新增材料内联分类维护 + 界面优化（2026-08-07，已完成）

- **需求**：AI 建议处理 → 确认新增材料界面，材料分类支持编辑并允许添加新分类；页面文案与视觉优化
- **交付**：`deps.py` 新增 `require_any_permission(*codes)`（任一权限放行，超管自动通过）；分类 POST/PUT 权限放宽为 base:category **或** ai:suggestion（AI 建议处理员可内联维护分类，删除仍要求 base:category）；前端 AiSuggestions「确认新增材料」弹窗分类下拉底部加「新增分类/编辑分类」（新增后自动选中，编辑缺省当前选中分类，支持两级父级 + 排序）；页面改版：页头标题/说明/待确认胶囊、列表卡片化、确认弹窗信息横幅 + 标签式表单（名称 / 分类 / 单位 / 进价三栏栅格）、文案改为「由视觉识别后生成建议；人工确认后才新增材料（可在系统设置配置模型配置）」
- **验证**：pytest test_base_data 9/9 ✅（新增 test_category_write_with_ai_suggestion_perm：仅 ai:suggestion 角色可建/改分类、删除 403）；desktop tsc --noEmit ✅
- **提交**：feat(ai)+feat(ui)（随本记录一并提交）

## 报表导出样式对齐模板：库存金额收发存表（2026-08-07，已完成）

- **需求**：读取 `testdata/匹配导出表格/库存金额收发存（2026.06）.xlsx` 作为导出样式模板，修改报表中心导出表格样式与其格式/布局/样式保持一致；导出文件名末尾追加当前时间（精确到分钟），不使用括号
- **交付**：
  - `_inventory_rows` 补充金额口径：入库/出库金额 = 流水变动数量 × 该笔流水成本价；期初金额 = 结存金额 − 入库金额 + 出库金额（恒等）；单价 = 结存金额/结存数量；API 同步返回 `opening_amount/in_amount/out_amount/unit_price`
  - 进销存汇总导出重写为 `_export_inventory_xlsx`（1:1 复刻模板）：sheet 名「模板」；标题「YYYY年M月库存金额收发存表」合并 A1:P1（行高 47.45、宋体 14 加粗居中、全细边框）；21 列表头（行高 52.15，S/T 列红色加粗，其余 14pt 加粗居中换行、左/右/上细边框）；数据行（行高 25.15、宋体 11、垂直居中、全细边框、U 列单价 `0.00_ ` 格式）；列宽逐列对齐模板（B=12…U=10.5）；I~N 列隐藏；A4 纵向 + 页边距 0.7/0.75/0.3
  - 数据列：年月 / 仓库名称 / 物料分类编码（分类 ID）/ 物料分类名称 / 物料编码 / 物料名称 / 规格型号 / 计量单位 / 期初·入库·出库·结存数量与金额 / 领料员 / 备注（板块）/ 已使用数量 / 金额（元）/ 单价（Q~U 库存口径无数据留空，单价 = 结存金额/结存数量）
  - 文件名：`库存金额收发存2026.08_1124.xlsx`（无括号，末尾 HHMM 分钟，RFC 5987 filename* 中文编码）；库存报表/库存流水导出同步升级通用样式（标题行 + 加粗表头 + 宋体边框数据行）
- **验证**：pytest test_report 10/10 ✅（新增金额字段/导出布局样式/文件名断言）；真实导出与模板 XML 级对比 ✅（列宽、隐藏列、行高、合并 A1:P1、页面设置、字体、边框一致；差异仅为隐藏列省略 width=0 属性与多一个未使用的默认字体，行为相同）；前端 exportReportUrl 参数不变无需改动
- **提交**：feat(report)（随本记录一并提交）

## React 19 兼容修复：antd-mobile 弹层崩溃 + favicon 404（2026-08-07，已完成）

- **需求（控制台报错）**：手机端每次 Toast/Dialog 自动关闭报 `Uncaught TypeError: unmountComponentAtNode is not a function` + 警告 `[Compatible] antd-mobile v5 support React is 16 ~ 18`；两端控制台均出现 `Failed to load resource: 404`（/favicon.ico）
- **根因**：antd-mobile v5 命令式弹层（Toast/Dialog 等）内部默认走 `ReactDOM.render` / `unmountComponentAtNode`，React 19 已移除这两个 API；React 19 + antd-mobile v5 必须注册官方兼容渲染器（mobile.ant.design/guide/v5-for-19）
- **交付（前端）**：
  - mobile `main.tsx` 注册 `unstableSetRender`——命令式弹层改用 `createRoot` 渲染、卸载走 `root.unmount()`，容器复用 `_reactRoot`；Toast/Dialog/Popup 全部恢复正常，兼容警告消除
  - 两端 `index.html` 补 `<link rel="icon" type="image/svg+xml" href="%BASE_URL%favicon.svg" />`（Vite 构建按 base 前缀替换，生产 /m/ 下正常），新增 `public/favicon.svg`（品牌色 #1668dc 圆角方块 + 「物」字），消除浏览器自动请求 /favicon.ico 的 404
- **验证**：mobile tsc --noEmit ✅；mobile vite build ✅；dev 服务器 /favicon.svg 200 ✅；弹层行为为官方兼容方案 + 类型/构建验证，浏览器实测待用户确认
- **提交**：fix(mobile)（随本记录一并提交）

## 我的申请/通知加载性能优化 + 全部已读反馈修复（2026-08-07，已完成）

- **需求（用户反馈）**：①「我的申请」「通知」加载慢；②「全部已读」点击无反馈
- **排查（实测定位）**：
  - 接口耗时（curl 实测）：我的申请 2.06s、通知 1.01s、unread-count 2.01s、health 12ms → 健康检查快、带认证接口全慢 → 瓶颈在认证链路的 Redis 访问；数据库 SQL 直测（pymysql）：通知 count 1.4ms、分页 20 行 0.5ms → SQL 毫秒级，排除索引/数据量问题（通知表 10804 行）
  - **根因 1（主）**：Redis（127.0.0.1:6379）服务未运行；cache.py `_get_client` 短路失效——`Redis.from_url` 是**惰性连接**，ping 失败时 `_client` 已非 None，后续请求永远命中 `if _client is not None` 分支返回损坏客户端，每个请求的 session_get/cache_aside 都白等 0.5s 连接超时（unread-count 两次 Redis 操作 = 2s）
  - **根因 2**：`_req_out` N+1——每单逐行查询明细/仓库/申请人/审计人/产品/库位，一页 20 单 ≈ 200+ 次 ORM 查询（约 +1s）
  - 「全部已读」：接口链路完好（shared → PUT /notifications/read-all），实测 541→0 未读且缓存正确失效；前端缺 loading/成功提示导致"无反馈"
- **修复（后端）**：
  - cache.py：ping 失败时 `_client = None` 清空损坏客户端，短路真正生效——Redis 不可用时首次失败后**零开销直查 DB**，不再每个请求白等超时（Redis 可用性属运维项：phpstudy 需启动 Redis 服务）
  - requisition.py：`_req_out` 重写为 `_req_out_batch` 批量预取（一次查询取全部明细/仓库/用户/产品/库位 + 内存分组映射，20 单查询次数 200+ → 6 次；管理员判断整批仅一次）；`/requisitions/my`、`/requisitions` 列表切换批量版；`_req_out` 保留给详情接口（包装批量版）；删除无引用的 `_user_name`/`_loc_code`
- **修复（前端 mobile）**：
  - MyRequisitions：首屏 SpinLoading + 上拉 InfiniteScroll 分页加载（每页 20 条，hasMore 按已加载数 < total 判断，ref 防重复请求）
  - Notifications：列表加载 SpinLoading；「全部已读」加 marking 状态（进行中按钮变 SpinLoading、防重复点击）+ 成功 Toast「已全部标记为已读」+ 失败 Toast（原有）+ 本地列表 is_read 同步更新
- **验证**：pytest test_requisition+test_advanced 61/61、test_settings 5/5、test_auth 10/10 ✅；mobile tsc --noEmit + vite build ✅；重启后端后 curl 实测：我的申请 2.06s→17~34ms、通知 1.01s→15ms、unread-count 2.01s→12ms（约 60~100 倍提升）；read-all 实测 541→0 ✅
- **提交**：perf(api) + fix(mobile)
- **遗留**：① 本机 Redis 服务未启动（phpstudy 开启 6379 后缓存层——会话/未读数/角色权限才真正生效，首启失败日志为 warning 属预期降级）；② 排查时对 admin 执行了 read-all（541 条未读标已读，属该功能正常行为）

## 入库「新增材料」拍照/相册 + 全端相册支持修复（2026-08-07，已完成）

- **需求（用户反馈）**：入库模块「新增材料」页面输入字段缺少相机/相册按钮；并检查全部相机/相册代码——部分只能拍照不能选相册
- **根因**：所有 `<input type="file" accept="image/*">` 都带 `capture="environment"`——移动端浏览器（iOS Safari/Android Chrome）遇到 capture 属性会**强制直接打开相机**，用户无法从相册选图（PhotoUpload、入库扫码、拍照识别、桌面端新建材料/入库明细均受影响）；「新增材料」表单则完全没有拍照入口
- **修复（前端）**：
  - 新增 mobile 组件 `CameraAlbum.tsx`：拍照（`capture="environment"`）+ 相册（不带 capture）双按钮，选图后自动重置 input 值，支持连续选同一张图；触屏目标 44px
  - `PhotoUpload.tsx`：未拍时拍照/相册双按钮，已拍显示缩略图 + 删除/重拍（重拍弹 ActionSheet 拍照/相册二选一）；出库/领用/盘点照片留底全部可用相册
  - `Inbound.tsx`：新增材料弹窗「材料名称」加拍照/相册（OCR 快查识别名称）、「条码」加拍照/相册（条码解码）；明细行「扫码」改为 ActionSheet 拍照识别/从相册选择
  - `OcrScan.tsx`：新增「🖼 从相册选择」按钮
  - 桌面端 `Materials.tsx`（新建/编辑材料名称、条码）与 `PurchaseIn.tsx`（入库明细条码、名称列）相机按钮旁补相册按钮（PictureOutlined，input 不带 capture）
- **验证**：mobile/desktop `tsc --noEmit` ✅；两端 `vite build` ✅；后端无改动
- **提交**：fix(mobile) + fix(desktop)（随本记录一并提交）

## 拍照识别卡死修复：OCR 引擎看门狗 + 客户端请求超时（2026-08-07，已完成）

- **需求（用户反馈）**：拍照识别模块存在卡死——点识别后一直转圈无结果，无法恢复
- **根因（代码排查）**：`/ocr/quick` 为同步链路（RapidOCR 子进程 → DeepSeek 纠错 → 模板/商品匹配），全程无超时兜底：
  1. `rapidocr_api.py runDict()` 的 `stdout.readline()` **无限期阻塞**——引擎子进程卡死（死锁/杀软扫描/异常图片）时请求线程永久挂起，服务端永不返回
  2. 前端 `fetch` 无超时——服务端挂起时按钮永远停在「识别中…」，页面表现为卡死；loading 中也无防重复提交（连点会堆叠多个识别请求）
  3. 引擎启动失败/识别失败抛 `OCRInitError`（RuntimeError）未被 `_local_ocr` 捕获 → 500 而非可读错误
- **修复（后端）**：
  - `rapidocr_api.py`：识别读取加 30s 看门狗（threading.Timer），超时杀掉引擎进程使 readline 立即返回，返回错误码快速失败（不再永久挂起请求线程）
  - `client.py`：引擎启动失败归一为 `OCRInitError`（不再裸抛 500）
  - `api/ocr.py _local_ocr`：捕获 `OCRInitError`/`ValueError` 返回 None → 走视觉兜底或统一 5001 可读错误
- **修复（前端）**：
  - shared `client.ts`：`http.get/post/put/delete` 支持可选 `timeoutMs`（AbortController），超时抛可读错误「请求超时，请重试」
  - `business.ts`：`ocrApi.quick` 内置 60s 超时、`decodeBarcode` 内置 30s 超时（全部调用点统一受保护：拍照识别/入库扫码/新增材料/桌面端）
  - `OcrScan.tsx`：loading 中忽略再次选图（防并发识别堆叠）
- **验证**：backend `compileall` ✅；pytest test_ocr 8/8 ✅；前端 `tsc --noEmit` + 两端 `vite build` ✅
- **提交**：fix(ocr)（随本记录一并提交）

## 条码扫描模块落地 + 识别链路重排（条码→模板→视觉→文本分析→兜底）+ 搜索栏扫码按钮（2026-08-07，已完成）

- **需求（用户反馈）**：① 条码扫描模块未能正常加载；② 识别流程需按「先条码 → OCR 模板 → 视觉识别物品 → 提取文字分析 → 视觉兜底」顺序执行；③ 库存查询、领用申请添加、盘点当场新增账外物料的搜索栏都没有相机按钮，无法调用条码扫描模块
- **根因**：BarcodeScanner 在设计文档中列出但**源码从未实现**（shared/components 只有 FileImage），搜索栏只有关键词搜索；后端 `/ocr/quick` 链路也没有「条码优先」步骤（条码解码是独立接口，不参与快查链路）
- **修复（后端）**：
  - `barcode.py` 新增 `try_decode_barcode()`：组件缺失/解析失败/识别不到均静默返回 None（不抛错），供 quick 链路首步
  - `api/ocr.py` `/ocr/quick` 链路重排为五级：① 条码解码（zxing-cpp）→ 条码命中商品库直接返回（不走 OCR）；② 本地 OCR + DeepSeek 纠错 + 模板匹配；③ 模板未命中或本地 OCR 不可用 → 视觉模型结构化识别物品（SiliconFlow → 豆包兜底链）；④ 未识别出物品 → 视觉纯文本提取 + 文本匹配，仍无匹配 → DeepSeek 从文本行分析物品名称（`_guess_product_name_from_lines`）再匹配；⑤ 全部不可用 → 统一 5001。响应新增 `barcode` 字段（空串=未识别到条码）
  - 模板匹配改为「本地 OCR 可用但模板未命中」时也进入视觉识别级（原实现本地 OCR 成功就不走视觉）
- **修复（前端）**：
  - 新建 mobile `BarcodeScanner.tsx`（条码扫描模块）：全屏取景 + 原生 BarcodeDetector 实时检测（350ms 轮询、10 种格式、含手电筒）；**加载兜底**——摄像头不可用/权限拒绝/浏览器不支持时自动退化「拍照/相册」→ 上传 → 服务端 zxing-cpp 解码，任何环境可用；扫描成功只回调一次
  - `ProductPicker.tsx` 搜索栏加「📷 扫码」按钮（覆盖领用申请/入库/出库/盘点当场新增账外物料）：扫码命中直接选中材料，未命中条码填入搜索框
  - `StockQuery.tsx` 搜索栏加「📷 扫码」按钮：扫码后直接按条码查库存
  - `OcrScan.tsx`（扫码 Tab）加「📷 扫描条码」入口：命中复用匹配材料结果区，可直接带入入库/出库
  - shared `OcrQuickResult` 加 `barcode` 字段
- **验证**：backend `compileall` ✅；pytest test_ocr + test_barcode **17/17** ✅（新增 2 用例：条码命中直接返回、条码未命中继续 OCR）；前端 `tsc --noEmit` + 两端 `vite build` ✅
- **提交**：feat(ocr) + feat(mobile)（随本记录一并提交）
- **遗留**：BarcodeDetector 摄像头实时扫码需真机浏览器实测（Android Chrome / iOS Safari 17.4+）；非 HTTPS 环境浏览器会拒绝摄像头权限，自动走拍照/相册兜底属预期行为

## 条码识别异常排查与修复：iOS 仅支持 QR 提示 + 服务端解码增强 + 测试样本（2026-08-07，已完成）

- **需求（用户反馈）**：① 条码识别好像不能正常工作；② iOS 用户需提示「只支持二维码/QR 码，一维码要拍照识别」；③ 测试样本放入 `testdata/条码测试`
- **排查（实测定位）**：
  - 用 zxing-cpp 实测 `testdata/条码测试/` 内**用户实拍照片 IMG_3055.JPG（6956367338680）、IMG_3056.JPG（6934502304523）均可正常解码**，压缩链路（WebP q80 ≤1600px）验证通过 → 服务端解码可靠，问题在前端实时扫码
  - 前端根因：BarcodeDetector 带 formats 数组构造在部分浏览器抛错被整体判不可用；**iOS Safari 17.4+ 仅支持 qr_code 格式**（无 EAN/CODE128），原实现判定不可用后仍打开摄像头 → 出现"取景画面正常但永远扫不出"的假象
- **修复（前端 BarcodeScanner.tsx）**：
  - `createBarcodeDetector()` 改为不带 formats 构造（避免 NotSupportedError）+ `getSupportedFormats()` 探测能力，返回 `{detector, qrOnly}`：仅支持 QR（iOS）→ **仍启动摄像头实时扫二维码**，并常显提示「当前设备仅支持二维码（QR 码）扫码；一维条码（EAN/CODE128 等）请使用下方「拍照 / 相册」识别」；检测器完全不存在 → 不请求摄像头直接拍照/相册兜底（不再出现"正常取景但扫不出"）
  - 摄像头开启 8s 未扫到 → 顶部提示对准条码或使用拍照/相册
- **修复（后端 barcode.py）**：`try_decode_barcode` 原尺寸解不到时**放大 2 倍（LANCZOS）重试一次**（长边 ≤4000 限制），提升实拍小/模糊条码解码率
- **测试样本**：`testdata/条码测试/` 新增 6 张样本（EAN13 6901234567892、Code128 WLT-BEARING-6204、QR、无条码对照、小条码贴 1600 画布模拟实拍、高斯模糊模拟失焦），全部经 zxing-cpp 验证解码结果；README 测试样本章节补充说明
- **验证**：backend `compileall` ✅；pytest test_ocr + test_barcode **17/17** ✅；前端 `tsc --noEmit` + 两端 `vite build` ✅
- **提交**：fix(mobile) + fix(ocr)（随本记录一并提交）
- **遗留**：iOS 实时扫码仅支持 QR 属系统能力限制（WebKit 未实现一维码检测），已通过提示+拍照兜底解决；需真机确认提示展示与 QR 扫码可用

## 扫码流程简化：扫码 Tab 直接进全屏相机 + iOS 进入即提示仅支持 QR（2026-08-07，已完成）

- **需求（用户反馈）**：① 点「扫码」应直接跳转到相机/扫码界面进行拍照识别，移除中间的图片识别页面（三个按钮页），减少操作步骤、无确认弹窗/冗余页面；② iOS 进入则提示「仅 QR 扫码，条形码需要拍照」
- **交付（前端）**：
  - `OcrScan.tsx` 重写为**扫码页**：进入页面（扫码 Tab / 首页「扫码」入口）即直接打开全屏相机扫码（BarcodeScanner 挂载即 visible=true），**移除原「拍摄/相册/扫描条码」三个按钮的中间页与全部确认弹窗**；扫码/拍照识别到条码 → 自动 resolveByBarcode 查材料 → 命中展示结果卡片（名称/编码/规格/条码 + 入库/出库/重新扫码按钮），未命中显示提示 + 重新扫码按钮；返回（NavBar/关闭）回首页
  - `BarcodeScanner.tsx`：新增 `autoClose` prop（默认 true，扫码成功后自动关闭；扫码页传 false，等异步查材料完成后自行关闭，避免查询未完成就跳走）；**iOS（仅支持 QR）进入扫码界面即常显提示**「当前设备仅支持二维码（QR 码）扫码，条形码请拍照识别（下方『拍照 / 相册』）」（不依赖摄像头是否打开成功）；Android 长时间未识别提示与 iOS 提示互斥避免重叠
  - 首页工作台快捷入口「拍照识别 / OCR 快查」保持指向 /ocr/scan（现为扫码页），TabBar「扫码」Tab 不变
- **验证**：前端 `tsc --noEmit` + 两端 `vite build` ✅；后端无改动
- **提交**：refactor(mobile)（随本记录一并提交）

## iOS 实机扫码提示修复：不再显示「不支持」，统一按仅 QR 提示（2026-08-07，已完成）

- **需求（用户反馈）**：iOS 实机进入扫码仍提示「当前浏览器/设备不支持摄像头扫码」，而非「仅支持 QR」
- **根因**：iOS Safari <17.4 无 BarcodeDetector、17.4+ 仅支持 qr_code 且 `getSupportedFormats()` 可能返回空数组/构造抛错——原逻辑在探测失败时返回 null → 直接显示「不支持」提示；摄像头权限被拒时同样落入「不支持」分支
- **修复（BarcodeScanner.tsx）**：
  - 新增 `isIOS()`（UA + iPadOS 触屏点判断）：iOS 上检测器缺失/构造抛错/格式探测异常/返回空，一律返回 `{detector:null, qrOnly:true}`，**永不判为「不支持」**
  - 渲染互斥：`qrOnly`（iOS）→ 常显「当前设备仅支持二维码（QR 码）扫码，条形码请拍照识别（下方『拍照 / 相册』）」；「不支持摄像头扫码」提示仅在非 iOS 且摄像头不可用时显示；iOS 无检测器时不请求摄像头直接拍照/相册兜底
- **验证**：前端 `tsc --noEmit` + 两端 `vite build` ✅；后端无改动；iOS 真机行为待用户确认
- **提交**：fix(mobile)（随本记录一并提交）

## iOS 扫码细节修复：摄像头未开启时禁用手电筒，不再弹「摄像头未开启」（2026-08-07，已完成）

- **需求（用户反馈）**：iOS 实机提示「摄像头未开启」
- **根因**：iOS 无 BarcodeDetector 或权限被拒时未启动摄像头流（cameraOk=false），底部「🔦 手电筒」按钮仍可点击 → toggleTorch 找不到视频轨道弹 Toast「摄像头未开启」
- **修复**：手电筒按钮 `disabled={!cameraOk}`——摄像头未开启时置灰不可点，不再弹提示；拍照/相册按钮不受影响（不依赖摄像头权限）
- **验证**：前端 `tsc --noEmit` + 两端 `vite build` ✅；后端无改动；iOS 真机行为待用户确认
- **提交**：fix(mobile)（随本记录一并提交）

## 恢复最初摄像头权限检测方式：检测器探测不再阻断摄像头打开（2026-08-07，已完成）

- **需求（用户反馈）**：回到最开始权限检测方式——最开始摄像头能正常使用；此前改为「检测器不可用则不请求摄像头」导致 iOS 上摄像头打不开
- **修复（BarcodeScanner.tsx）**：
  - useEffect 恢复最初流程：**无论 BarcodeDetector 是否可用，都先 getUserMedia 打开摄像头**（权限弹窗/预览/手电筒正常）；检测器探测仅用于提示文案与是否启动检测循环
  - 无检测器时摄像头仍正常预览：iOS → 常显「仅支持二维码（QR 码）扫码，条形码请拍照识别」；非 iOS → 提示「当前浏览器不支持实时扫码，请使用下方『拍照 / 相册』识别条码」
  - 「不支持摄像头扫码」提示仅保留在非 iOS 且 getUserMedia 失败（权限被拒/非 HTTPS）时
- **验证**：前端 `tsc --noEmit` + 两端 `vite build` ✅；后端无改动；iOS 真机行为待用户确认
- **提交**：fix(mobile)（随本记录一并提交）

## 实时扫码改 zxing-js 纯 JS 解码：iOS 一维码/二维码均可实时扫码（2026-08-07，已完成）

- **需求（用户反馈）**：实时扫码没效果，二维码也不支持（iOS 实机）
- **根因**：原生 BarcodeDetector 在 WebKit 上不可靠——iOS<17.4 无此 API、17.4+ 仅支持 qr_code 且 detect(video) 实际工作异常，导致 iOS 上实时扫码（含 QR）无效
- **修复（BarcodeScanner.tsx）**：
  - **改用 zxing-js 纯 JS 解码**（新增依赖 `@zxing/library@^0.23`，动态 import 分包，仅扫码时加载）：`BrowserMultiFormatReader` + `decodeFromVideoElementContinuously`（canvas 拉帧 300ms/帧，TRY_HARDER），支持 EAN13/EAN8/CODE128/CODE39/CODE93/ITF/UPC-A/UPC-E/CODABAR/QR/DATA_MATRIX 共 11 种格式，**跨平台（iOS Safari 亦支持全格式实时扫码）**，不再依赖 BarcodeDetector
  - 移除 BarcodeDetector/isIOS/qrOnly 相关代码与「仅支持 QR」提示（zxing 全格式支持后提示不再需要）；保留：摄像头先 getUserMedia 打开（权限弹窗/预览/手电筒）、zxing 加载失败或摄像头被拒 → 拍照/相册 → 服务端解码兜底、8s 未识别提示、autoClose、手电筒 disabled
- **验证**：前端 `tsc --noEmit` + 两端 `vite build` ✅；后端无改动；iOS/Android 真机实时扫码待用户确认
- **提交**：feat(mobile)（随本记录一并提交）

## 实时扫码改手动帧循环：iOS 对准条码可识别（2026-08-07，已完成）

- **需求（用户反馈）**：无法正常扫码，提示「长时间未识别到条码」；追问对准条码后有无反应
- **根因**：zxing `decodeFromVideoElementContinuously` 持续模式在 iOS Safari 上不触发结果（WebKit 兼容问题）；且采集默认 1080p 全帧解码慢、小条码易漏检
- **修复（BarcodeScanner.tsx）**：改用**手动帧循环**——自建 canvas（长边≤720 缩放提速）→ `getImageData` → `RGBLuminanceSource` + `HybridBinarizer` + `BinaryBitmap` → `MultiFormatReader.decodeWithState`（300ms/帧，TRY_HARDER 全格式），完全自控解码流程，绕开 zxing 持续模式 iOS 兼容问题；getUserMedia 增加 `width/height: {ideal: 1280/720}` 降采集分辨率；stopCamera 清理帧循环定时器
- **对准条码后的预期反应**：识别成功（约 0.5s 内）→ 自动查材料 → 关闭扫码并反馈（库存查询直接出结果 / 添加材料直接选中 / 扫码页显示材料卡片可带入入库出库）；未识别到时每帧静默继续
- **验证**：前端 `tsc --noEmit` + 两端 `vite build` ✅；后端无改动；iOS 真机对准条码识别待用户确认
- **提交**：fix(mobile)（随本记录一并提交）

## 解码库替换为 zxing-cpp WASM（与服务端同源）：Node 实测全部样本解码成功（2026-08-07，已完成）

- **需求（用户反馈）**：手机无法实时解码，判断为页面解码库问题
- **根因（双因素）**：① `@zxing/library` 是 zxing Java 的 JS 移植，行为与 C++ 原版不同，iOS 上持续模式不触发结果；② zxing-wasm 默认 `locateFile` 指向 **jsDelivr CDN**——内网/离线部署时 WASM 加载失败直接导致扫码失效
- **修复（BarcodeScanner.tsx）**：
  - 卸载 `@zxing/library`，改用 **`zxing-wasm@^3.1`**（zxing-cpp C++ 编译的 WASM，**与服务端解码同源**，能力一致）；动态 import `zxing-wasm/reader`（wasm 约 1MB）
  - **WASM 本地化**：`zxing_reader.wasm` 复制到 `apps/mobile/public/`（随构建进 dist 根），`prepareZXingModule({ overrides: { locateFile: () => import.meta.env.BASE_URL + "zxing_reader.wasm" }, fireImmediately: true })` 预热，消除 CDN 依赖
  - 保持手动帧循环：canvas 拉帧（长边≤720）→ `readBarcodes(ImageData, { tryHarder, formats: 11 种 })`，300ms/帧
- **验证（Node 实测 zxing-wasm/reader）**：`testdata/条码测试/` 全部样本解码——EAN13 6901234567892 ✅、Code128 WLT-BEARING-6204 ✅、QR ✅、小条码 ✅、**用户实拍 IMG_3055→6956367338680 / IMG_3056→6934502304523 ✅**、无条码/模糊图正确未识别；前端 `tsc --noEmit` + 两端 `vite build` ✅（dist 含 zxing_reader.wasm）；后端无改动
- **提交**：feat(mobile)（随本记录一并提交）
- **遗留**：iOS 真机实时扫码（浏览器 canvas 帧路径）待用户确认；本地部署需同步 `dist/zxing_reader.wasm`（vite build 自动产出）

## 真机"实时扫码组件加载失败"修复：WASM 改 assets 静态资源导入（2026-08-07，已完成）

- **需求（用户反馈）**：真机提示「实时扫码组件加载失败」（startZxing 的 catch 被触发）
- **根因**：WASM 此前放 `public/` 直出到 dist 根目录（`/m/zxing_reader.wasm`）——SPA fallback 或 Nginx 静态规则未放行 dist 根新文件时，fetch 要么 404、要么被 try_files 转成 index.html（HTML 编译为 WASM 失败）→ prepareZXingModule 抛错
- **修复（BarcodeScanner.tsx + 资源布局）**：
  - `zxing_reader.wasm` 移入 `src/assets/`，用 vite `?url` 静态导入（新增 `vite-env.d.ts` 声明 `*.wasm?url`）→ 构建产出 `dist/assets/zxing_reader-<hash>.wasm`，**与所有 JS/CSS 同目录、自动带 /m/ base 前缀、带 hash 防缓存**——只要站点能加载 index.html 引用的 JS，wasm 必然可达，彻底消除部署路径变量
  - `prepareZXingModule({ overrides: { locateFile: () => zxingWasmUrl }, fireImmediately: true })` 预热
  - catch 增加 `console.error("zxing-wasm 加载失败:", e)`，后续定位可看控制台
- **验证**：mobile `vite build` ✅（dist/assets/zxing_reader-vui-RP1A.wasm 生成且被 chunk 引用）；两端 `tsc --noEmit` ✅；**desktop build 因本机 dev 服务器（5174/5175 正在运行）锁定 dist 旧文件报 EPERM，与本次改动无关（desktop 代码未动），关闭 dev 后即可构建**；后端无改动
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：iOS/Android 真机实时扫码待用户确认；desktop 构建需在本机 dev 服务器关闭后重跑

## 扫码解码库最终定案：zxing-wasm（Node 对比实测）+ 并行预加载 + 错误原因显示（2026-08-07，已完成）

- **需求（用户反馈）**：真机仍提示「实时扫码组件加载失败」；用户提供插件候选（vue-qrcode-reader 为 Vue 组件不适用、jsQR 仅支持二维码不支持商品一维码、zxing-wasm）
- **关键实测（Node 直接跑两个库解码 testdata/条码测试/）**：
  - `@zxing/library`（纯 JS）：**EAN13/Code128/QR/小条码全部未识别**——JS 移植解码能力弱，证实「解码库问题」
  - `zxing-wasm`（zxing-cpp C++ WASM，与服务端同源）：**全部解码成功**（含用户实拍 IMG_3055/3056）
  - → 结论：必须用 zxing-wasm；真机失败是**加载**问题而非解码能力
- **修复（BarcodeScanner.tsx）**：
  - 切回 `zxing-wasm/reader` + `src/assets/zxing_reader.wasm?url`（vite 静态资源，带 hash 与 base 前缀）
  - **并行预加载**：进入扫码即同时启动 WASM 加载（import + prepareZXingModule 预热）与 getUserMedia 权限请求，互不等待，失败窗口最小化
  - **错误原因显示在界面**：zxingFail 状态存具体错误信息（如 404/编译失败/网络），提示改为「实时扫码组件加载失败（原因）…」，配合 console.error 便于用户直接反馈根因
- **验证**：`tsc --noEmit` ✅；mobile `vite build` ✅（dist/assets/zxing_reader-vui-RP1A.wasm）；desktop 无代码改动（typecheck ✅，build 被本机 dev 服务器锁定 dist 文件 EPERM，关闭 dev 后可构建）
- **提交**：fix(mobile)（随本记录一并提交）
- **真机验证（2026-08-07）**：✅ **iPhone 真机实时扫码测试通过**（zxing-wasm 加载正常、实时解码可用）；本功能闭环，无遗留

## 扫码界面现代化改造：闪光灯改名 + 半透明文字框按钮（2026-08-07，已完成）

- **需求（用户）**：扫码界面现代化——「手电筒」改「打开闪光灯」；闪光灯/拍照/相册按钮改半透明文字框样式
- **实现（BarcodeScanner.tsx）**：
  - 文案：`🔦 手电筒/关闭` → `打开闪光灯/关闭闪光灯`（开启时高亮：背景 rgba(255,255,255,.30) + 白边 .65）；Toast「当前设备不支持手电筒」→「不支持闪光灯」
  - 样式：底部三按钮统一 `actionPillStyle` 半透明玻璃文字框——背景 rgba(0,0,0,.38)、1px 白边 .22、圆角 999、白字 14px、`backdrop-filter: blur(10px)` + `-webkit-` 前缀（iOS Safari）、minHeight 44px（触屏规范）、去 emoji 纯文字（规范：严肃产品不用 emoji 当图标）
  - 摄像头未开启时闪光灯按钮 opacity .45 置灰（disabled 不可点）
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅；dev 5175 模块转换 200 且新代码生效（HMR）
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无（真机视觉待用户确认，逻辑无变化）

## 添加材料弹窗新增「拍照/相册/扫码」三种添加方式（2026-08-07，已完成）

- **需求（用户）**：领用申请「添加材料」弹窗新增「拍照」「相册」「扫码」三个按钮；弹窗为公共组件，其他使用页面需同步支持
- **实现（ProductPicker.tsx，公共组件）**：
  - 搜索行下新增三按钮行：拍照（capture 直达后置相机）/ 相册（不带 capture 可正常选图）/ 扫码（复用 BarcodeScanner），等宽 outline 按钮、触屏 ≥44px；拍照识别中显示 loading、相册按钮置灰防重复
  - **拍照/相册识别链路**：上传（fileApi.upload）→ `ocrApi.quick(file_id, 2)` 服务端识别快查（链路①条码解码+商品匹配）→ 命中商品 `baseApi.product(id)` 补全单位等字段后直接选中关闭；有条码未命中 → 条码填入搜索框；无条码 → 首行 OCR 文本作为关键词搜索展示供选择；未识别到 → Toast 提示手动搜索
  - 抽公共 `finish(p)`：扫码命中/拍照命中/列表点击统一选中关闭逻辑
  - **覆盖范围**：ProductPicker 为公共弹层，领用申请（RequisitionNew）/入库（Inbound）/出库（Outbound）/盘点（CheckRun）四个页面自动同步支持
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅（exit 0）；dev 5175 ProductPicker 模块转换 200
- **提交**：feat(mobile)（随本记录一并提交）
- **遗留**：真机拍照/相册识别效果待用户实测

## 领用申请出库拍照留痕按钮改半透明样式（2026-08-07，已完成）

- **需求（用户）**：领用申请「出库拍照留痕」的「拍照」「相册」按钮改为半透明按钮
- **实现**：
  - `CameraAlbum` 新增 `translucent?` 属性：主色浅底 rgba(22,119,255,.08) + 半透明描边 rgba(22,119,255,.35) + 毛玻璃 blur(8px)（-webkit- 前缀兼容 iOS Safari），默认 false 不影响其他页面
  - `PhotoUpload` 透传 `translucent?`；RequisitionNew 两处（明细行照片 + 出库拍照留痕区块）传 translucent
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无

## 移动端入库「送货单识别」功能（2026-08-07，已完成）

- **需求（用户）**：移动端实现入库功能，支持送货单识别方式入库（或新增独立模块）
- **架构分析结论**：移动端已有完整手动入库页（Inbound.tsx：表头/明细/库位选择/校验/提交），**复用现有流程改造**——新增「送货单识别」入口，识别结果带入现有表单，走原提交逻辑；不新增独立模块（避免重复表单与提交逻辑，与桌面端 DeliveryOcr 同 API 链路）
- **实现（Inbound.tsx）**：
  - 表头新增「送货单识别」List.Item：拍照/相册（ActionSheet 二选一）→ `fileApi.upload(purchase_bill)` → `ocrApi.recognize(1, "llm")` 视觉识别异步任务 → 5s 轮询 `taskStatus`（2 分钟超时，与桌面端一致），识别中 DotLoading
  - 识别结果 Popup：供应商/单据号可编辑 + 明细预览（名称/规格/数量×单价）→「确认并带入入库」→ `ocrApi.deliveryConfirm`（供应商落库 + 物料自动匹配/新增）→ 逐项 `baseApi.product(id)` 补全后带入明细（跳过已存在项/查询失败项）+ 自动设置供应商 → Toast 汇总（自动创建供应商/关联供应商/自动新增物料数）
  - 取消/遮罩关闭清理轮询；卸载清理定时器
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅（exit 0）；dev 5175 Inbound 模块转换 200
- **提交**：feat(mobile)（随本记录一并提交）
- **遗留**：真机拍照送货单识别效果待用户实测（识别链路本身桌面端已生产验证）

## 入库明细行「扫码」按钮改调全屏扫码界面（2026-08-07，已完成）

- **需求（用户）**：入库材料明细的「扫码」按钮调用扫码界面（BarcodeScanner）
- **实现（Inbound.tsx）**：
  - 行「扫码」按钮不再弹 ActionSheet（原仅拍照/相册），改为直接打开 BarcodeScanner 全屏实时扫码弹层（zxing-wasm 摄像头扫码 + 界面内置拍照/相册兜底，覆盖原三种方式）
  - 移除已无用的 handleScanFile/openScanSheet/scanCamRef/scanAlbRef；列表头文案「拍照识别」改「扫码识别」
  - 命中逻辑不变：扫码结果 → resolveByBarcode → 命中带入明细行 / 未命中弹「新增材料」确认
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅（exit 0）；dev 5175 Inbound 模块转换 200
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无

## 扫码结果自动回填条码输入框（2026-08-07，已完成）

- **需求（用户）**：入库明细扫码完成后自动将扫描内容填入条码输入框，无需手动输入
- **实现（Inbound.tsx）**：`scanBarcode` 先 `updateRow(i, { barcode: b })` 回填条码框（可见可改）再走匹配逻辑（命中带入明细 / 未命中弹新增材料确认）；手动输入回车路径天然已在框中，行为不变
- **验证**：mobile `tsc --noEmit` ✅；dev 5175 Inbound 模块转换 200
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无

## 扫码成功不再弹「新增材料」提示（2026-08-07，已完成）

- **需求（用户）**：扫码成功后，入库界面不要弹出「新增材料」提示
- **实现（Inbound.tsx）**：`scanBarcode` 增加 `fromScan` 参数——扫码路径（BarcodeScanner onScan）未命中时仅 Toast「条码已填入，可手动添加」，不弹 Dialog；手动输入+回车路径仍弹「新增材料」确认（保留新增能力，扫码枪场景不打断）
- **验证**：mobile `tsc --noEmit` ✅；dev 5175 Inbound 模块转换 200
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无

## 添加材料弹窗（ProductPicker）加标题栏与展示优化（2026-08-07，已完成）

- **需求（用户）**：入库「添加材料」界面缺少标题栏，显示不够清晰直观，请加标题栏并调整展示
- **实现（ProductPicker.tsx，四个页面共用的公共弹层，同步生效）**：
  - 顶部新增 NavBar 标题栏：居中「添加材料」+ 右侧「关闭」按钮（点击关闭，原遮罩点击关闭保留）
  - 空态引导文案改为两步说明：「输入材料名称 / 编码 / 条码搜索，或使用下方「拍照 / 相册 / 扫码」直接添加」
  - 搜索结果列表上方显示「搜索结果（N 个），点击选中」，明确列表可点选
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅（exit 0）；dev 5175 ProductPicker 模块转换 200
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无（真机视觉待确认）

## 入库「材料明细」加标题栏与展示优化（2026-08-07，已完成）

- **需求（用户）**：澄清指**入库页面的材料明细区域**（非添加材料弹窗）——缺标题栏，显示不够清晰直观
- **实现（Inbound.tsx 材料明细区）**：
  - 新增标题栏卡片：左侧「材料明细」+ 项数（N 项），右侧提示「条码可扫码 / 手输后回车」
  - 明细行结构重组为三段式：①操作行（选库位 + 扫码 / 删除）②字段行（数量/进价，带 11px 字段标签，不再只靠 placeholder）③条码行（标签注明「扫码或手输后回车自动匹配」）+ 编码/规格/单位信息
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅（exit 0）；dev 5175 Inbound 模块转换 200
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无（真机视觉待确认）

## 修复入库明细数量输入框溢出重叠 + 「进价」改「价格」（2026-08-07，已完成）

- **需求（用户）**：数量输入框溢出并与价格字段重叠；标签「进价」改为「价格」
- **根因**：antd-mobile Input 为 flex 容器，默认 `min-width: auto` 时原生 input 有约 170px 内在宽度，flex 双列下不收缩导致溢出重叠
- **实现（Inbound.tsx）**：数量/价格/条码三个 Input 显式 `width: 100% + minWidth: 0 + boxSizing: border-box`（允许 flex 收缩、边框计入宽度）；「进价」标签改「价格」（注释同步）
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅（exit 0）；dev 5175 Inbound 模块转换 200
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无（真机布局待确认）

## 「扫码」改「识别」+ 拍照识别链路升级（条码优先，无条码大模型兜底）（2026-08-07，已完成）

- **需求（用户）**：文本「扫码」改「识别」；点击拍照后对图片识别——先检测二维码/条形码（有则按原扫码逻辑），无条码则用大模型识别图片物品兜底
- **实现**：
  - `BarcodeScanner`：`onScan` 增加可选 `product` 参数（识别链路命中商品）；拍照/相册兜底从 `decodeBarcode` 升级为 `ocrApi.quick(file_id, 2)` 全链路——①服务端条码解码命中商品库直接返回（秒级）；②无条码 → 视觉大模型识别物品/文本匹配兜底（复用后端既有链路，见 ocr.py /ocr/quick）；命中商品直接回调 product，仅条码未命中回传 code 走原流程
  - `Inbound`：明细行「扫码」按钮改「识别」、标题/条码标签文案同步；`scanBarcode` 支持 matched 商品直接补全带入（Toast「识别命中」）；原条码流程不变
  - `ProductPicker`：「扫码」按钮改「识别」，识别链路命中商品直接补全选中
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅（exit 0）；dev 5175 三个模块转换 200
- **提交**：feat(mobile)（随本记录一并提交）
- **遗留**：OcrScan/StockQuery 等仅消费 code 的调用方在大模型命中无条码商品时可能收到空 code（边缘场景，不影响主流程）

## 主界面导航栏「扫码」改「识别」（2026-08-07，已完成）

- **需求（用户）**：主界面导航栏（TabBar）「扫码」改为「识别」
- **实现**：TabLayout.tsx TabBar 第 2 项 title「扫码」→「识别」（路径 /ocr/scan 与图标不变）；main.tsx/TabLayout 注释同步
- **验证**：mobile `tsc --noEmit` ✅；dev 5175 TabLayout 模块转换 200（已含「识别」文案）
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无

## 「我的」修改密码界面重做（2026-08-07，已完成）

- **需求（用户）**：修改密码界面重做
- **实现（Mine.tsx）**：Dialog 改**全屏弹层界面**——NavBar 标题栏（返回+关闭）、当前账号提示条、表单卡片（原密码/新密码/确认新密码）、「显示/隐藏密码」统一切换（防输错）、新密码规则提示（至少 6 位）、确认框回车直接提交、底部固定「确认修改」主按钮（loading 防重复）、成功 Toast 后自动关闭并清空
- **验证**：mobile `tsc --noEmit` ✅；`vite build` ✅（exit 0）；dev 5175 Mine 模块转换 200
- **提交**：fix(mobile)（随本记录一并提交）
- **遗留**：无（真机视觉待确认）

## 前后端启动器（.bat）（2026-08-07，已完成）

- **需求（用户）**：生成前端和后端启动 bat 或启动器
- **实现**：仓库根目录新增 4 个双击启动器（`%~dp0` 相对定位，不写死路径；UTF-8 + `chcp 65001`，命令与 README 快速启动一致）：
  - `启动后端.bat`：校验 backend/.venv 与 certs/dev 证书 → uvicorn `--host 0.0.0.0 --port 8443` + SSL（certs/dev）
  - `启动桌面端.bat` / `启动手机端.bat`：校验 npm → node_modules 缺失自动 `npm install` → `npm run dev:desktop`（5174）/ `dev:mobile`（5175）
  - `一键启动全部.bat`：三个独立窗口分别拉起 后端/桌面端/手机端，打印访问地址
- **验证**：启动命令与 README 快速启动逐条一致；bat 为本机双击场景，实跑待用户确认（若控制台中文乱码属编码显示问题，不影响功能）
- **提交**：chore(scripts)（待 OCR 验证批一并或单独提交）
- **遗留**：无

## 送货单未知格式通用识别（回退解析 + 容错）+ 训练样本归档（2026-08-07，已完成）

- **需求（用户）**：送货单识别/入库识别在新格式或无模板送货单时识别错误多；要求先分析根因，再实现通用字段提取/回退解析/容错机制，且保持已知格式的模板识别兼容不变；用 `testdata/进货单/新格式进货单.JPG` 验证；另追加：送货单识别入库上传的图片按时间保存供后期训练
- **根因分析**：① 识别链单点依赖 SiliconFlow 视觉大模型（规则模板 template.py 已移出主流程，且只认 9~15 位物料编码锚点，新格式直接 None）；② 未利用 OCR 行坐标（Paddle/Rapid 均返回 box），表格列错位、合计行被当明细（新图 84460.00 合计误作第 5 条）；③ 无结果校验（表头词/无名称行/异常数值透传）
- **实现**：
  - **通用字段提取（新 services/ocr/generic_parser.py）**：`parse_delivery_generic(lines, boxes)`——全文正则提取供应商（供货单位/供应商+盖章）/单号；表头词定位列原型（货物名称/厂家品牌/规格型号/数量单价/金额/备注…）；数字列按 y 聚行 + qty×price≈amount 校验（「数量单价」合列按 x 分裂）；名称/品牌/规格碎片合并（同格断行，英文碎片补空格）；列斜线校正（表头线性回归斜率）抗照片倾斜，文本就近归行；合计行/大写金额/无名称行剔除；box 缺失退化为行文本启发式
  - **容错校验 sanitize_items**（所有来源通用）：剔表头/合计/无名称行、qty≤0 且价额齐全反推、异常值（qty>10⁶/price>10⁹）剔除、price/amount 缺失互推、金额不一致记 warning 保留原值
  - **/ocr/recognize 四级回退链（mode=auto 默认）**：① 规则模板（已知格式兼容不变，engine=template）→ ② 通用字段提取（engine=generic）→ ③ SiliconFlow 视觉结构化（提示词增强：合计/大写不得作明细；engine=siliconflow+deepseek）→ ④ DeepSeek 文本结构化（engine=deepseek）；全部失败返回 {lines} 人工录入；mode=template 仅本地 / llm 仅大模型；desktop DeliveryOcr / mobile Inbound 识别调用改传 auto
  - **确认链路容错**：`/ocr/delivery/confirm` 名称含表头/合计词跳过自动建料（product_id=0）；确认新建供应商/单位/分类后失效 dict:suppliers*/dict:units/dict:categories 缓存（此前 Redis 在线时列表读到旧缓存导致新供应商查不到）
  - **训练样本归档（新 services/ocr/sample_archive.py）**：/files/upload biz_type=purchase_bill 时把原始图片字节写入 `data/ocr_training/送货单/YYYY-MM-DD/<HHMMSS>_f{file_id}_{uuid8>.<ext>`（按上传日期分目录）+ 元信息 json；识别任务完成后补写结构化结果到同名 json（图+标注成对，供后期训练）；best-effort 失败静默
- **验证**：
  - test_generic_parser.py 4/4 ✅（真实 PaddleOCR 输出夹具：4 条明细/供应商/金额全对、碎片合并、合计行剔除、无坐标退化、sanitize）
  - test_ocr.py 新增模板优先（不调视觉/DeepSeek）+ 通用回退（engine=generic）链路 ✅；test_ocr/test_delivery_ocr 等用例通过（test_delivery_confirm_creates_supplier 根因：测试名「送货单供应商+uuid」与历史残留数据前缀碰撞，被本地供应商归一（前 4 字一致）误判复用旧 id → 测试改 uuid 前置 + keyword 搜索隔离；顺带修复 confirm 新建供应商/单位/分类后未失效 dict:* 字典缓存的隐患）
  - 真实图直跑：`新格式进货单.JPG` PaddleOCR 53 行 → 通用解析 4 条明细（容量型硬盘录像机 4×9700=38800 / 业无线WiFi 6 24×890=21360 / PTZ枪球一体机 12×675=8100 / 硬盘录像机 12×1350=16200）+ 供应商「海口耐沃办公设备有限公司」，合计 84460.00 未入明细 ✅；已知格式模板解析回归不变
  - test_quota.py::test_quota_refresh_interval_respected 隔离修复：未显式置空 quota.warning.threshold.deepseek/doubao，开发库真实 DeepSeek 阈值致其误入预警 providers（r2 断言失败）→ 补 _save_originals + 置空，测试结束恢复真实值
  - **首次验证运行（verify_ocr_improvements.sh）**：pytest **143 passed** ✅；前端 typecheck + build ✅；git 提交 **e81c382**（13 文件）✅；归档验证：两张测试图均落 `data/ocr_training/送货单/<日期>/`（原图 + sidecar json 含 structured）✅
  - **e2e 首跑暴露缺陷并已修复**：PaddleOCR 的 box 是 **numpy 数组**，`_box_bounds` 只认 list/tuple → `float(数组)` 抛错 → 新格式图整体退化文本启发式（仅 1 条错误明细，warning=未获取文字坐标）。修复：`_box_bounds` 兼容 numpy（`hasattr __getitem__`）+ 个别行无坐标跳过不整体退化；test_generic_parser.py 新增 `test_numpy_boxes` 回归用例
  - **复验运行（第二次）**：pytest **144 passed** ✅（含新增 test_numpy_boxes）；e2e 新格式图 **engine=generic 4 条明细全部正确**（容量型硬盘录像机 4×9700=38800 / 业无线WiFi 6 24×890=21360 / PTZ枪球一体机 12×675=8100 / 硬盘录像机 12×1350=16200，供应商/规格正确，无 warning 无退化）✅；已知格式模板回归不变（4 条明细 + 供应商 + 单号）✅；前端 typecheck + build ✅；提交 fix(ocr) f6c6b38 + chore(scripts) 50773f2 ✅
- **改动文件**：backend app/api/ocr.py、app/api/files.py、app/services/ocr/generic_parser.py（新）、app/services/ocr/sample_archive.py（新）、tests/test_generic_parser.py（新）、tests/test_ocr.py；frontend desktop DeliveryOcr.tsx、mobile Inbound.tsx；AI开发文档 AI赋能设计.md、后端API设计.md
- **提交**：feat(ocr) e81c382 → fix(ocr) f6c6b38 → chore(scripts) 50773f2（验证闭环 ✅）


## health 接口增加 Redis 状态（2026-08-07，已完成）

- **需求（用户）**：`https://localhost:8443/api/v1/health` 添加 Redis 状态
- **实现**：`backend/app/api/system.py` 的 `GET /health` 响应新增 `redis` 字段——调用 `app/core/cache.py` 的 `ping()`（走规范统一缓存工具，禁裸连 redis）；Redis 可用返回 `"ok"`，不可用返回 `"down"`，且按《开发规范.md》§4-10 静默降级不向请求路径抛异常（health 是运维探活接口，Redis 挂了只标记不阻断）
- **验证**：`python -m compileall` ✅；pytest `test_auth.py::test_health` 新增 `redis in (ok, down)` 断言 ✅；本机实测 Redis 在线 → `redis: "ok"` ✅
- **提交**：feat(system)（随本记录一并提交）
- **遗留**：无


## health 接口增加 LLM 服务商状态（2026-08-07，已完成）

- **需求（用户）**：`GET /health` 添加 LLM 状态（沿用 redis 状态的思路，探活接口返回 LLM 服务商状态）
- **实现**：`backend/app/api/system.py` 的 `GET /health` 响应新增 `llm` 字段——一次查询全部 `llm.%` 配置键，返回 doubao/deepseek/siliconflow 三家的 `{enabled: 开关=="1", configured: 是否已配 Key, model: 模型名}`；**只读配置不做在线探测**（探活不消耗配额、不因供应商网络问题拖慢 health）
- **验证**：`python -m compileall` ✅；pytest `test_auth.py::test_health` 新增 llm 结构断言（三家齐全、字段齐全）✅；本机实测 8443 返回三家状态 ✅
- **提交**：feat(system)（随本记录一并提交）
- **遗留**：无

## 开源发布准备：数据脱敏 + 发布副本（2026-08-07，已完成）

- **需求（用户）**：为开源发布做准备——对项目数据脱敏（数据库连接凭据、API Key、个人信息等），脱敏完成后将处理好的前后端代码移动到新文件夹（保留原有目录结构），移动前妥善验证脱敏结果
- **敏感信息盘点**（源仓库）：
  - 数据库凭据（真实本地密码）：`backend/.env`、`backend/.env.example`、`backend/app/config.py` 默认 DB_URL、README.md「本地开发环境」节
  - 私有数据目录（不入库、不发布）：`backend/.venv`、`backend/ocr/`（引擎二进制+模型）、`backend/certs/`（自签名证书私钥）、`backend/data/`（数据库备份 sql.gz、上传单据图片、OCR 训练样本含真实供应商名）、`backend/logs/`、`backend/tests/_tmp/`、前端 `node_modules/`、`dist/`、`build2.log`、`backend/.env`、`.pytest_cache`、`__pycache__`
  - 已确认无：API Key 明文（sk- 模式 0 命中）、身份证号、真实手机号（仅测试号段 13800000000 等）、TLS 私钥（证书目录整体排除）
- **实现**：
  - 新建发布目录 `开源发布/`，用 tar 按排除清单复制 `backend/` + `frontend/`（保留原目录结构），另复制脱敏后的 `README.md`、`.gitignore`；补回被 `--exclude=ocr` 误伤的 `backend/app/services/ocr/`（源码）——排除规则只应排除引擎资产目录 `backend/ocr/`
  - 脱敏替换（仅发布副本，源仓库不动）：数据库密码 → `CHANGE_ME` 占位符，涉及 `backend/.env.example`、`backend/app/config.py`（默认 DB_URL）、`README.md`；`backend/scripts/verify_ocr_improvements.sh` 注释中的绝对路径 `G:\phpstudy_pro\进销存` → 相对描述
- **验证**（移动/交付前）：
  - 敏感扫描：数据库密码、`sk-` API Key、`BEGIN PRIVATE KEY`、`phpstudy_pro` 绝对路径、真实手机号/邮箱/身份证 —— 发布副本全部 0 命中 ✅
  - 文件一致性：副本 vs 源（排除清单外）diff 为空，无遗漏无多余 ✅
  - L1 语法：`python -m compileall`（app/scripts/tests/conftest）零错误 ✅；`from app.config import settings` 导入 OK，DB_URL 显示占位符 ✅
  - 前端源码未改动（diff 一致），无 node_modules 无需重新构建
- **交付物**：`G:\phpstudy_pro\进销存\开源发布\`（backend 89 个 py + frontend 全部源码 + README.md + .gitignore，共 182 个文件）
- **提交**：不涉及（发布副本不入库；README 等脱敏仅发生在副本，源仓库无代码改动）
- **遗留**：① `开源发布/` 目录建议发布前整体压缩并核对 git 历史中是否曾提交过数据库密码（若有需按 git-filter-repo 处理）；② 源仓库 `.gitignore` 建议补充 `开源发布/` 避免误提交

## 开源发布准备：AI开发文档副本脱敏（2026-08-07，已完成）

- **需求（用户）**：将《AI开发文档》复制到 `开源发布/` 目录，仅对副本脱敏（原始文件不动），覆盖 API 密钥/访问令牌/密码/内部 IP/域名/员工姓名/客户信息/内部代号/未公开架构细节等，完成后确认无残留并报告
- **实现**：
  - 复制 10 个 .md 到 `开源发布/AI开发文档/`（原始目录不动）
  - 对副本执行 34 处脱敏替换（Python 脚本，长串优先避免半替换）：内网 IP `<内网IP>`→`192.168.x.x`；本地绝对路径（phpstudy_pro 等 11 种变体）→`<项目根目录>`/`<本地目录>` 占位符；数据库密码→`REDACTED`；真实供应商/客户名（海口耐沃办公设备、海南工友商贸、海南矿业）→`示例供应商甲/乙有限公司`、`示例客户`（保留归一示例逻辑）；客户采购单号 POAB2025xxxxxx→`POAB********`
  - 发布准备记录章节中的扫描模式名（sk-、BEGIN PRIVATE KEY）改为通用表述
- **验证**：副本内 0 残留（密码/内网 IP/路径/公司名/单号/API Key 前缀/私钥头全清零，仅剩脱敏占位符与测试号段 13800000000）；内网 IP 正则扫描 0 命中；diff 确认源 vs 副本差异全部为预期脱敏替换，源文档未改动 ✅
- **交付物**：`开源发布/AI开发文档/`（10 个 .md，脱敏后）
- **提交**：不涉及（副本不入库）
- **遗留**：源文档仍含真实凭据/内网 IP（内部使用），发布前勿将源目录打包；git 历史早期提交含密码，开源前需清理历史

## 开源发布准备：同步最新代码到发布副本（2026-08-07，已完成）

- **需求（用户）**：将源项目最新代码同步到 `开源发布/` 副本
- **实现**：diff 源 vs 副本定位 7 个变更文件 + 1 个新增文件，同步到副本；config.py 合并新增 `session_remember_hours`（保留 DB_URL 脱敏占位符）；README 同步（技术栈行、删除「本项目代码由AI开发」段），保留脱敏行；新增 `frontend/apps/mobile/src/styles/widescreen.css`
  - 同步内容：登录「记住登录状态」（remember）——backend app/api/auth.py、app/schemas/auth.py、tests/test_auth.py、app/config.py；frontend mobile Login.tsx/main.tsx（宽屏适配）、shared api/auth.ts、stores/auth.ts
- **验证**：compileall 零错误 ✅；敏感扫描（密码/API Key/私钥/本地路径）0 命中 ✅；源 vs 副本剩余差异仅为 3 个脱敏文件（.env.example/config.py/verify_ocr_improvements.sh）+ README 脱敏行，无遗漏无多余 ✅；AI开发文档副本（10 个 .md）不受影响 ✅
- **提交**：不涉及（副本不入库）
- **遗留**：无新增

## 版本管理引入（SemVer 2.0，2026-08-07，已完成）

- **需求（用户）**：为项目添加版本管理，避免版本不一致问题——定义清晰版本控制策略、为所有相关文件更新版本号、确保全代码库版本一致性
- **策略**（写入《开发规范.md》§9）：
  - 语义化版本 `MAJOR.MINOR.PATCH`，git tag 加 `v` 前缀；MAJOR=破坏性变更、MINOR=新功能/阶段收尾、PATCH=修复/文档；开发期 0.x，首个正式发布 1.0.0
  - **单一事实源**：`backend/app/__init__.py` 的 `__version__`；前端 4 个 package.json（根/desktop/mobile/shared）只同步不自行定版
  - 同步位置：事实源 + 4 个 package.json + `GET /api/v1/health` 的 `version` 字段（自动读取）+ git tag
  - 一致性校验（提交前必跑）：4 个 package.json 版本 `sort -u` 必须单行且等于事实源；`test_health` 断言 `health.version == __version__`
  - 旧约定「每个 P 阶段打 v0.1.0-p0 / v0.2.0-p1」废止，改为 §9 新规则
- **实现**：
  - `backend/app/__init__.py`：定义 `__version__ = "0.1.0"`（含同步位置注释）
  - `backend/app/api/system.py`：`GET /health` 返回新增 `version` 字段（`from app import __version__`）
  - `backend/tests/test_auth.py::test_health`：新增断言 `version == __version__`
  - 前端 4 个 package.json 已是 0.1.0，与事实源一致，无需改动
- **验证**：`python -m compileall` 零错误 ✅；`pytest test_auth.py::test_health` 通过 ✅；4 个 package.json `sort -u` 唯一且 = 0.1.0 ✅
- **提交**：feat(version)（随本记录一并提交）→ 打 tag **v0.1.0**
- **遗留**：无

## 开源发布准备：生成发布描述 RELEASE.md（2026-08-07，已完成）

- **需求（用户）**：生成发布描述
- **实现**：新建 `开源发布/RELEASE.md`（v0.1.0 发布说明）：项目简介、核心亮点（AI 拍照录入/统一库存事务/领用三步合一/大模型无绑定/双端同构/安全运维）、技术栈表、快速开始（含默认账号）、目录结构、质量保障、AI 生成声明与脱敏说明、许可证占位（待补充）
- **验证**：发布描述内敏感信息扫描 0 命中（无凭据/内网 IP/客户信息）✅
- **提交**：不涉及（副本不入库）
- **遗留**：许可证未指定，发布前需选择并补充 LICENSE 文件

## 开源发布准备：GitHub 仓库英文描述（2026-08-07，已完成）

- **需求（用户）**：提供 GitHub 仓库 description 的英文文本
- **交付**：
  - 完整版：Internal warehouse & inventory management system for enterprises. Full inbound/outbound/requisition workflow with desktop + mobile clients, photo-based OCR and LLM-assisted entry (delivery-note recognition, auto-learning product templates), transactional stock control to prevent over-issue. Built with React 19, FastAPI, MySQL & Redis; LLM provider-agnostic via OpenAI-compatible API.
  - 短版：Enterprise inventory management system covering inbound, outbound and requisition workflows with desktop & mobile clients. Photo + OCR + LLM-assisted entry, transactional stock changes, and provider-agnostic AI via OpenAI-compatible APIs. Stack: React 19, FastAPI, MySQL, Redis.
- **提交**：不涉及（GitHub 仓库设置项，无代码变更）

## 初始化安装修复：自动建库导入表结构 + 数据库密码必填（2026-08-08，已完成）

- **需求（用户）**：① 安装提交报 `数据库连接失败：(1049, "Unknown database 'wuliaotong1'")`——目标库需手工预建，库名填错即装不上且报错是原始英文；② 安装页「数据库密码」非必选；③ 安装程序应支持自定义数据库名，安装时自动导入数据库 SQL
- **设计变更**（《后端API设计.md》§1.1、《前端设计.md》§2.2 已同步）：
  - 数据库连接验证三态：**目标库不存在（1049）→ 自动建库 utf8mb4 + 导入 `backend/sql/init.sql`**；**目标库存在但为空 → 自动导入 init.sql**；**已有表的库 → 仅验证连接**（不动表结构，保护已有部署数据）
  - 常见 MySQL 错误码映射中文提示：1045 账号/密码错误、1044 无权限、2003/2002 无法连接服务器
  - db_name 限 `^[A-Za-z0-9_]+$`（防标识符注入 + 建库时反引号转义双保险）；db_password 安装页必填（接口层保留空串，兼容无密码本地 MySQL）
- **实现**：
  - `backend/app/api/init.py`：`_test_db_conn` 改造为三态验证；新增 `_ensure_db_schema`（建库+逐条执行 init.sql）、`_db_has_tables`（information_schema 判空）、`_split_sql`（分句，跳过注释/空行）、`_friendly_db_error`/`_errno_of`（错误码映射）
  - `backend/app/schemas/init.py`：db_name 增加 pattern 校验
  - `frontend/apps/desktop/src/pages/Init.tsx`：数据库密码加 required 规则；db_name 加字母/数字/下划线校验；数据库步骤 Alert 文案改为自动建库说明
  - `backend/tests/test_init.py`：新增 4 个测试——真实集成「不存在库自动建库导入」「已存在空库自动导入」（临时库，测试后 DROP 清理）、「建库失败阻止安装且不写 .env/标记文件」、错误码映射与 SQL 分句单测；校验用例补充非法 db_name
- **验证**：`python -m compileall` 零错误 ✅；`pytest tests` 151/151 通过（含 14 个 init 用例）✅；`tsc --noEmit -p apps/desktop` 零错误 ✅；`npm run build -w wlt-desktop` 成功 ✅
- **提交**：fix(init)（安装页自动建库/导入表结构、数据库密码必填）
- **遗留**：`开源发布/` 代码副本未同步（按发布流程处理）；版本未升级（0.1.0 保持，非里程碑收尾）

## 初始化安装修复（二）：目标库写入 + 连接热切换 + 启动容错（2026-08-08，已完成）

- **需求（用户）**：① 用 `wuliaotong1` 安装成功后，后端实际仍在使用 `wuliaotong`；② 后端在数据库不可用时也应能正常启动并进入安装/设置流程，只有安装流程完成后才因数据库不可用而报错
- **根因**（数据实证）：`do_init` 的业务写入（管理员账号/站点配置）使用**启动时旧引擎**连接的库——安装表单填 `wuliaotong1` 时账号/配置被写进了旧库 `wuliaotong`，`.env` 却指向 `wuliaotong1`；运行中进程不切换连接，重启前一直用旧库；另外后端启动自检 `engine.connect()` 失败直接抛异常导致无法启动
- **设计变更**（《后端API设计.md》§1.1/§9 已同步）：
  - 初始化业务写入**必须落在安装表单填写的目标库**（独立会话工厂，不用启动时旧引擎）
  - 安装成功后**热切换进程内数据库连接**（`app/db.py::reconfigure_db`：先建新引擎验证 SELECT 1 再替换全局 engine/SessionLocal，旧池 dispose），并同步 `settings.db_url/redis_url`（Redis 客户端惰性建连，同步后自动生效）——**无需重启后端**；切换失败返回 5003 提示重启（.env 已保存，重启可生效）
  - **启动容错**：lifespan 数据库自检失败仅告警不阻止启动；`GET /init/status` 数据库不可用时返回 `site_name: ""` 不报错（initialized 仅由标记文件判断）；`GET /health` 数据库不可用时返回 `db: "down"` 不报错（安装完成前 db down 属正常态，安装完成后即视为故障）
- **实现**：
  - `backend/app/api/init.py`：新增 `_build_db_url`/`_build_redis_url`/`_target_sessionmaker`/`_apply_runtime_config`；`do_init` 改用目标库会话执行业务写入，成功后热切换
  - `backend/app/db.py`：新增 `reconfigure_db(db_url)`（热切换 + 旧池释放）
  - `backend/app/main.py`：启动自检 try/except 仅告警
  - `backend/app/api/init.py` `init_status`、`app/api/system.py` `health`：数据库不可用容错
  - 测试：`test_init.py` 新增「数据库不可用 /init/status 仍 200」「数据库不可用后端仍能启动」「热切换失败 5003 不写标记文件」；真实集成测试断言安装后 `engine.url.database == 目标库`（finally 切回原库）；`test_auth.py` 新增「数据库不可用 /health 仍 200（db: down）」
- **验证**：`python -m compileall` 零错误 ✅；`pytest tests` **155/155 通过** ✅；真实场景（DB_URL 指向不可达端口启动 uvicorn）：进程正常启动监听、`/health` 返回 `db:"down"`、`/init/status` 返回 200 ✅
- **提交**：fix(init)（本次随代码一并提交）
- **遗留**：`开源发布/` 代码副本未同步（按发布流程处理）；测试环境曾因旧代码污染 `wuliaotong` 库 admin 密码，已重置为种子值 admin123

## 系统设置：图片池目录 + 多存储地址管理（2026-08-08，已完成）

- **需求（用户）**：① 系统设置页新增「图片池目录」设置项（显示当前值 + 输入框修改 + 保存提示）；② 图片存储：**多存储地址**——后台可管理多个存储位置（本地目录/盘符），支持选择策略（最空闲/轮询/手动指定），上传文件按策略落到不同存储
- **事实核对**：用户所述「后端 API 已写好」经全库/全分支/发布副本检索**不成立**——后端 `SETTINGS_KEYS` 无 image_pool 键；但**多存储地址 API 已完整存在**（《后端API设计.md》§7：`GET/POST /storages`、`PUT/DELETE /storages/{id}`、`GET /storages/health`，权限 sys:config，策略 fill/round/manual，有文件禁删），前端此前完全未实现
- **实现**：
  - 后端 `app/api/system.py`：`SETTINGS_KEYS` 新增 `image_pool.dir`（str）——补齐用户要求的设置项后端
  - `backend/tests/test_settings.py`：新增 `test_settings_image_pool_dir_roundtrip`（GET 空串 → PUT 保存 → 读回；测试后恢复原值）
  - 前端 `packages/shared/src/api/system.ts`：Settings 接口加 `image_pool.dir`；新增 `StorageItem`/`StoragePayload`/`StorageHealth` 类型与 `storageApi`（list/create/update/remove/health）
  - 前端 `apps/desktop/src/pages/Settings.tsx`：基础设置 Tab 新增「**图片池与存储**」分区——图片池目录输入框 + 存储地址管理表格（名称/路径/策略 Tag/启停用/默认/健康状态/文件数/操作：设默认、编辑、删除（Popconfirm，有文件禁删由后端提示））、「新增存储位置」「检测存储健康」按钮、新增/编辑 Modal（名称/路径/策略 Select/默认与启用 Switch/备注）；健康检测结果以 Tag 展示（路径不存在/不可写/正常·剩余空间）
- **验证**：`python -m compileall` 零错误 ✅；`pytest tests` **156/156 通过**（含新增 image_pool.dir 读写与既有 storage 测试）✅；`tsc --noEmit -p apps/desktop` 零错误 ✅；`npm run build -w wlt-desktop` 成功 ✅
- **提交**：feat(settings)（随本记录一并提交）
- **遗留**：`开源发布/` 代码副本未同步（按发布流程处理）；版本未升级（0.1.0 保持，非里程碑收尾）

## 测试执行分层策略（2026-08-08，已完成）

- **需求（用户）**：当前每次代码改动都触发全量测试太耗时——改为只有发布、主干构建、关键变更才跑全量；普通改动只跑与本次修改相关的针对性测试；保留现有测试用例不变，只控制触发时机与执行范围
- **实现**：
  - `scripts/run_tests.sh`：分层测试执行脚本——默认按 `git status`（或 `--changed=` 显式列表）自动映射相关测试：`app/api|schemas/<mod>.py` → 对应 `tests/test_<mod>.py`（auth→auth+auth_ext 等），services 按模块映射（无映射回退全量），前端 desktop/mobile/shared 变更 → 对应端 `tsc`，纯文档自动跳过；**关键路径**（models/sql/db/config/main/scheduler/core/stock/init/scripts/requirements/前端根配置）命中即升级全量；**无法映射的新改动安全回退全量**；`--full` 全量（compileall + pytest 全量 + 两端 typecheck + build）；`--files=` 指定文件；`--dry-run` 预览范围
  - `.github/workflows/ci.yml`：PR → 针对性测试（与目标分支 diff）；main 推送 / `v*` tag / 手动触发 → 全量；MySQL 5.7 service + init.sql 导入
  - 《开发规范.md》新增 §6.1 测试执行分层策略（场景表 + 关键变更清单 + 安全回退 + CI 约定）；README 验证门禁与测试命令段同步
- **验证**：dry-run 全场景验证通过（auth 映射 / models 全量 / desktop 单端 tsc / shared 两端 tsc / 纯文档跳过 / 混合映射 / --files）；真实执行针对性测试通过
- **提交**：feat(tests)（随本记录一并提交）

## 环境切换：MySQL 5.7.26 → 8.0.12（2026-08-08，已完成）

- **需求（用户）**：切换环境到 MySQL 8.0.12
- **操作**：mysqldump 导出 5.7.26 的 wuliaotong/wuliaotong1 → 停 5.7.26 → 启动 8.0.12 → 导入（数据完整：wuliaotong 576 用户/40 表、wuliaotong1 34 用户）；清理早前测试残留临时库 wlt_auto_test_*；`backend/.env` 的 `BACKUP_MYSQLDUMP` 更新为 8.0.12 的 mysqldump 路径；wuliaotong 库 admin 密码重置为种子值 admin123（旧安装 bug 写入的是用户自定义密码）
- **验证**：8.0.12 上针对性测试 13/13 通过 ✅；业务库数据/表结构完整 ✅
- **提交**：不涉及（.env 不入库）

## 共用存储池：合并「图片池目录」配置项（2026-08-08，已完成）

- **需求（用户）**：「图片池目录」与「共用存储池」是同一概念——移除重复的 image_pool.dir 配置项，统一使用「共用存储池」，优化界面更清晰易用，保留相对/绝对路径支持（用户实测保存报「未知配置项: image_pool.dir」，系旧后端进程未重启所致，合并后从根源消除该键）
- **实现**：
  - 后端 `app/api/system.py`：`SETTINGS_KEYS` 移除 `image_pool.dir`（GET 不再返回、PUT 报未知配置项 4006）
  - 前端 `packages/shared/src/api/system.ts`：Settings 接口移除 `image_pool.dir`
  - 前端 `apps/desktop/src/pages/Settings.tsx`：移除「图片池目录」输入框；分区更名「**共用存储池**」，desc 说明"单据/OCR 图片等文件统一存放于共用存储池，按策略（最空闲/轮询/手动指定）自动落盘"；新增 Alert 说明相对/绝对路径支持（相对以 backend/ 为基准）；表格列「文件数」更名「已存文件」；新增/编辑弹窗路径说明补充示例
  - `backend/tests/test_settings.py`：`test_settings_image_pool_dir_roundtrip` 改为 `test_settings_image_pool_dir_removed`（GET 不含该键、PUT 返回 4006 未知配置项）
- **验证**：`python -m compileall` 零错误 ✅；`pytest tests/test_settings.py` 全过 ✅；`tsc --noEmit -p apps/desktop` 零错误 ✅
- **提交**：feat(settings)（随本记录一并提交）
- **遗留**：8443 端口运行中的后端为旧进程，需重启后新配置生效（重启后 image_pool.dir 报错即消失）

## 共用存储池测试零残留改造 + 历史数据清理（2026-08-08，已完成）

- **需求（用户）**：修改与共用存储池相关的测试文件；测试不得向存储池添加任何垃圾数据或不必要的数据（用户强调：本地默认存储 data/files 为默认，不可删除）
- **发现的问题**：
  - `test_storage.py` 每次运行都创建测试存储并上传文件、且停用全部存储，**从不清理**——库中累积 357 个测试存储（存A/存B/手动/轮A/轮B+hex 命名）、数千条测试文件记录与磁盘文件，真实存储被停用污染
  - 首个清理 fixture 因 **MySQL REPEATABLE READ 快照**问题失效：fixture session 在 setup 时的 SELECT 开启事务快照，teardown 看不到测试期间其他会话提交的数据（排查：文件/存储查询命中 0）
  - 一次性清理脚本按「storage_id 在测试存储」+「biz_type='test'」删除记录——其余测试模块（check/ocr/other/requisition_work 等）上传时 fill 策略选中测试存储，其记录随之清理（属测试数据，正确）；但 `data/files` 磁盘遗留 43 个无记录历史孤儿文件（旧测试存储 path=data/files 时代落盘），导致设置页「已存文件」数量与磁盘不符
- **实现**：
  - `tests/test_storage.py`：新增模块级 autouse fixture `_storage_pool_cleanup`——测试前快照全部存储状态；teardown 先 `db.rollback()` 结束快照事务，再删除测试文件（biz_type=test，含历史残留自动收敛）、删除本次新增测试存储（快照差集）、恢复快照状态、清空 `tests/_tmp`；`file_path` 按存储根路径解析删除磁盘文件
  - 数据整理（一次性）：删除 357+15 个历史测试存储、2007+ 条测试文件记录、43 个无记录磁盘孤儿文件；**保留本地默认存储（id=1, data/files）并恢复 status=1/is_default=1**；`sys_file` 恢复为仅真实记录（1 条 check 文件，磁盘 1 个一致）
- **验证**：`pytest tests/test_storage.py tests/test_settings.py` 9/9 通过 ✅；测试后零残留：存储池仅本地默认存储（默认启用）、biz_type=test 文件 0、tests/_tmp 空、data/files 磁盘与记录数量一致 ✅；compileall 零错误 ✅
- **提交**：test(storage)（随本记录一并提交）
- **遗留**：`data/files` 下保留 5 个空业务子目录（check/ocr/other/requisition_work/test）；8443 后端旧进程需重启

## 全部测试文件数据残留审查（2026-08-08，审查完成）

- **需求（用户）**：检查全部测试文件，审查测试是否会向数据库/存储添加垃圾数据；如需添加数据，测试完毕后必须移除
- **审查范围**：backend/tests/ 全部 28 个测试文件——统计每个文件的写操作（POST/PUT/DELETE/DB 直写）与清理逻辑，并核对当前库实际残留
- **审查结论（分类）**：
  - **A 类：写入后自清理（4 个）**——`test_storage.py`（模块级 fixture 零残留）、`test_init.py`（_save_state/_restore_state）、`test_settings.py`（_restore_config）、`test_admin.py`（创建的用户/角色/备份均删除）
  - **B 类：大量写入、零清理（高风险 13 个）**——`test_advanced.py`（51 次 POST：商品/调拨/盘点/其他出入库/库存流水/预警通知）、`test_requisition.py`（领用单/通知）、`test_stock.py`（入库/库存/流水）、`test_ocr.py`+`test_barcode.py`+`test_delivery_ocr.py`（OCR 记录/自动新增商品/LLM 日志/文件）、`test_report.py`（报表数据）、`test_company_import.py`+`test_product_code.py`（商品）、`test_supplier_norm.py`（DB 直写供应商/商品关联）、`test_auth_ext.py`（注册用户，部分删除覆盖不全）、`test_llm_log.py`/`test_quota.py`（低量日志/快照）；均依赖 `uuid4` 随机后缀保证可重复运行，但**每次运行都累积数据且从不清理**
  - **C 类：无写入、零污染（7 个）**——`test_alert_text.py`、`test_correction.py`、`test_dedupe.py`、`test_generic_parser.py`、`test_paddle_engine.py`、`test_product_template_learn.py`、`test_report_summary.py`
- **当前库实测残留**（多为历次测试运行累积）：sys_user 583、base_product 3437、base_warehouse 2614、pch_purchase_in 2093、stk_stock_log 4490、out_requisition 790、stk_other_io 589、base_supplier 581、sys_notification 15483、sys_llm_log 11713、ocr_record 507、stk_transfer 173、stk_check 131
- **建议（待用户确认后实施）**：① 为 B 类测试文件加"创建数据收集 + 测试后删除"清理（按模块 fixture，仿 test_storage 模式）；② 一次性清理当前库中 B 类测试累积残留（保留 A 类涉及的真实数据）；③ 清理后回归全量验证
- **提交**：不涉及（本次仅审查分析，未改代码）

## 测试数据零残留落地：全局自动清理 + 历史数据一次性清理（2026-08-08，已完成）

- **需求（用户）**：确认执行审查后的修复方案——① 为 B 类测试加清理（测试完毕数据移除）；② 一次性清理当前库累积残留；③ 清理后全量回归
- **实现**：
  - 新增 `tests/_data_cleanup.py`：通用测试数据清理器——按**可归因模式**（测试命名/编码/关联）删除测试商品、仓库、供应商、单据（头+明细）、库存/流水、用户、角色、部门、分类、注册申请、OCR/AI 关联、测试文件（含磁盘）；**孤儿处理**（仓库已删导致悬空的单据/货架/库位/流水/文件）；保留 admin/tester_user、种子数据（角色/权限/单位/默认分类）、本地默认存储、无法精确归因的日志类数据（操作日志/LLM 日志/预警通知/OCR 历史记录/biz_id=0 原图）
  - 新增 `tests/conftest.py`：**模块级 autouse fixture**——每个测试模块结束后自动清理（含历史残留，自动收敛）；修复 **MySQL REPEATABLE READ 快照**问题（teardown 先 rollback）
  - 修复 4 个测试的顺序依赖（依赖其他模块残留商品）：`test_alert_text.py`/`test_product_template_learn.py` 的 `_any_product` 与 `test_dedupe.py` 的 unit 改为**自建依赖数据**（库空时创建，测试后由清理器移除）；`test_requisition.py::test_notification_flow` 直接 DB 插入通知后**显式失效 unread 缓存**（30s TTL 缓存时序缺陷）
  - 一次性清理当前库：删除历次测试累积的 3000+ 商品、2600+ 仓库、2000+ 入库单、4000+ 库存流水、580+ 测试用户、357+ 测试存储等（**保留真实/种子数据**）
- **验证**：`pytest tests` **173/173 通过** ✅（含用户新增的 test_loop_guard/test_ratelimit）；测试后零残留复核：业务表（商品/仓库/供应商/单据/库存/用户/角色/部门）全部归零或仅种子数据 ✅；compileall 零错误 ✅
- **提交**：test(cleanup)（随本记录一并提交）
- **遗留**：① `sys_file` biz_id=0 的 OCR 原图、`ocr_record`、`ai_suggestion`、`sys_llm_log`、`sys_notification`、`sys_operation_log` 为无法精确归因的历史数据（未删，可手动按需清理）；② 用户新增的 `test_loop_guard.py`/`test_ratelimit.py` 为未跟踪文件，未改动（其创建的数据若匹配测试模式会被自动清理）

## 开源发布准备：同步最新代码到发布副本（2026-08-07，已完成）

- **需求（用户）**：将源项目最新代码同步到 `开源发布/`
- **变更内容**（源新增）：
  - 后端：接口限流（`core/ratelimit.py`、`core/loop_guard.py`、错误码 4008、HTTP 429）、`config.py`/`.env.example` 新增 RATE_LIMIT_* 配置、OCR AI 训练脚本 `scripts/ocr_ai_train.py`、测试零残留清理器 `tests/_data_cleanup.py` + `tests/conftest.py` 模块级自动清理、`api/init.py`/`api/system.py`/`core/response.py`/`db.py`/`main.py`/`schemas/init.py` 等更新、新增 `tests/test_loop_guard.py`/`test_ratelimit.py`
  - 前端：`Init.tsx`/`Settings.tsx`/`api/system.ts`/`index.ts` 更新（限流与系统设置联动）
  - README：新增「数据库可手动预建」注释、生产部署说明更新
- **脱敏处理**：源 config.py/.env.example 已自行改为 `CHANGE_ME` 占位符（直接同步）；副本 README 144 行内网 IP `<内网IP>` → `<内网IP>`（此前遗漏项，本次修复）；verify_ocr_improvements.sh 保留脱敏版注释
- **验证**：compileall 零错误 ✅；敏感扫描 0 命中（仅物料编码数字误报，非手机号）✅；剩余差异仅 verify_ocr_improvements.sh 注释路径（预期脱敏）✅
- **提交**：不涉及（副本不入库）

## 全量代码审查修复（2026-08-17，已完成）

- **需求（用户）**：以资深全栈架构师视角对 `backend/` 与 `frontend/` 全部代码做系统审查（正确性/安全/性能/架构/可维护性/测试/配置部署/文档八维度），并逐项修复审查报告发现的问题
- **审查结论**：总体评分 **B（良）**；基线验证——后端 pytest 173/173 通过、前端 typecheck 与双端 build 通过；按严重程度定位 🔴2 / 🟠14 / 🟡7（含既有未提交的同类安全加固一并收口）
- **修复（按报告条目）**：
  - 🔴 严重：① OCR/条码/模板训练接口按 file_id 越权读取任意文件（IDOR）——`_read_file_bytes` 增加文件归属校验（本人或超管/管理者/仓管员，与 /files/{id} 语义一致），全部 5 个调用点传入当前用户，`/barcode/decode` 与模板训练接口补 user 依赖；② 生产 Nginx 反代下接口限流失效（所有请求记到 127.0.0.1）——限流中间件支持 uvicorn `--proxy-headers` 重写后的真实 IP，并对回环代理 + X-Forwarded-For 做兜底解析（直连伪造 XFF 不采信），README 生产 Nginx 与 uvicorn 启动命令同步更新
  - 🟠 中等：③ `init.sql` 移除 admin/admin123 固定口令，改为不可登录占位（`!`），强制经安装向导设置密码，README 默认账号章节同步；④ Redis 客户端故障短路可恢复并实现**自治愈**（30s 退避自动重连、运行中操作失败快速降级、初始化热切换立即 reset），新增 `REDIS_RETRY_SECONDS`；⑤ 修复「记住登录」30 天会话在 Redis 回填/续期后被缩短为 8 小时——会话 meta key 保存总时长、回填 TTL=剩余时长、续期阈值=总时长/2；⑥ 系统设置 `session.expire_hours` 真正接入登录/续期（保存即生效），移除无效的 `bill.rule` 死设置；⑦ 认证验证码/重置码/失败计数/邮件与注册额度、OCR 任务表与模板学习表、限流器 key 全部有界（TTL/上限/淘汰/周期清理）；⑧ 报表分页下沉 SQL（/reports/stock 独立 count+limit）、流水/库存导出与进销存汇总加行数上限防 OOM；⑨ 前端 401 跳转按 BASE_URL 生成（手机端 /m/login）；⑩ 统一 `apiBase()`，消除 10 处硬编码 /api/v1；⑪ 历史采购价接口补 stk:query/pch:in/pch:ocr 权限，桌面菜单补入口；⑫ 完成工作照片提交校验本人上传；⑬ pytest 支持 TEST_DB_URL 测试库隔离 + wlt-test: Redis key 前缀，admin/admin123 测试契约自愈；⑭ 桌面/手机端全部页面 React.lazy 路由懒加载（桌面主 chunk 1656KB→888KB）；⑮ 审计落库改用 get_running_loop + 信号量上限（500），防 executor 队列无界
  - 🟡 建议：⑯ 新增 backend/requirements.lock（pip freeze）；⑰ 找回密码每 IP 10 分钟 3 次 + 同账号发信 60s 冷却、注册每 IP 每小时 5 次；⑱ POST /init 默认仅允许本机/内网来源（INIT_ALLOW_PUBLIC）；⑲ LLM 错误信息对客户端脱敏（详情只进日志/调用记录）；⑳ 移动端通知角标真正轮询展示（修复死轮询）；㉑ 水印字体支持 WATERMARK_FONT_PATH 与 Linux 常见中文字体；㉒ fileApi.upload 统一错误处理、OCR 轮询第 24 次成功不再误报超时、BarcodeScanner 卸载后不再调度
  - 既有未提交的安全加固一并收口：admin 防提权（创建/修改/授予超管角色）、上传 biz_type 路径穿越白名单、init 表单 host/库名注入防护、schema 字段长度限制等
- **验证**：后端 `compileall` 零错误 ✅；`pytest tests` 全量 **173/173 通过** ✅；`TEST_DB_URL` 隔离模式 16/16 通过（独立库 wuliaotong_test 实建验证）✅；前端 `npm run typecheck` 零错误 ✅；`npm run build`（desktop+mobile）成功 ✅；desktop 主 chunk 由 1656KB 降至 888KB、页面拆分为 30+ 独立 chunk ✅
- **问题**：① 限流修复依赖生产运维按新版 README 配置 Nginx（X-Real-IP/X-Forwarded-For）与 uvicorn（--proxy-headers --forwarded-allow-ips=127.0.0.1），否则不生效；② 测试库 wuliaotong_test 已在本机创建（含种子数据），可复用/按需 DROP；③ 节点模块 npm workspace 符号链接因项目目录迁移损坏，已重建 junction（新环境直接 `npm ci` 无此问题）
- **提交**：fix(review) `050781c`——50 files changed, 1710 insertions(+), 440 deletions(-)（含既有未提交安全加固与新增 excel_guard.py/requirements.lock/ocr_train_from_photos.py/upgrade_indexes.sql）
- **遗留**：① 生产 Nginx/uvicorn 配置需运维侧落地；② `开源发布/` 代码副本未同步（按发布流程处理）；③ CSP/HSTS/X-Frame-Options 等安全头、缓存击穿防护、CI 全链路（audit/pip-audit）列入下一轮长期优化