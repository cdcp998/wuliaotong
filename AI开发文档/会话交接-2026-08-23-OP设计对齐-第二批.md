# 会话交接 —— UI 批量二（风格更新 / 新 OP 设计 / 入口移除 / 故障弹窗）（2026-08-23）

> 本文件供新会话直接复制全文继续任务。上一份交接见 `AI开发文档/会话交接-2026-08-23-OP设计对齐.md`（那一轮已提交：`c93329a` + `005436c`）。

## 一、环境与入口（不变）
- 工作目录：`G:\wuliaotong_dev`（前端 frontend/，后端 backend/）
- 设计基准：`AI开发文档/OpenPencil设计稿/物料通UI全界面设计.op`（62 页，4.67MB JSON；OpenPencil MCP 已载入）
- 文字规格：`AI开发文档/UI设计交付文档.md`
- 实机：桌面 dev `https://localhost:5174`（HMR 生效）、后端 `https://localhost:8443`，**本轮由我后台启动**：前端 pwsh 后台 job `npm run dev:desktop`（G:\wuliaotong_dev\frontend）、后端 `.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8443 --ssl-keyfile certs\dev\key.pem --ssl-certfile certs\dev\cert.pem`（G:\wuliaotong_dev\backend）。手机端 5175 未启（本批无手机改动）
- 登录：admin / admin123
- 开发库：MySQL 127.0.0.1:3306/wuliaotong（root/<密码见 backend/.env DB_URL>）
- 视觉代理：hohai，baseURL `https://api.hohai.eu.org/v1`，Key 在 `C:\Users\CDCP\.dsh\.credentials.yaml`（HOHAI_API_KEY），模型 deepseek-v4-flash-vision-exp
- 本机无视觉能力；COI 适配器 kimi/hermes 未安装；codex 可用（未用）
- ⚠️ 子代理并发会触发本环境 LLM API「1 分钟内最多 30 次」限流 → **同一时刻只派 1 个子代理**，顺序执行

## 二、本批任务清单与完成状态（全部代码完成，typecheck 零错误，未提交）

### ✅ 1. 故障管理「故障上报」改弹窗
`frontend/apps/desktop/src/modules/cable/CableFaults.tsx`：右侧常驻玻璃面板移除（WarningOutlined 等一并清理），页头保留「+ 故障上报」主按钮，点击开 Modal（640 宽，表单：关联线缆/类型/严重度/描述/地图选点/照片 + 取消/提交上报）；表格满宽。

### ✅ 2. 入口隐藏（送货单识别入库 id=11、领用审计 id=21）
- 开发库已 UPDATE：`sys_menu SET visible=0 WHERE id IN (11,21)`（已确认）
- 种子同步：`backend/sql/init.sql`、`backend/scripts/migrate_menu.py` 两行 visible 1→0
- 路由/页面保留（/ocr/delivery、/requisitions 仍可直达）

### ✅ 3. 按 OP 新设计 11 页（全部：页头 h2+副题+右侧按钮规范 / wlt-glass 筛选条 / wlt-glass 表格卡+原生 Table / 胶囊状态色 / 主色 #5B7FFF）
| 页面 | 文件 | 要点 |
|---|---|---|
| 供应商管理(15) | pages/Suppliers.tsx | Excel 导入（后端 /suppliers/import 已有）+ CSV 导出 + 新增供应商；列 供应商(待合并胶囊)/编码/联系人/电话/最近供货/状态/操作；新增「最近供货」字段（后端） |
| 采购计划单(18) | pages/PurchasePlans.tsx | 列 计划单/编制人/材料/数量/已入库/申请日期(MM-DD)/状态/操作(链接式)；新建计划主按钮 |
| 材料入库(19) | pages/PurchaseIn.tsx | 页头「送货单 OCR 识别」描边+「新增单据」主按钮；弹窗 Meta 改 Field/Value 字段块；列表包 glass；其余扫码/OCR/计划带入等逻辑原样 |
| 仓库与货架(17) | pages/Warehouses.tsx | 左 300px「仓库列表」白卡（选中 #EAEFFF）+ 右侧筛选条（搜索库位/材料+汇总胶囊）+ 分层货架；「按单位过滤」因 LocationStock.items 无单位字段**未实现**（已注明差异） |
| 历史价格(21) | pages/HistoryPrice.tsx | 列 材料/供应商/单价/涨跌胶囊(+x%红/-x%绿/持平)/单据/日期；「近 N 天」新增后端 start 参数；导出 CSV |
| 库存调拨(22) | pages/Transfers.tsx | 列 单号/材料/数量/调出/调入/状态(待审核橙/已完成绿/已驳回红)/审计人/日期；新增后端 keyword 搜索（单号/材料/仓库） |
| 其他出入库(23) | pages/OtherIo.tsx | 页头「新增出库」描边+「新增入库」主按钮；类型胶囊 出=红/入=绿；列 单号/类型/材料/数量/原因备注/状态/经办人/日期；借出/归还配对保留；新增后端 keyword（单号/材料/原因） |
| 任务看板(44) | modules/task/TaskBoard.tsx | 7 列白底圆角列头+圆点+数量；卡片浅底 #F6F8FE（优先+标题/描述/状态胶囊+时间）；「切换列表视图」描边+「新建任务」主按钮 |
| 任务列表(45) | modules/task/TaskList.tsx | 列 任务/优先/负责人/状态/排期/来源/操作；新增 验收/驳回 链接操作（默认结论「验收通过/驳回重做」）；「切换看板视图」描边按钮 |
| 操作日志(37) | pages/Logs.tsx | 列 时间/操作人/模块胶囊/动作/详情/IP；行点击详情抽屉；导出 CSV |
| 地图缓存(47) | modules/map/MapCache.tsx | 统计卡×4（区域/瓦片/成功+失败/失败）+ 全局进度条 + 表格（区域/模式/级别/瓦片/磁盘/状态胶囊/进度/操作） |

### ✅ 4. 风格更新 6 页（保留布局与功能，仅统一视觉语言）
`Departments.tsx`（页头+胶囊+链接按钮）、`DeleteReviews.tsx`（glass 容器+类型/状态胶囊）、`MaterialsData.tsx`（主从保留+胶囊+工具栏）、`RequisitionApply.tsx`（平铺表单保留+胶囊）、`RequisitionQuery.tsx`（筛选条 glass+状态胶囊+单号品牌蓝）、`AiLogs.tsx`（筛选条+场景/状态胶囊+共 N 条）。

### ✅ 5. 后端配套改动（已重启 uvicorn 生效）
- `backend/app/schemas/base.py`：SupplierOut + `last_supply_at`
- `backend/app/api/base_data.py`：suppliers 列表返回最近供货（PchPurchaseIn max(created_at)）
- `backend/app/api/stock.py`：history-price 接口 + `start` 日期参数
- `backend/app/api/advanced.py`：/transfers、/other-io 列表 + `keyword`（含 EXISTS 子查询）
- `backend/sql/init.sql`、`migrate_menu.py`：经营看板种子（上轮）+ 两个入口 visible=0

### ✅ 6. 工具链扩展
- `scripts/extract-op-spec.mjs`：从 .op 提取页面规格文本 → `AI开发文档/design-ref/specs/*.md`（16 页已生成）
- `scripts/probe-crash.mjs`：登录后打开单页收集 console/异常（诊断用）
- `scripts/verify-ui.cdp.mjs`：PAGES 扩到 22 条（含 6 风格页：/system/departments /delete-reviews /materials-data /requisitions/apply /requisitions/query /llm-logs）
- `scripts/prep-compare.ps1`：映射扩到 16 对（注意截图名是**连字符**：purchase-plans、purchase-in、history-price、other-io）
- `scripts/vision-compare.mjs`：PAIRS 扩到 16 页，支持 `node scripts/vision-compare.mjs <name...>` 只跑指定页
- 设计基准图已导出：`AI开发文档/design-ref/op/01-{Suppliers,Plans,PurchaseIn,Warehouses,HistoryPrice,Transfers,OtherIo,TaskBoard,TaskList,MapCache,Logs,CableFaults}.png`

## 三、验证现状
- `npm run typecheck`（desktop+mobile）零错误（每页改后都跑过）
- 16 页截图已拍（`AI开发文档/screenshots/desktop_1440_*.png`）
- 视觉比对已跑 16 页：**前 6 页正常**（dashboard/map/menus/users/suppliers/plans）；**后 10 页截图全部是登录页**（purchase-in、warehouses、history-price、transfers、other-io、task-board、task-list、cable-cache、system-logs、cable-faults）
- 崩溃诊断：`node ../scripts/probe-crash.mjs /purchase-in` 单页复测**正常**（location 正常，仅 antd Drawer width 弃用警告 + 个别 401/400 资源日志）→ 疑似批量截图时页面加载时序/资源问题，**需重拍确认**

## 四、遗留（按顺序）
1. 重拍 22 页截图（node ../scripts/verify-ui.cdp.mjs），确认 purchase-in 起不再出现登录页；若仍登录页 → 用 probe-crash 逐页定位（看 401/400 资源与 console）
2. 重跑视觉比对（至少 10 个失败页；可只跑失败页：`node ../scripts/vision-compare.mjs purchasein warehouses historyprice transfers otherio taskboard tasklist mapcache logs faults`）；风格 6 页无设计基准图，按需人工核对截图即可
3. `AI开发文档/开发进度记录.md` 顶部追加本批记录（含提交 hash）
4. git 提交：当前约 83 项改动未提交（代码/后端/种子/截图/脚本/specs）。建议两个提交：`feat(web): 风格更新6页+按OP新设计11页+故障上报弹窗`（含后端 keyword/last_supply/start 配套）+ `docs: 记录本批+截图/比对`
5. 会话目标（goal）本会话内 active，新会话续跑时用 goal 工具 resume（目标 id goal-cbd51f44-e0c0-4e35-a670-1b7c73a23e37，revision 以 get_goal 为准）

## 五、关键命令
```bash
cd G:\wuliaotong_dev\frontend
npm run typecheck
node ../scripts/verify-ui.cdp.mjs
powershell -File ../scripts/prep-compare.ps1
node ../scripts/vision-compare.mjs [name...]   # 不带参数=16页全量
node ../scripts/probe-crash.mjs /purchase-in
node ../scripts/extract-op-spec.mjs <pageId>...
```

## 六、踩坑备忘（本批新增）
- 子代理并发 → LLM API 限流（1 分钟 30 次），一次一个子代理；子代理无 openpencil MCP、无 read_image，必须由主会话提供规格（specs/*.md 已备好）
- OpenPencil MCP：`set_active_page(index)` 0 基 + `export_frames(dir)` 同消息连发会按序执行（已验证）；pageId 索引表（0 基）：14-物料数据=0, 15-供应商=1, 16-材料单位=2, 17-仓库=4, 18-采购计划=13, 19-材料入库=5, 20-送货单OCR=21, 21-历史价格=6, 22-库存调拨=7, 23-其他出入库=8, 26-领用申请=10, 27-申请查询=14, 31-AI调用日志=20, 34-用户=12, 35-单位=18, 37-操作日志=15, 43-故障=29, 44-看板=30, 45-列表=31, 46-地图工作台=32, 47-地图缓存=33, 52-导航管理=38
- verify-ui 截图文件名把路由 `/` 换成 `-`（如 purchase-plans），prep-compare 映射必须一致（已对齐）
- antd v6：Drawer 的 width 已弃用（警告，非错误，后续可换 size）
- 后端改动无 --reload，需重启 uvicorn 才生效
- 设计稿 `设计页 N` 中的部分数据/列（如调拨「创建人」、仓库「按单位过滤」）后端无字段，已用现有字段近似并在代码注释/记录中标注
