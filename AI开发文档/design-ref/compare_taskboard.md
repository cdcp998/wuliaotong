# 任务看板（设计页 44） —— 设计基准 vs 实机截图 视觉比对

【符合】
- 页头区域：标题「维修任务看板」、副题、右侧「切换列表视图」白底描边按钮 +「新建任务」主按钮均存在，文字与布局位置基本一致。

【不符合】
1. **看板主体区域（缺失元素）**：图2 中所有 7 列均显示「暂无任务」虚线占位框，没有任何实际任务卡片；图1 中每列均有卡片，包含优先级、标题、描述、状态胶囊和时间。
2. **看板列头（样式明显不符）**：图2 列名与图1 不一致。图1 可见列名为「待派发、已派发、进行中、待验收、已完成」；图2 为「待派发、已派发、进行中、完成待验、已验证、已关闭、已取消」。
3. **列头数量（明显不符）**：图2 每列数量均显示为 0；图1 列头有实际数量（如 3、2、1、2、1）。
4. **列头圆点颜色**：图2 中「完成待验」为绿色、「已验证」为橙色、「已关闭」「已取消」为红色；与图1 可见列圆点颜色（蓝、紫、绿等）不匹配。

【建议】
1. 按图1 补齐 7 个状态列的任务卡片数据，并使用浅底 #F6F8FE，卡片内需包含优先标签、标题、描述、状态胶囊和时间。
2. 核对并修正列头名称、顺序及数量显示，确保与图1 设计基准一致。
3. 同步调整列头圆点颜色，使其与图1 各状态列配色对应。
4. 检查「切换列表视图」按钮描边颜色，确保与设计图浅蓝描边一致。
